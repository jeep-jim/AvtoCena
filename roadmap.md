# АвтоЦена — постоянный roadmap и техническое задание

> Последнее обновление: 2026-07-29
> Статус: обязательный источник контекста перед любыми изменениями проекта.

## 0. Обязательный рабочий протокол

Перед изменением каталога, парсеров, расчётов, CRM, API, JSON-хранилища, GitHub Actions или деплоя необходимо:

1. Прочитать этот roadmap и связанные контракты.
2. Не менять уже утверждённый дизайн в backend-задаче.
3. Не откатывать весь проект ради восстановления одного слоя; сравнивать с последней рабочей версией и возвращать только нужную логику.
4. Изменять существующие подходящие файлы; новые создавать только при реальной архитектурной необходимости.
5. После падения workflow фиксировать run, commit, job, step, симптом и причину.
6. Добавлять или обновлять тест, который воспроизводит проблему.
7. Создавать PR, ждать полного зелёного CI и только затем вливать в `main`.
8. Не считать каталог исправленным по зелёному CI, deploy или наличию адаптера в коде.
9. После merge проверять production: новый `generationId`, `catalog/manifest.json`, `/api/catalog/market-counts`, реальные карточки, цены, характеристики и фотографии.
10. Не заявлять, что источник даёт 1 000 машин, пока это не подтверждено свежим probe/rebuild/publish-отчётом.
11. После значимого изменения актуализировать этот roadmap.

Источники истины в порядке приоритета: фактические production-данные → актуальный код и тесты → этот roadmap. При расхождении roadmap обновляется немедленно.

---

## 1. Цель продукта

**АвтоЦена** — сайт, PWA, Telegram-бот и mini app для подбора и расчёта автомобиля «под ключ» примерно за 30 секунд.

Основная ценность:

- большой реальный каталог по семи рынкам;
- поиск и фильтры по маркам, моделям, поколениям и характеристикам;
- собственная база знаний автомобилей;
- цена автомобиля в рублях;
- таможенные платежи РФ;
- утилизационный сбор;
- логистика, брокер, СВХ, лаборатория, СБКТС, ЭПТС и доставка;
- итоговая цена «под ключ» по выбранному городу;
- CRM компаний, менеджеров и клиентов;
- партнёрская CPA-система;
- автоматическое формирование договоров.

Основной домен: `avtocena.com`.

Production: Yandex Cloud.

DNS/CDN: Cloudflare + REG.RU.

Репозиторий: `jeep-jim/AvtoCena`.

---

## 2. Непереговорные архитектурные решения

### 2.1. Только JSON

Единственное хранилище данных проекта — JSON.

Не использовать PostgreSQL, MySQL, MongoDB, Firebase или другую БД без отдельного решения владельца проекта.

JSON используется для:

- каталога и generations;
- manifest и индексов;
- базы знаний моделей, вариантов и мощности;
- курсов валют;
- таможенных правил;
- утилизационного сбора;
- логистики;
- SEO;
- CRM, лидов, компаний и менеджеров;
- партнёров и CPA;
- шаблонов договоров и служебных настроек.

### 2.2. Чанки

- Обычные бизнес-JSON делить на чанки при приближении к 500 записям.
- Production-каталог хранить чанками примерно по 250 карточек.
- Не создавать один огромный JSON с тысячами объектов.

### 2.3. Реальные данные

Запрещено публиковать выдуманные объявления, цены, пробеги, характеристики, мощности и фотографии.

Сайт, API и CRM читают одну опубликованную generation.

### 2.4. Дизайн и SEO

Без отдельной UI-задачи нельзя менять утверждённый дизайн карточек и страниц.

Обязательный SEO-путь:

`логотип бренда → /cars/brand/{slug} → список моделей → /cars/brand/{slug}/model/{modelSlug}`

Metadata, canonical, breadcrumbs и sitemap являются частью продукта.

---

## 3. Семь рынков

Стабильные ID:

| ID | Рынок |
|---|---|
| `korea` | Корея |
| `china` | Китай |
| `japan` | Япония |
| `uae` | ОАЭ |
| `europe` | Европа |
| `georgia` | Грузия |
| `kyrgyzstan` | Кыргызстан |

Источник истины: `apps/web/lib/catalog/runtime-config.ts`.

### Целевой объём

- квота до 1 000 проверенных карточек с продуктивного источника за проход;
- минимум 3 независимых продуктивных источника на рынок;
- целевой bootstrap — 3 000 машин на рынок;
- бизнес-цель — минимум 10 000 реальных карточек суммарно;
- верхний публичный лимит — 30 000 машин на рынок;
- retention — 3 дня;
- автоматический проход — один раз в сутки;
- временная блокировка источника не удаляет уже проверенные карточки в пределах retention.

1 000 — квота, а не обещание. Публикуется только фактически полученное и проверенное количество.

### Бизнес-приоритет наполнения

Сначала формируется массовый сегмент:

1. итоговая рассчитанная цена до 6 000 000 ₽;
2. мощность до 160 л.с.;
3. возраст не более 6 лет;
4. реальные объявления с готовым расчётом;
5. полноценные галереи;
6. затем дорогие, старые и мощные машины.

---

## 4. Реестр площадок

Наличие адаптера означает только `registered`. Рабочий статус подтверждается production-отчётом.

Статусы:

- `registered` — адаптер есть в коде;
- `probe-live` — probe получил и нормализовал карточки;
- `productive` — источник дал опубликованные машины;
- `blocked` — CAPTCHA, 403, Cloudflare или региональное ограничение;
- `broken-route` — изменился URL, HTML или API;
- `retention-only` — свежие данные временно не получены, старые карточки сохраняются.

### Корея

Encar, K Car, Autowini, KB ChaChaCha, Bobaedream и другие зарегистрированные экспортные листинги. Encar не должен быть единственной точкой отказа.

### Китай

Guazi Global EN, Guazi RU, Che168, Autohome, Dongchedi, Taoche, Uxin, Renrenche, 58che, Ganji, Yiche/BitAuto, XCar, CN2Che, 273, AutoCango и другие подтверждённые источники.

### Япония

Japan Transit, JPAuc, CarVector, BE FORWARD, TCV, Goo-net, Carused.jp, Car From Japan, Japan Partner, Car Deal Page, PicknBuy24, Autocom Japan, EveryCar, Autorec, Nikkyo, JP Center, Prestige Motorsport и другие.

Отыгранные аукционные лоты отделять от активных экспортных объявлений. Коммерческую технику не публиковать в каталоге легковых автомобилей.

### ОАЭ

DubiCars, Dubizzle, YallaMotor, CarSwitch, BE FORWARD UAE и другие подтверждённые источники.

### Европа

mobile.de, AutoScout24, Otomoto, La Centrale, Leboncoin, Subito, Coches.net, Standvirtual, Marktplaats, Gaspedaal, Bilbasen, FINN, Blocket, Bytbil, Willhaben, Car.gr, AutoTrader UK, Motors UK, Hasznaltauto, TipCars, Bazos, AutoUncle и региональные домены.

### Грузия

Основные направления:

- MyAuto;
- AUTO.GE;
- AutoPapa;
- SS.GE;
- MyMarket.

Точные/detail-адаптеры должны иметь приоритет над общими HTML-парсерами с тем же `sourceId`.

Корректно разбирать десятичные разделители: `6,400.00` = 6 400, а не 640 000.

### Кыргызстан

Основные направления:

- Mashina.kg;
- Turbo.kg;
- Lalafo;
- Bazar.kg;
- O!Market.

Актуальные публичные маршруты Mashina.kg:

- `/en/search/`;
- `/search/`;
- мобильные эквиваленты.

Не использовать устаревший `/search/all/` как основной маршрут.

Turbo.kg использует карточки вида `/cars/{id}`. Проверять KGS/USD и принадлежность фотографий конкретному объявлению.

---

## 5. Production workflow каталога

Основной workflow:

`.github/workflows/catalog-production-recovery-v15.yml`

Имя: `Catalog source-scale daily`.

Схема:

1. `validate` — синтаксис, тесты и TypeScript.
2. `knowledge` — синхронизация и аудит базы знаний в JSON Object Storage.
3. `collect` — 7 рынков × 3 шарда = 21 job.
4. `publish` — объединение артефактов, аудит, расчёт, публикация manifest и smoke.

Ключевые параметры:

- Node.js 24;
- `max-parallel: 21`;
- `CATALOG_REBUILD_SHARD_COUNT=3`;
- `CATALOG_REBUILD_TARGET_PER_SOURCE=1000`;
- `CATALOG_REBUILD_TARGET_PER_MARKET=3000`;
- `CATALOG_PUBLISH_TARGET_PER_MARKET=3000`;
- retention `259200000` мс = 3 дня;
- максимум 30 фотографий на карточку;
- production-цель галереи — накапливать до 30 фотографий, когда источник их предоставляет;
- `CATALOG_COLLECTION_IMAGE_LIMIT=30`;
- `CATALOG_GALLERY_FAST_PATH=false`;
- `CATALOG_REBUILD_DETAIL_LIMIT_PER_SOURCE=100`;
- image concurrency = 6;
- prepare concurrency = 16;
- артефакты сжимаются и хранятся 1 день;
- cron: `17 20 * * *` UTC;
- concurrency group: `catalog-source-scale-daily`.

Минимум одна реальная фотография допускается только как аварийный старт реальной карточки. Для стабильного приоритетного сегмента цель — не менее 4 фотографий и ежедневное накопление до 30.

Пустой или малый рынок не должен уничтожать здоровую предыдущую generation. При этом такой рынок обязательно отмечается как незавершённый и не считается выполненным рубежом перед CRM.

Фатальны:

- отсутствие новой публикации;
- отсутствие `generationId`;
- полный ноль карточек во всей generation;
- невалидный manifest;
- расхождение сайта и опубликованной generation.

---

## 6. Двухэтапное наполнение и галереи

Нельзя одновременно открывать detail и скачивать до 30 фотографий для всех потенциальных машин.

### Этап 1 — массовый листинг и расчёт

1. Получить реальный листинг.
2. Извлечь ID, рынок, источник, марку, модель, год, цену, валюту, URL и фотографии листинга.
3. Нормализовать характеристики.
4. Обогатить из собственной базы знаний.
5. Рассчитать таможню и утиль по доступным данным.
6. Сохранить реальную карточку с честным статусом `needs_data`, если точный расчёт пока невозможен.

### Этап 2 — прогрессивный detail enrichment

Detail/API открывается, когда:

- нет минимальной фотографии;
- отсутствуют критические характеристики;
- приоритетная машина имеет неполную галерею.

На один источник действует отдельный бюджет detail-карточек за проход.

Галереи расширяются ежедневно и накапливаются до 30 фотографий в пределах трёхдневного retention.

Фотографии должны быть привязаны к конкретному объявлению. Запрещено подмешивать логотипы, баннеры, соседние машины и изображения другой карточки.

Shard-report обязан показывать:

- `detailLimitPerSource`;
- `detailReservationsBySource`;
- `detailSuccessBySource`;
- `detailDeferredBySource`;
- `imageStats.minimum`;
- `imageStats.maximum`;
- `imageStats.average`;
- `imageStats.preferredShare`.

---

## 7. Публичная карточка каталога

Эталон интерфейса — компактная карточка, согласованная к 25 июля 2026 года.

Структура:

1. фотография;
2. рынок и название автомобиля;
3. одна короткая подпись года/статуса;
4. одна основная цена;
5. сразу компактные характеристики: пробег, двигатель или силовая установка, мощность, 30-минутная мощность и применимая мощность утильсбора.

Обязательные правила:

- публичная цена показывается только в рублях;
- KRW, USD, JPY, CNY, AED, EUR, GEL, KGS и другие исходные валюты не выводятся как цена ни основной, ни дополнительной строкой;
- если `totalRub` готов, показывается итоговая цена в ₽;
- если полный расчёт ещё не готов, но есть корректный `sourcePriceRub`, показывается рублёвый ориентир стоимости автомобиля;
- если рублёвая цена ещё не получена, показывается `Цена по запросу`;
- использовать короткую подпись `ориентир`, а не `ориентир под ключ`;
- не добавлять строки `Цена в объявлении`, `Цена торгов`, длинные пояснения про таможню и утильсбор;
- backend-задачи не меняют размеры, типографику и количество строк карточки;
- иностранная валюта может использоваться внутри расчёта и справки курса, но не как публичная цена автомобиля.

Основные файлы:

- `docs/catalog-card-contract.md`;
- `apps/web/components/catalog/CatalogCard.tsx`;
- `apps/web/components/catalog/PriceTrend.tsx`.

---

## 8. Собственная база знаний

Основные пути:

- `data/catalog/vehicle-knowledge/**`;
- `data/catalog/power-reference/**`;
- `apps/web/lib/catalog/vehicle-knowledge.ts`;
- `scripts/catalog-sync-vehicle-models.mjs`;
- `scripts/catalog-sync-vehicle-knowledge-seed.mjs`;
- `scripts/catalog-build-vehicle-variants.mjs`;
- `scripts/catalog-build-power-knowledge.mjs`;
- `scripts/catalog-audit-vehicle-knowledge.mjs`.

Последняя каноническая сборка содержала более 8 000 моделей; production gate требует не менее 5 000 и проверяет безопасное удержание предыдущего объёма. Конкретное актуальное число подтверждается только свежим knowledge audit.

База содержит:

- марку, модель и алиасы;
- поколение и годы выпуска;
- кузов;
- двигатель и объём;
- топливо;
- коробку и привод;
- мощность ДВС;
- мощность электромотора;
- системную мощность;
- подтверждённую 30-минутную мощность;
- источник и уровень доверия;
- признаки EV/PHEV/EREV/HEV;
- варианты и комплектации.

Knowledge enrichment выполняется до тяжёлой detail-загрузки.

Если площадка не передала лошадиные силы, карточка обязана попытаться получить их из базы знаний по нормализованной марке, модели, поколению, году и силовой установке.

Пиковая мощность EV/PHEV не заменяет `power30MinKw`.

Оценочные значения маркировать как `estimated`, документированные — как `documented`/доверенные.

---

## 9. Таможня и утилизационный сбор

Расчёт выполняется кодом, а не текстовой формулой в UI.

Основные пути:

- `packages/engine/src/calculation/**`;
- `apps/web/lib/catalog/customs-pricing.ts`;
- связанные JSON-правила и тесты.

### Таможня

Учитывать:

- физическое или юридическое лицо;
- возраст автомобиля;
- таможенную стоимость или объём двигателя по действующему режиму;
- тип силовой установки;
- курс валюты на дату расчёта;
- таможенный сбор;
- пошлину;
- акциз и НДС, если применимы;
- маршрут оформления и правила ЕАЭС;
- актуальное законодательство.

### Утилизационный сбор

Учитывать:

- статус импортёра;
- личное или коммерческое использование;
- возраст;
- объём ДВС;
- тип автомобиля;
- применимую мощность по действующей шкале;
- для EV/PHEV — сертифицированную 30-минутную мощность, когда именно она требуется правилами;
- льготные коэффициенты и ограничения перепродажи;
- актуальные коэффициенты на дату расчёта.

Если обязательной мощности нет, точный итог не показывать как подтверждённый.

### Цена «под ключ»

Breakdown может включать:

- стоимость автомобиля;
- комиссию;
- брокера;
- СВХ;
- лабораторию;
- СБКТС;
- ЭПТС;
- таможенные платежи;
- утильсбор;
- международную логистику;
- доставку по РФ;
- другие подтверждённые расходы рынка.

Все строки хранятся в `calculationSnapshot`/breakdown и проверяются тестами.

---

## 10. Хранилище и публикация

Production использует JSON Object Storage в Yandex Cloud.

Обязательные переменные:

- `JSON_STORAGE_DRIVER=object`;
- `YC_OBJECT_STORAGE_ENDPOINT`;
- `YC_OBJECT_STORAGE_REGION`;
- `YC_OBJECT_STORAGE_BUCKET`;
- `YC_OBJECT_STORAGE_ACCESS_KEY_ID`;
- `YC_OBJECT_STORAGE_SECRET_ACCESS_KEY`;
- `YC_OBJECT_STORAGE_PREFIX`;
- `CATALOG_IMAGE_CDN_URL`.

Ключевые объекты:

- `catalog/manifest.json`;
- `catalog/internal/manifest.json`;
- `catalog/generations/{generationId}/offers/{market}/chunk-XXXX.json`;
- generation indexes;
- source cursors;
- cached images.

Публикация атомарная:

1. записываются chunks и индексы новой generation;
2. затем переключается публичный manifest;
3. при storage failure предыдущий здоровый manifest сохраняется.

Нельзя очищать старый каталог до успешной публикации нового.

---

## 11. Сайт, поиск, CRM и SEO

Единый источник количества машин — опубликованный `catalog/manifest.json`.

API счётчиков: `/api/catalog/market-counts`.

Сайт и CRM должны показывать одну generation.

Поиск и фильтры должны работать по всему опубликованному каталогу и использовать индексы/chunks, а не загружать все карточки в память одним файлом.

Фильтры:

- рынок;
- марка;
- модель;
- бюджет;
- год;
- пробег;
- объём двигателя;
- мощность;
- топливо;
- коробка;
- привод;
- кузов.

Брендовый rail ведёт на существующие SEO-страницы брендов и моделей.

К CRM переходим только после двух последовательных здоровых production-сборок каталога.

---

## 12. Диагностика production

Порядок:

1. Зафиксировать run, commit, время и event.
2. Найти первый реальный failed job.
3. Прочитать steps и logs.
4. Скачать artifacts:
   - `catalog-probe-{market}-{shard}.json`;
   - `catalog-rebuild-{market}-{shard}.json`;
   - knowledge audit;
   - validation report;
   - publish report.
5. Проверить `generationId`, `byMarket`, `byMarketAndSource`, `calculatedShare`, `imageStats`.
6. Сравнить manifest с `/api/catalog/market-counts` и реальным сайтом.
7. Исправить минимально необходимый слой, добавить тест, прогнать CI и только затем merge.

Не делать:

- бесконечные trigger-коммиты;
- технические PR только ради запуска;
- считать cancelled run ошибкой парсера без проверки concurrency;
- снижать качество до фейковых карточек;
- делать один внешний сайт фатальным для всего каталога;
- удалять retention при временном сбое;
- заявлять об исправлении до production-проверки.

---

## 13. Definition of Done каталога

Каталог считается готовым к переходу на CRM только когда две последовательные production-сборки подтверждают:

- CI зелёный;
- workflow `Catalog source-scale daily` завершён;
- создан новый `generationId`;
- manifest опубликован атомарно;
- сайт и `/api/catalog/market-counts` читают эту же generation;
- присутствуют все 7 рынков;
- Грузия и Кыргызстан имеют реальное массовое наполнение, а не 0–2 карточки;
- доступны `byMarket` и `byMarketAndSource`;
- видны фактически productive источники;
- нет фиктивных объявлений;
- ссылки ведут на реальные карточки;
- публичные цены показываются только в рублях;
- у рассчитанных машин есть customs/utilization breakdown;
- без подтверждённой 30-минутной мощности точный EV/PHEV-утиль не выдумывается;
- карточки получают характеристики из базы знаний, когда источник их не передал;
- галереи содержат реальные фотографии конкретного автомобиля и прогрессивно растут до 30;
- `imageStats.maximum` достигает значений больше 1 и до 30;
- поиск, фильтры, брендовые и модельные страницы работают;
- следующий ежедневный проход не уничтожает предыдущую базу;
- суммарный каталог движется к бизнес-цели минимум 10 000 реальных машин.

---

## 14. Журнал инцидентов и решений

### 2026-07-28 — run #91

Симптом: workflow каталога завершился красным после полезной работы.

Причины: внешний smoke и слишком строгий первый gate могли сделать run красным; artifacts перегружали Actions.

Исправление: PR #131, merge `c8704e20f305c4c7f47d6f0bb1c0c4baf8f20b5e`; smoke стал диагностическим, retention сохраняется, artifacts сжимаются и хранятся 1 день.

### 2026-07-28 — run #92, опубликовано 1 158 машин

Фактические счётчики:

- Япония 2;
- Китай 0;
- Корея 68;
- ОАЭ 84;
- Европа 1 002;
- Грузия 0;
- Кыргызстан 2.

Причина: quality gate требовал готовую мощность, таможню, утиль и `totalRub` до публикации; реальные объявления с pending-расчётом удалялись.

Решение: отделить достоверность объявления от готовности расчёта и сохранять реальную карточку со статусом `needs_*`.

### 2026-07-28 — расхождение счётчиков и карточек

Симптом: CRM показывала 5 006 машин, публичная страница показывала «Каталог обновляется».

Причина: публичная DTO повторно проходила внутренний валидатор, требующий удалённый `operational.sourceUrl`.

Исправление: PR #137, merge `d2d8f13bac2c637df8d04d355f5a11326d00d75b`.

### 2026-07-28 — восстановление фото, характеристик и SEO

Симптом: одна фотография, отсутствующие характеристики и брендовый query-фильтр вместо модельной страницы.

Исправление: PR #138, merge `dea207f6d66ee9ce10b8635de1ad44430070706b`; возвращены SEO-маршруты, detail-галереи и расчётный pipeline.

### 2026-07-28 — run #97, перегрузка полного detail

Симптом: production run упал примерно через 1 час 16 минут, новая generation не подтверждена.

Причина: detail и до 30 фото открывались почти для каждой потенциальной карточки.

Исправление: PR #139, merge `70481c1cc698a15c6e895415567664773e2bab8b`; knowledge и расчёт выполняются до detail, detail ограничен бюджетом, галереи накапливаются через retention.

### 2026-07-29 — карточки показывали иностранную валюту и лишний текст

Симптом:

- Корея показывала цены в KRW;
- Китай и ОАЭ показывали USD;
- карточка стала выше из-за дополнительных строк;
- утверждённый интерфейс был изменён backend-задачей.

Причина:

- `CatalogCard` выводил `sourceMoney(...)`, когда `totalRub` отсутствовал;
- тесты закрепили ошибочное расширенное поведение.

Исправление:

- PR #147;
- CI #282 — success;
- merge `5caf069753132b5b1fb593edf50b39adb4b87633`;
- возвращена компактная структура от 25 июля;
- публичная цена теперь только в рублях;
- иностранная цена источника не выводится;
- добавлен блокирующий UI-контракт.

### 2026-07-29 — Грузия 0, Кыргызстан 2

Симптом: два региональных рынка фактически отсутствовали на сайте.

Подтверждённая причина по реестру адаптеров:

- общие `regionalLiveOverrides` регистрировались после точных адаптеров и заменяли адаптеры с тем же `sourceId`;
- Mashina.kg использовал устаревший основной маршрут `/search/all/`.

Исправление:

- PR #147;
- точные MyAuto/AUTO.GE/Mashina адаптеры получили приоритет;
- Mashina переведён на актуальные `/en/search/`, `/search/` и мобильные маршруты;
- добавлены региональные route-тесты.

Production-результат ещё должен быть подтверждён свежим run и manifest.

### 2026-07-29 — политика галерей до 30 фото

Решение владельца: production должен накапливать до 30 фотографий конкретной машины.

Исправление:

- PR #148;
- CI #283 — success;
- merge `5ff6bb51defda3d18cdc19525a6c89b40464410c`;
- production preferred/max/network image limits закреплены на 30;
- фотографии остаются listing-bound и накапливаются прогрессивно.

Production-результат ещё должен быть подтверждён `imageStats` новой generation.

---

## 15. Текущие приоритеты

1. Проверить первую production-сборку после merge `5ff6bb51`.
2. Подтвердить новый `generationId` и совпадение сайта с manifest.
3. Проверить все семь `byMarket`, особенно Грузию и Кыргызстан.
4. Проверить `byMarketAndSource` и фактически productive площадки.
5. Проверить, что публичные карточки нигде не показывают иностранную цену.
6. Проверить `calculatedShare`, customs/utilization breakdown и 30-минутную мощность.
7. Проверить `imageStats`, реальные галереи и накопление до 30 фото.
8. Проверить поиск и фильтры на главном каталоге, страницах рынков, брендов и моделей.
9. Повторить всё на следующем ежедневном production-проходе.
10. Только после двух здоровых проходов перейти к CRM, CPA и договорам.

---

## 16. Правило обновления roadmap

Обновлять файл при изменении:

- рынков и источников;
- URL/HTML/API площадки;
- статуса источника;
- shard count;
- target per source/market;
- retention;
- требований к изображениям;
- публичного UI-контракта;
- JSON-структуры;
- manifest и индексов;
- workflow/cron/concurrency;
- таможенного алгоритма;
- коэффициентов утильсбора;
- правил мощности;
- базы знаний;
- CRM/CPA/договоров;
- production-инцидента;
- важного решения владельца проекта.

Каждое существенное изменение должно оставлять след: дата, причина, изменённые файлы, PR/commit, CI и фактический production-результат.


---

## 17. Накопительный протокол roadmap — решение владельца от 2026-08-27

> Зафиксировано: 2026-08-27 10:20 (+07).
> Эта и все последующие датированные главы дополняют историю. При противоречии более новая датированная запись имеет приоритет над старой, но старый текст не удаляется.

### 17.1. Постоянное правило

`roadmap.md` в корне репозитория — обязательная пояснительная записка и точка восстановления контекста для любого нового чата, агента и рабочей сессии по проекту «АвтоЦена».

Перед началом любой задачи исполнитель обязан:

1. Открыть и полностью прочитать `roadmap.md`.
2. Найти последнюю датированную запись и текущий раздел «Что осталось».
3. Сверить roadmap с текущим `main`, активными GitHub Actions и production; непроверенный старый факт не использовать как действующее правило.
4. Зафиксировать исходное состояние: branch/SHA, активный writer, live `generationId` и относящиеся к задаче публичные симптомы.
5. Выделить границы задачи и явно сохранить запреты владельца: какие слои нельзя трогать.
6. Не повторять уже выполненное и не восстанавливать старую реализацию поверх более новой без доказанной необходимости.

Во время работы:

1. Использовать отдельную ветку и PR.
2. Делать минимальное изменение в подтверждённом проблемном слое.
3. Добавлять тест на воспроизведённую причину.
4. Не сливать PR до полного зелёного CI.
5. Не запускать конкурирующий catalog writer и не терять сохранённые shard-артефакты.
6. Не считать задачу завершённой только по коду, CI или deploy: обязательна фактическая production-проверка.

После завершения каждой задачи исполнитель обязан только **дописать** в конец `roadmap.md` новый датированный блок. Старые записи не переписывать и не удалять. Если старое утверждение стало неверным, добавить новую запись вида «заменяет/отменяет раздел или решение …» с причиной и доказательствами.

Каждый новый блок должен содержать:

- запрос и границы задачи;
- что было до изменения;
- установленную причину;
- что сделано и какие файлы изменены;
- PR, commit, CI и deploy/run;
- фактический production-результат и проверенные числа;
- что не трогалось;
- оставшиеся ограничения или блокеры;
- следующий безопасный шаг.

Команда владельца **«читай роадмап»** означает: сначала открыть этот файл в корне `jeep-jim/AvtoCena`, полностью прочитать его и только затем продолжать работу.

---

## 18. Актуальная карта проекта на 2026-08-27

### 18.1. Какие старые разделы теперь являются только историей

- Перечни площадок в разделе 4 не являются разрешением на их использование. Единственный действующий источник истины — центральный allowlist в текущем коде и его тесты. Упоминание площадки в старой записи не делает её разрешённой.
- Описанный в разделе 5 workflow `catalog-production-recovery-v15.yml` больше не является текущей автоматической схемой. Действующая очередь описана ниже.
- Правило раздела 7, допускавшее публичную надпись `Цена по запросу`, отменено. Непосчитанные предложения остаются во внутренней базе и не публикуются.
- Приоритеты раздела 15 от 2026-07-29 выполнены или заменены последующими решениями; текущие задачи указаны в разделе 18.8.

### 18.2. Что было перед текущим циклом исправлений

- В репозитории оставался устаревший ручной workflow с запрещёнными KB ChaChaCha и OTOMOTO.
- Live-каталог показывал общий счётчик, но страницы рынков, брендов и моделей повторно отбрасывали корректные компактные V3-проекции; из-за этого карточки могли быть видны на главной, а `/cars`, модельная страница и блок похожих автомобилей были пустыми.
- Публичная энциклопедия смешивала реальные автомобили с сырыми названиями источников, пустыми марками и справочным мусором.
- В production оставались 539 карточек без полного расчёта: Китай 421, Европа 60, Кыргызстан 58; интерфейс показывал `Цена по запросу`.
- Япония имела 964 публичные машины, хотя в сохранённом shard Prestige содержалось 1 392 реальных проданных лота 2010 года и новее с точной ценой в JPY и полным расчётом. Значительная часть отбрасывалась общим правилом `totalRub / carPriceRub < 8`, которое некорректно исключало дешёвые подтверждённые японские лоты, когда фиксированные доставка, таможня и оформление были дороже самого автомобиля.

### 18.3. Что сделано в текущем цикле

1. PR [#708](https://github.com/jeep-jim/AvtoCena/pull/708): полностью удалён устаревший `.github/workflows/catalog-live-daily-working-markets.yml`; тесты теперь требуют отсутствия этого writer. Полный CI, deploy и послепродовые проверки были зелёными.
2. PR [#709](https://github.com/jeep-jim/AvtoCena/pull/709), commit `bc9b58ff27140e58648fdb573ada99cedf7396ce`: действующей последовательной V3-очереди добавлен строго ограниченный операторский marker `.github/catalog-v3-run-all`; расписание сохранено.
3. PR [#710](https://github.com/jeep-jim/AvtoCena/pull/710): Encar для Кореи восстановлен через production-egress АвтоЦены; `sourceId`, URL и парсер остаются Encar. Исправлена безопасная очистка запрещённых старых хвостов.
4. PR [#711](https://github.com/jeep-jim/AvtoCena/pull/711): исправлен отсутствующий импорт `isCatalogMarketSourceAllowed`, из-за которого публикации всех рынков падали после успешного сбора.
5. PR [#712](https://github.com/jeep-jim/AvtoCena/pull/712): старые записи запрещённых источников исключаются до новой публичной generation; разрешённые записи и внутренние собранные базы сохраняются.
6. PR [#713](https://github.com/jeep-jim/AvtoCena/pull/713): публичный слой больше не принимает предложения без рассчитанной видимой цены; постаудит падает при `unpricedPublicCount > 0`. Непосчитанные записи остаются во внутренней базе для будущего обогащения.
7. PR [#727](https://github.com/jeep-jim/AvtoCena/pull/727) и [#728](https://github.com/jeep-jim/AvtoCena/pull/728): восстановлено чтение компактных V3 card projections и fallback сводки каталога.
8. PR [#729](https://github.com/jeep-jim/AvtoCena/pull/729), merge `bc6d3ff6079f81ed2fcd3b990bc1c29a1eaccb32`: страницы `/cars`, бренда, модели и похожих автомобилей используют единый gate публичной V3-проекции. Похожие машины выбираются по подтверждённой точной модели, без опасного fuzzy-объединения разных моделей. Публичный автокаталог показывает только марки и модели с реальными предложениями и рассчитанной ценой.
9. PR [#730](https://github.com/jeep-jim/AvtoCena/pull/730), merge/main `d4abc15f31df7b6a97da78f7057be3a2e27bd3b1`: общий ценовой ratio-gate сохранён, но точные цены идентифицированных проданных японских аукционных лотов больше не отбрасываются только из-за дорогой логистики и оформления. Доказательство sold identity и точной JPY-цены сохраняется в компактной публичной записи.

### 18.4. Действующий production workflow

- Основная автоматическая очередь: `.github/workflows/catalog-v3-sequential-queue.yml`, имя `Catalog V3 · Sequential production queue`.
- Расписание: `17 21 * * *` UTC, то есть 04:17 в продуктовой зоне +07.
- Япония автоматически присоединяется 1, 8, 15 и 22 числа локального месяца; операторский `workflow_dispatch` или marker запускает её вне этого календаря.
- Порядок рынков: Япония при положенном дне → Корея → Китай → ОАЭ → Европа → Грузия → Кыргызстан.
- Reusable workflow: `.github/workflows/catalog-v3-market-10k-reusable.yml`.
- Каждый рынок собирается пятью shard-job: `0…4`, `max-parallel: 5`, `CATALOG_REBUILD_SHARD_COUNT=5`.
- Текущая цель Кореи, Китая, ОАЭ, Европы, Грузии и Кыргызстана: до 10 000 за рынок/источник и максимум 10 000 публичных карточек рынка за проход.
- Текущая цель Японии: до 30 000; retention 30 дней (`2592000000` мс), курсоры не сбрасываются.
- Retention остальных рынков: 3 дня (`259200000` мс).
- На карточку: минимум 2 подтверждённые фотографии для допуска, предпочтительно и максимум 30; фотографии должны принадлежать конкретному объявлению.
- Сбор и публикация последовательны; `cancel-in-progress: false`. Нельзя запускать второй writer до terminal-результата текущего.

### 18.5. Проверенное состояние production

Срез выполнен 2026-08-27 примерно в 10:17–10:20 (+07).

- `main`: `d4abc15f31df7b6a97da78f7057be3a2e27bd3b1`.
- Production deploy [run 33035324962](https://github.com/jeep-jim/AvtoCena/actions/runs/33035324962) завершён `success`.
- `/cars` показывает 27 421 публичную машину:
  - Корея — 4 764;
  - Китай — 1 875;
  - Япония — 964;
  - ОАЭ — 3 944;
  - Европа — 6 967;
  - Грузия — 5 441;
  - Кыргызстан — 3 466.
- Сумма по семи рынкам: `4 764 + 1 875 + 964 + 3 944 + 6 967 + 5 441 + 3 466 = 27 421`; она совпадает с заголовком `/cars` на момент проверки.
- `/cars/brand/toyota/model/crown` показывает реальные Toyota Crown по рынкам вместо пустой заглушки.
- Карточка Toyota Crown показывает блок `Ещё Toyota Crown` с другими подтверждёнными Crown, затем отдельную подборку по рынку.
- `/cars/autocatalog` показывает только актуальные позиции: 114 марок, 1 401 модель и 27 414 автомобилей с рассчитанной ценой; сырые названия источников и пустые справочные карточки скрыты.
- Разница в 7 машин между `/cars` и `/cars/autocatalog` зафиксирована как наблюдение текущей динамической generation; не считать её ошибкой или нормой без отдельной диагностики.

### 18.6. Состояние текущего автоматического прохода

- Последовательная очередь: [run 33028069889](https://github.com/jeep-jim/AvtoCena/actions/runs/33028069889).
- На срезе Корея и Китай завершили collect, publish и health с `success`.
- ОАЭ прошёл validate; четыре shard завершились успешно, shard `uae / collect (2)` оставался `in_progress`.
- Ошибок в завершённых job очереди на момент среза не было.
- Япония в этом плановом проходе корректно пропущена, потому что 27 число не входит в календарь 1/8/15/22.

### 18.7. Непереговорные правила текущей сборки

1. Без нового прямого указания владельца не менять парсеры, адаптеры источников и расписание: текущий сбор должен продолжать работу.
2. Использовать только источники центрального allowlist. Старые KB ChaChaCha и OTOMOTO не возвращать.
3. Не обходить CAPTCHA, Cloudflare, авторизацию или техническую защиту внешних сайтов.
4. Encar обязателен для Кореи и должен оставаться реальным Encar, без подмены другим источником.
5. Япония публикует только идентифицированные завершённые/проданные аукционные лоты с положительной точной ценой и фотографиями конкретного лота. Активные ставки и неподтверждённые JPAuc-записи не публиковать.
6. Производственный год: Япония — 2010 и новее; остальные рынки — 2020 и новее.
7. Публичная карточка обязана иметь рассчитанную видимую цену. `Цена по запросу` не допускается в public; запись без расчёта сохраняется только внутри.
8. `forbiddenSourceCount` и `unpricedPublicCount` должны быть равны нулю.
9. Не ослаблять identity, source URL, year, photo, customs/pricing и atomic-publication gates ради количества.
10. Сохранённые shard-артефакты не удалять и не перезапускать сбор, если публикацию можно безопасно повторить из них.
11. Похожие модели строить по подтверждённой идентичности. Не объединять отдельные модели только по похожей строке.
12. Пустой или полупустой блок похожих автомобилей не показывать.

### 18.8. Что осталось

1. Не вмешиваясь, дождаться terminal-результата очереди run `33028069889` по ОАЭ, Европе, Грузии и Кыргызстану.
2. После освобождения writer ровно один раз перепубликовать Японию из сохранённых пяти shard-артефактов run `32917466479` через существующий workflow `.github/workflows/catalog-republish-market-artifacts.yml` и marker `.github/market-runs/republish-artifacts`. Повторный парсинг для этого не нужен.
3. После Japan republish проверить новый `generationId`, фактическое количество Японии, `forbiddenSourceCount=0`, `unpricedPublicCount=0`, рассчитанные цены и непустой `/cars?market=japan`.
4. Ожидается рост Японии выше 1 000 за счёт сохранённых подтверждённых лотов, но это не считается выполненным, пока новый publish-report и live-каталог не покажут фактическое число.
5. Повторно проверить `/cars`, все семь рынков, модельную страницу Toyota Crown, блок `Ещё Toyota Crown` и `/cars/autocatalog` после смены generation.
6. Зафиксировать итог отдельным новым датированным блоком в конце этого roadmap. Только после этого сообщать владельцу `готово`.

---

## 19. Шаблон следующего накопительного обновления

### YYYY-MM-DD HH:MM (+07) — краткое название задачи

- **Запрос и границы:**
- **Что было:**
- **Подтверждённая причина:**
- **Что сделано:**
- **Изменённые файлы:**
- **PR / commit:**
- **CI / workflow / deploy:**
- **Production-проверка и числа:**
- **Что намеренно не трогалось:**
- **Что осталось:**
- **Следующий безопасный шаг:**

---

## 20. 2026-08-27 13:06 (+07) — финальный аудит production, расписания и объёма Японии

- **Запрос и границы:** проверить текущий проект, live-каталог, состояние парсеров, точное расписание и установить причину малого количества японских машин. По прямому правилу владельца парсеры, адаптеры и расписание в этой задаче не изменялись.
- **Что было:** после PR #729 и #730 код интерфейса, похожих автомобилей, чистого автокаталога и специального допуска точной цены японского проданного лота уже находился в `main`, но Япония оставалась на старой публичной generation с 964 карточками. Параллельно продолжалась плановая последовательная очередь run `33028069889`.
- **Подтверждённая причина:** порог года `2010+` не является причиной малого объёма. В сохранённом Prestige-shard 1 392 предложения имеют годы 2015–2026, а 1 062 сохранённых JPAuc — 2025–2026; записей младше 2010 года в этих артефактах нет. Ограничение создают доступность источников, нулевая продуктивность части адаптеров, строгие identity/calculation gates и отсутствие повторной публикации Японии после PR #730.

### 20.1. Текущее состояние кода и production

- `main`: `8bb6da4ed64b2faab4cf2f03f93042195d1a9ce4`.
- CI документационного PR #731: run `33036211285` — `success`.
- Production deploy: run `33036331478` — `success`.
- Post-deploy seven-market parity: run `33036560702` — `success`.
- Visible calculation coverage: run `33036560701` — `success`.
- Текущая live-generation после публикации Грузии: `gen_1787809895015_4f5abfe1`.
- `/cars`: 27 212 карточек: Корея 4 764, Китай 1 875, Япония 964, ОАЭ 4 782, Европа 5 860, Грузия 5 501, Кыргызстан 3 466.
- По постаудиту всех семи рынков: `forbiddenSourceCount=0`, `unpricedPublicCount=0`, `belowMarketMinYearCount=0`, `belowMinimumImagesCount=0`; число `exactCalculatedCount` равно числу публичных карточек каждого рынка.
- Карточка Toyota Crown показывает блок `Ещё Toyota Crown` с четырьмя точными Crown, затем отдельную подборку по Японии.
- `/cars/autocatalog` показывает 117 марок, 1 412 моделей и 27 143 индексируемых автомобиля; разница 9 с общим каталогом остаётся наблюдением и не признана ни нормой, ни дефектом без отдельной диагностики.

### 20.2. Действующее расписание

- Единственная плановая production-очередь сбора: `.github/workflows/catalog-v3-sequential-queue.yml`.
- Cron: `17 21 * * *` UTC = 04:17 в продуктовой зоне +07.
- Порядок: Япония в положенный день → Корея → Китай → ОАЭ → Европа → Грузия → Кыргызстан.
- Япония автоматически запускается только 1, 8, 15 и 22 числа локального месяца. `workflow_dispatch` или изменение marker `.github/catalog-v3-run-all` включает Японию вне календаря.
- Все рынки: 5 shard-job `0…4`, максимум 5 параллельно внутри рынка; публикация рынков последовательна, `cancel-in-progress=false`.
- Цель шести ежедневных рынков — до 10 000 на источник/рынок; retention 3 дня. Цель Японии — до 30 000, retention 30 дней, курсоры не сбрасываются. Это верхние цели, а не гарантия фактического количества.
- Отдельный cleanup `.github/workflows/catalog-storage-cleanup.yml` запускается в 02:40 UTC = 09:40 (+07); он не парсит площадки и безопасно чистит старые поколения/изображения.

### 20.3. Фактическая продуктивность источников

| Рынок | Фактический статус последнего проверенного прохода | Публичный результат |
|---|---|---:|
| Корея | Encar и K Car здоровы и продуктивны; свежие валидные строки 5 756 и 2 082 | 4 764: Encar 3 818, K Car 946 |
| Китай | Autohome/Che168 и Autohome new продуктивны; Dongchedi завершился `source_errors`, Guazi дал 0 свежих продуктивных строк | 1 875: Che168 1 874, сохранённый Guazi 1 |
| ОАЭ | DubiCars и Dubizzle здоровы и продуктивны | 4 782: DubiCars 2 388, Dubizzle 2 394 |
| Европа | AutoScout24 и mobile.de дали свежие строки и остаются продуктивными, но health-проверка обоих источников была деградирована; AutoScout зафиксировал `source_errors` | 5 860: AutoScout24 2 685, mobile.de 3 175 |
| Грузия | AutoPapa и MyAuto продуктивны; AutoPapa отмечен unhealthy/`source_errors`, MyAuto — healthy, остановка `no_progress` | 5 501: AutoPapa 4 942, MyAuto 559 |
| Кыргызстан | Единственный разрешённый Mashina.kg; на момент среза четыре пустых shard завершены, основной shard ещё собирается | до завершения текущего run остаётся 3 466 |

- Плановая очередь run `33028069889` стартовала на SHA `e250821210f671d5f941b613b7fb8c255b2f73de`. Корея, Китай, ОАЭ, Европа и Грузия завершили collect/publish/health успешно; Япония штатно пропущена 27 числа; Кыргызстан ещё `in_progress` на момент записи.
- Предупреждения GitHub Actions в этом run относятся к принудительному переходу actions с Node.js 20 на Node.js 24 и не являются падениями job.

### 20.4. Почему в Японии только 964 машины

1. Сегодня, 27 августа, Япония не должна была запускаться по расписанию 1/8/15/22, поэтому текущая очередь её не обновила.
2. Последний полный японский source-run `32917466479` проверил все пять обязательных источников, но полная готовность источников не достигнута.
3. Prestige: live-запрос получил HTTP 403 от Cloudflare; свежих строк 0, но сохранены 1 392 точных проданных лота.
4. JPAuc: probe завершился timeout на сохранённом курсоре 259; в артефакте сохранены 1 062 записи. Они не попали в current public из-за строгих требований к подтверждённой sold identity и полному расчёту; среди диагностических причин встречались незавершённая таможня/price breakdown, отсутствующая мощность и неизвестный powertrain.
5. JP Center: HTTP 200, но `parsed_zero` на ответе 55 533 байта.
6. Auction Data Search: HTTP 200, но `parsed_zero` на ответе 13 738 байт.
7. CarVector: probe живой, однако 25 страниц дали 25 пустых/непригодных результатов, сохранено 0.
8. В current public все 964 японские карточки происходят только из Prestige. Они полностью рассчитаны и проходят year/source/image/identity gates.
9. До PR #730 дешёвые подтверждённые проданные лоты дополнительно отбрасывал общий ratio-gate `totalRub / carPriceRub >= 8`. PR #730 уже в `main` и production-коде, но сохранённые японские артефакты после него ещё не были перепубликованы. Поэтому исправление к live-данным Японии пока фактически не применено.

- **Что сделано:** выполнен read-only аудит current main, Actions, live `/cars`, карточки Crown, автокаталога, publish/validation/postpublish отчётов свежего run и пяти японских shard-артефактов. Этот раздел добавлен накопительно.
- **Изменённые файлы:** только `roadmap.md`.
- **PR / commit:** документационный PR этого аудита; продуктовый код не менялся.
- **CI / workflow / deploy:** для продуктового состояния использованы зелёные run `33036211285`, `33036331478`, `33036560701`, `33036560702`; текущая production-очередь `33028069889` продолжает Кыргызстан.
- **Production-проверка и числа:** зафиксированы выше для generation `gen_1787809895015_4f5abfe1`; все 27 212 публичных карточек имеют рассчитанную цену, разрешённый источник и минимально допустимые изображения.
- **Что намеренно не трогалось:** парсеры, source adapters, central allowlist, cron, retention, shard count, цены/таможня, текущий writer и его артефакты.
- **Что осталось:** дождаться terminal-результата Кыргызстана; после освобождения writer отдельным разрешённым действием перепубликовать Японию из сохранённых пяти shard-артефактов run `32917466479` на текущем `main`, не выполняя повторный парсинг; подтвердить фактический рост, новый generationId и нулевые forbidden/unpriced gates. Ремонт JP Center, Auction Data Search, CarVector или маршрута Prestige требует отдельного прямого разрешения владельца, поскольку это изменение парсеров/доступа.
- **Следующий безопасный шаг:** не запускать конкурентный writer. Сначала получить terminal по run `33028069889`, затем выполнить одну Japan republish из сохранённых артефактов и проверить live-результат; расписание и парсеры не менять без нового указания.


---

## 21. 2026-08-27 14:05 (+07) — успешная перепубликация Японии из сохранённых артефактов

- **Запрос и границы:** после terminal-успеха штатной последовательной очереди один раз перепубликовать только Японию из пяти сохранённых shard-артефактов run `32917466479`; повторный сбор, парсеры, адаптеры источников, allowlist, gates и расписания не менять.
- **Что было:** PR #729 и #730 находились в `main`; deploy `d4abc15f31df7b6a97da78f7057be3a2e27bd3b1` и более новый deploy `90372c470d771ee5ce946229c9017fbf6cb38820` были зелёными. Штатная очередь [run 33028069889](https://github.com/jeep-jim/AvtoCena/actions/runs/33028069889) завершилась `success`; активного catalog writer после неё не осталось.
- **Что сделано:** marker `.github/market-runs/republish-artifacts` обновлён ровно один раз для `market=japan`, `runId=32917466479`; commit `e0475a6921d432cee8648d88b12f83e07c090722`. Повторный parsing/collect не запускался.
- **Изменённые файлы:** операционный marker `.github/market-runs/republish-artifacts`; этот накопительный блок в `roadmap.md`.
- **PR / commit:** продуктовый код не менялся; документационный PR этого блока.
- **CI / workflow / deploy:** [Japan republish run 33047121136](https://github.com/jeep-jim/AvtoCena/actions/runs/33047121136) завершён `success`.
- **Production-проверка и числа:** новая generation `gen_1787813433362_1b353465`; общий public count `28 704`. Рынки: Корея `4 764`, Китай `1 875`, Япония `1 320`, ОАЭ `4 782`, Европа `5 860`, Грузия `5 501`, Кыргызстан `4 602`.
- **Япония:** рост `964 → 1 320`, то есть `+356`; `exactCalculatedCount=1320`, `unpricedPublicCount=0`, `forbiddenSourceCount=0`, `japanSoldIdentityFailureCount=0`, `belowMarketMinYearCount=0`, `belowMinimumImagesCount=0`. Публичный источник — `prestige_japan_auctions_open`, 1 320 карточек.
- **Сохранность остальных рынков:** publish-report сохранил counts и public hashes Кореи, Китая, ОАЭ, Европы, Грузии и Кыргызстана; повторная публикация затронула только Японию.
- **Live UI:** `/cars?market=japan` непустой и показывает 1 320 карточек; модельная страница Toyota Crown показывает реальные карточки; карточка Toyota Crown содержит блок `Ещё Toyota Crown`; `/cars/autocatalog` скрывает сырые названия источников и пустые справочные карточки.
- **Что намеренно не трогалось:** коллекторы, source adapters, central allowlist, cron, retention, shard count, sold-auction identity, photo, year>=2010, forbidden-source, pricing и unpriced-public gates.
- **Что осталось:** отдельного действия по этой задаче не требуется.
- **Следующий безопасный шаг:** оставить штатное расписание работать без ручных конкурентных writer; ремонт непродуктивных японских источников выполнять только по новому прямому запросу владельца.

---

## 22. 2026-08-27 14:29 (+07) — уточнение роли Cloudflare

- **Причина уточнения:** формулировка раздела 20.4 могла быть понята как наличие Cloudflare в инфраструктуре АвтоЦены; в разделе 1 также сохранилась устаревшая строка `DNS/CDN: Cloudflare + REG.RU`.
- **Актуальное состояние:** Cloudflare не подключён к production-схеме `avtocena.com`. Действующая инфраструктура — Yandex Cloud; это зафиксировано в `docs/AVTOCENA_MARKET_AUTONOMY.md`.
- **Что означал диагноз Prestige:** адаптер АвтоЦены выполняет прямой запрос к внешнему домену `prestigemotorsport.com.au`. В диагностированном запуске внешний сайт вернул HTTP 403; при отдельной проверке его ответ выглядел как сторонняя JavaScript/cookie challenge-страница. Это характеристика ответа внешнего сайта, а не сервис или интеграция нашего проекта.
- **Точная рабочая формулировка на будущее:** «внешний сайт Prestige отклонил запрос GitHub Actions с HTTP 403». Провайдера внешней защиты указывать только при наличии сохранённого подтверждения ответа и никогда не описывать его как часть инфраструктуры АвтоЦены.
- **Слова `cloudflare` в коде:** если встречаются в общих сигнатурах обнаружения anti-bot/block-страниц, они служат только для распознавания ответа внешнего сайта и не означают подключение Cloudflare.
- **Что сделано:** документационное противоречие устранено этой накопительной записью; раздел 1 и двусмысленная строка раздела 20.4 считаются историческими и в части Cloudflare заменяются настоящим уточнением.
- **Изменённые файлы:** только `roadmap.md`.
- **Что намеренно не трогалось:** парсеры, source adapters, allowlist, каталог, расчёты, расписания, workflow, production и сохранённые артефакты.
- **Что осталось:** продуктовых действий по этому уточнению не требуется.



---

## 23. 2026-08-27 16:01 (+07) — финальный аудит расписания, японского рынка и мощности 100 л.с.

### 23.1. Область и неизменяемость проверки

- Перед аудитом прочитан настоящий roadmap; состояние сверено с актуальным `main`, GitHub Actions и публичным каталогом.
- Это диагностическая проверка. Код парсеров, source adapters, allowlist, расписания, workflow публикации, pricing engine и production-данные не изменялись.
- Текущий подтверждённый production generation: `gen_1787813433362_1b353465`.
- В публичной проекции 28 704 предложения: Корея 4 764, Китай 1 875, Япония 1 320, ОАЭ 4 782, Европа 5 860, Грузия 5 501, Кыргызстан 4 602.
- Visible-coverage и postdeploy-проверки зелёные по формальной видимости: запрещённых источников, публичных карточек без полной видимой цены и ошибок identity gate не найдено. При этом все 28 704 расчёта имеют статус `estimated`; `readyExact=0`. Полная сумма в карточке не равна доказанной точности исходных характеристик.

### 23.2. Фактическое расписание production-сбора

| Процесс | Фактический запуск | Порядок и ограничения |
|---|---|---|
| Последовательная очередь рынков | каждый день в `21:17 UTC`, то есть в `04:17 (+07)`; также ручной dispatch и специальный marker-push | один writer, `cancel-in-progress:false`; Япония при плановом запуске только 1/8/15/22 числа, затем Корея → Китай → ОАЭ → Европа → Грузия → Кыргызстан |
| Япония | 1/8/15/22 числа в составе очереди | 5 shard, retention 30 дней, target/source и target/market 30 000, priority 24 000, cursor не сбрасывается |
| Остальные шесть рынков | ежедневно | по 5 shard, retention 3 дня, target/source и target/market 10 000, priority 8 000, cursor не сбрасывается |
| Очистка object storage | заявленный cron `02:40 UTC`, то есть `09:40 (+07)` | хранить 2 поколения, grace 6 часов, staging 3 дня, не более 250 000 удалений за проход |

- Последняя последовательная очередь `33028069889` завершилась успешно 27.08.2026; Япония корректно пропущена, потому что 27-е число не входит в её календарь.
- Перед этим были failure/cancelled на нескольких плановых проходах 22–25 августа. Поэтому требование roadmap о двух последовательных здоровых production-проходах пока не выполнено.
- Последний найденный успешный scheduled cleanup — run `32927124966` от 26.08.2026. Отдельный запуск cleanup за 27.08 на момент аудита в списке Actions не найден; причина не установлена и должна быть проверена, а не угадана.
- В `runtime-config.ts` остаются декларативные цели 100 000 предложений в день, но активный queue фактически передаёт 10 000 для обычных рынков и 30 000 для Японии. Фактическим контрактом запуска являются параметры workflow; рассинхронизацию конфигурации и документации нужно устранить отдельно.

### 23.3. Готовность источников

- Корея: Encar и K Car продуктивны.
- ОАЭ: Dubizzle и DubiCars продуктивны.
- Европа: mobile.de и AutoScout24 дают данные, но AutoScout24 в последнем отчёте имел `source_errors`.
- Грузия: MyAuto и AutoPapa дают данные, но AutoPapa имел `source_errors`.
- Китай: публичный результат фактически держится на Che168 и Autohome; Dongchedi дал `source_errors`, Guazi — 0. При обязательных четырёх источниках рынок не является source-complete.
- Кыргызстан: в allowlist один источник Mashina.kg; он продуктивен.
- Япония: из пяти обязательных источников публичные карточки текущего поколения пришли только из сохранённого слоя Prestige. Рынок не является source-complete.

### 23.4. Почему в Японии только 1 320 машин

- Последняя публикация Японии `33047121136` повторно использовала сохранённые shard-артефакты полного source-run `32917466479`; нового обхода сайтов в этой операции не было.
- В пяти shard было 2 454 кандидата: JPAuc 1 062, Prestige 1 392, три остальных источника — 0.
- Prestige ранее не дал свежих строк: внешний сайт отклонил запрос GitHub Actions с HTTP 403; в повторной публикации использован сохранённый слой.
- JPAuc остановился по timeout после частичного обхода; 1 040 его строк publisher отклонил по причине `japan_auction_sold_identity_unverified`.
- JP Center вернул HTTP 200, но парсер извлёк 0 строк.
- Auction Data Search вернул HTTP 200, но парсер извлёк 0 строк.
- CarVector отвечал на probe, но 25 страниц дали 0 строк.
- Ограничение по году не является причиной: в сохранённых данных Prestige были годы 2015–2026, JPAuc — 2025–2026; записей младше установленного для Японии 2010 года не было.
- Итоговая причина малого количества: три обязательных источника ничего не извлекли, Prestige не обновился свежими данными, а основная часть JPAuc не прошла строгую проверку идентичности проданных лотов. Сам target 30 000 и нижняя граница 2010 года сбор не ограничили.

### 23.5. Дефект 100 л.с. и влияние на цену

- В `apps/web/lib/catalog/power-scenario.ts` намеренно задано `DEFAULT_CATALOG_POWER_FALLBACK_HP = 100`.
- Когда подтверждённая мощность отсутствует, `resolveCatalogPowerScenario` может подставить 100 л.с.; `applyCatalogPowerScenario` записывает это число в `powerHp`, `powerKw` и `utilizationPowerKw` с источником `power_scenario:fallback_100`.
- Эта подстановка выполняется до таможенного расчёта и делает отсутствующую мощность формально пригодной для вычисления утильсбора и итоговой цены. Существующий unit-тест прямо разрешает такую запись к публикации.
- В публичном каталоге ровно 12 655 из 28 704 предложений имеют фильтруемое значение ровно 100 л.с. — 44,09%. По рынкам: Корея 3 556, Китай 928, Япония 1 171, ОАЭ 563, Европа 20, Грузия 2 990, Кыргызстан 3 427.
- Число 12 655 нельзя автоматически считать количеством программных fallback: некоторые источники сами передают ровно 100 л.с. Например, в проверенной выборке Кыргызстана 43 из 48 записей имели явный source-unit, а 5 — `fallback_100`. Нужен отдельный provenance-аудит каждого ровно-100 значения.
- Доказанный пользовательский дефект: список Audi A6 по Корее показывал 59 предложений, первые 48 — по 100 л.с. У предложения `5d2e4daf189f1ca14c98e9ac` список показывал 3 633 975 ₽ и 100 л.с., а detail после enrichment базы модели показывал 190 л.с. и 5 121 575 ₽. Расхождение цены — 1 487 600 ₽, или 40,94% от цены списка.
- Компактная поисковая проекция вырезает provenance мощности, поэтому карточки списка могут показывать просто `100 л.с.`, а detail при открытии обогащает предложение из базы знаний и пересчитывает его. В результате список, related-карточки и detail не являются одной ценовой проекцией.
- Текущий visible-coverage gate этот дефект не ловит: 100 входит в допустимый числовой диапазон, fallback разрешён тестом, а аудит проверяет полноту видимой цены, но не равенство мощности и цены между compact list и detail.

### 23.6. Вердикт готовности и обязательная следующая последовательность

**Проект пока не готов переходить к следующему продуктовому этапу/CRM.** Расписание технически запускается, последняя очередь зелёная, но качество источников и ценовой контракт не обеспечены.

Обязательная последовательность перед переходом дальше:

1. Запретить `fallback_100` как основание публичного таможенного расчёта; предложение без доказанной мощности сохранять во внутренней базе, но не публиковать с выдуманной мощностью и ценой.
2. Не разрешать power scenario подменять сертифицированную 30-минутную мощность для EV и гибридов.
3. До записи поколения применять одно и то же knowledge-enrichment и ценообразование для compact list, related-карточки и detail.
4. Сохранять provenance мощности в публичной compact-проекции и добавить release gate: ноль публичных `fallback_100`, ноль расхождений list/detail по мощности и цене.
5. Отдельно проверить source-provided значения ровно 100 л.с., особенно массовые кластеры Кореи, Японии, Грузии и Кыргызстана: реальные значения не трогать, источник-default/заглушку исключать.
6. После исправления полностью пересчитать и атомарно перепубликовать семь рынков; подтвердить новый `generationId`, `forbiddenSourceCount=0`, `unpricedPublicCount=0`, `fallback100PublicCount=0` и parity list/detail.
7. Для Японии отдельно исправлять четыре доказанных причины — `parsed_zero` у JP Center/Auction Data Search, нулевой результат CarVector, внешний HTTP 403 у Prestige и sold-identity JPAuc — только отдельной задачей с сохранением строгого allowlist.
8. Получить минимум два последовательных здоровых scheduled production-прохода и подтвердить ежедневный cleanup.
9. Разобрать предупреждение CI: `npm ci` сообщил 4 уязвимости (3 high, 1 critical) и предупреждение безопасности для Next.js 14.2.5.

До выполнения этих пунктов зелёные deploy/coverage означают работоспособность контура, но не корректность всех публичных расчётов.

---

## 24. 2026-08-27 17:35 (+07) — запрет выдуманных 100 л.с. и единый пересчёт карточки

- **Запрос и границы:** владелец разрешил исправлять все выявленные дефекты. Первый обязательный этап — убрать ложные `100 л.с.`, исключить расчёт итоговой цены по неподтверждённой мощности и восстановить согласованность списка, похожих предложений и detail. Парсеры и расписание на этом этапе не изменялись.
- **Что было:** `resolveCatalogPowerScenario` подставлял `100 л.с.` при полном отсутствии мощности; это значение превращалось в `73,55 кВт`, попадало в таможенный расчёт и допускалось в public. Для EV/гибридов power scenario мог обходить требование сертифицированной 30-минутной мощности. Compact-проекция не сохраняла полный маркер сценария, а detail мог уточнить мощность по базе моделей без обязательного полного пересчёта сохранённой таможенной цены.
- **Подтверждённая причина:** программная заглушка была частью расчётного контракта, а не только UI. Поэтому 12 655 публичных предложений показывали ровно `100 л.с.`, и доказанная карточка Audi A6 расходилась между списком и detail на `190 л.с.` и `1 487 600 ₽`.
- **Что сделано в коде:** автоматический `fallback_100` и `source_peak_estimate` больше не создаются при отсутствии мощности; пользовательский сценарий остаётся только предварительным и не допускается к публикации; для EV/гибридов сценарий не создаёт `utilizationPowerKw` и не заменяет сертифицированную 30-минутную мощность; public-gate теперь отклоняет legacy `fallback_100`, другие неподтверждённые сценарии и ровно `100 л.с.` без provenance, но сохраняет реальное source-provided значение 100; compact-проекция сохраняет provenance и минимальный power-scenario snapshot; visible/postpersist-аудиты требуют `fallback100PublicCount=0` и `unprovenExact100PublicCount=0`; если knowledge enrichment изменяет расчётные характеристики, detail выполняет полный повторный таможенный и бизнес-расчёт.
- **Сохранность данных:** строка без подтверждённой мощности не удаляется из внутреннего каталога. Она не попадает в public до получения доказанной мощности из источника или базы знаний. Настоящие source-provided `100 л.с.` не изменяются.
- **Изменённые файлы:** `apps/web/lib/catalog/power-scenario.ts`, `customs-pricing.ts`, `power-sanity.ts`, `public-priority.ts`, `storage.ts`, `display-enrichment.ts`; два production-аудита; CI-контракт и пять целевых тестовых файлов.
- **Проверки до PR:** целевой расширенный набор `186/186` тестов — success; web и engine typecheck — success; `npm run build` — success; `node --check` двух изменённых audit-скриптов и `git diff --check` — success.
- **PR / commit / deploy:** PR, merge SHA, production deploy и live generation будут добавлены следующим накопительным блоком после фактического завершения; до зелёного CI изменения в `main` не вливаются.
- **Что намеренно не трогалось:** central allowlist, source adapters, cron, shard count, retention, Japan sold-identity и защита внешних сайтов.
- **Что осталось:** пройти GitHub CI, влить и задеплоить; последовательно пересчитать и перепубликовать семь рынков; подтвердить нулевые fallback/unproven/unpriced/forbidden counts и parity list/detail; затем отдельно исправить доказанные проблемы продуктивности японских источников, проверить cleanup, два последовательных scheduled-прохода и npm security warning.
- **Следующий безопасный шаг:** PR только с этим расчётным контуром и roadmap; после deploy не считать задачу завершённой до нового поколения всех семи рынков и live-аудита.

---

## 25. 2026-08-27 17:58 (+07) — merge строгого power-контракта

- **PR / commit:** [PR #736](https://github.com/jeep-jim/AvtoCena/pull/736) слит squash-методом в `main`; merge SHA `9cb6c1d58950b2ecb7f3c62c22520fb88c91b15b`.
- **CI:** run `33060397891` завершён `success`; GitHub прошёл функциональные тесты, web/engine typecheck и production build. Локально дополнительно подтверждены `186/186` целевых тестов и production build.
- **Production:** автоматический deploy для merge SHA не появился в Actions в течение контрольного окна. Настоящее накопительное добавление создаёт отдельный `main`-commit и одновременно служит безопасным штатным push-триггером deploy; успешность production ещё не объявляется.
- **Что осталось:** дождаться deploy commit с включённым power-контрактом, затем последовательно пересчитать семь рынков и проверить live-аудиты. До этого прежняя generation `gen_1787813433362_1b353465` остаётся историческим состоянием с массовыми 100 л.с.

---

## 26. 2026-08-27 — fail-closed чтение старого поколения во время пересчёта

- **Запрос и границы:** владелец поручил исправлять все выявленные дефекты. Во время полного пересчёта семи рынков отдельно проверено, что production немедленно перестал показывать ложные `100 л.с.`, а каталог, главная, автокаталог, похожие предложения и detail использовали один действующий публичный power-контракт. Сборщики, allowlist, cron, retention и содержимое сохранённых shard-артефактов в этом изменении не менялись.
- **Что было:** deploy строгого контракта не переписывает уже опубликованные JSON. Сайт продолжал читать generation `gen_1787813433362_1b353465`; компактная V3-проекция считалась ранее аттестованной и проверялась только по `id/market/make/model/year`. Поэтому старые `power_scenario:fallback_100` оставались в `/api/catalog/search`, счётчиках и detail до завершения многочасовой перепубликации. Старый `catalog/public/overview.json` также мог сохранять устаревшие counts и showcase-карточки.
- **Подтверждённая причина:** публичный writer уже запрещал новые fallback, но read-path `projectionCanRenderCard`, быстрые brand/facet summaries, homepage manifest counts и `getOffer` не применяли текущую проверку мощности и видимой цены к данным предыдущей generation.
- **Что сделано в коде:** compact projection допускается только при структурной целостности, реальной обложке, положительной видимой цене и отсутствии `catalogRequiredSpecificationRejectionReason`; текущие и immutable full-offers проходят тот же узкий price/spec gate без повторного source-only raw-провенанса; unsafe-строки исключаются до пагинации и подсчёта totals; market/brand/model/facet/home counts строятся из фактически видимых строк; fallback из full chunks также fail-closed; старый overview инвалидируется новым `publicPolicyVersion=2`; общий UI-helper V3 больше не считает одного старого server-attestation достаточным.
- **Сохранность данных:** исправление ничего не удаляет из внутренней базы или Object Storage. Оно временно скрывает только карточки, которые не проходят действующую публичную политику; после доказанного knowledge/source enrichment и новой атомарной публикации они могут вернуться.
- **Изменённые файлы:** `apps/web/lib/catalog/storage.ts`, `apps/web/lib/catalog/overview.ts`, `apps/web/lib/catalog/offer-quality.ts`; регрессионные тесты public safety, current read model, navigation, overview и V3 contract.
- **Проверки до PR:** затронутый CI-контур `212/212` тестов — success; web и engine typecheck — success; production build — success; `git diff --check` — success. Два не относящихся к изменению старых широких теста (`home-initial-catalog` и одна устаревшая строковая проверка deploy-аудита) воспроизводят baseline-рассинхронизацию текущего `main` и не использовались как доказательство этой правки.
- **Production-очередь:** полный последовательный run `33061297765` не отменялся и не дублировался. На момент записи четыре японских shard завершены; долгий shard `japan / collect (2)` (`JPAuc + JP Center`) продолжает `Collect real offers`. Второй catalog writer не запускался.
- **PR / commit / deploy:** будут зафиксированы следующим накопительным блоком после зелёного GitHub CI, merge и фактической live-проверки.
- **Что осталось:** провести PR/CI/merge/deploy; проверить новые безопасные live-counts и отсутствие старых fallback в list/detail; дождаться terminal-результата последовательного run, затем подтвердить новые generation всех семи рынков, source-отчёты Японии и нулевые `forbidden/unpriced/fallback/unproven` counters.

---

## 27. 2026-08-27 — единый контракт мощности в публикации и production-аудите

- **Что было:** после deploy fail-closed read-path production уже скрывал карточки с `fallback_100`, неподтверждёнными ровно `100 л.с.`, отсутствующей ценой или обложкой. Видимый каталог сократился с 28 704 до 19 143 безопасных карточек; `fallback100PublicCount=0`, `unprovenExact100PublicCount=0`, `unpricedPublicCount=0`. Однако deploy-аудит завершился красным из-за 1 643 карточек с `power_scenario:knowledge_reference`: публикационный шлюз разрешал проверенное справочное значение мощности ДВС как `estimated`, а аудит ошибочно запрещал любой префикс `power_scenario:` без различения происхождения.
- **Подтверждённая причина:** в `catalog-audit-visible-calculation-coverage.mjs` существовала отдельная широкая проверка `scenario.requiresConfirmation || /^power_scenario:/`, которая дублировала и противоречила центральному `catalogRequiredSpecificationRejectionReason`. Поэтому рабочий production был развёрнут, но deploy run становился `failure` на постаудите.
- **Что сделано в коде:** deploy-аудит теперь использует тот же fail-closed specification gate, что writer и read-path. Справочная мощность из базы модели допускается только для ДВС, остаётся помеченной `estimated` и не увеличивает `readyExact`; `fallback_100`, `customer_input`, `source_peak_estimate`, неподтверждённые ровно 100 л.с. и power-scenario для EV/гибридов по-прежнему блокируются. Обновлены оба строковых CI-контракта и добавлен unit-тест, явно различающий допустимый combustion knowledge-reference и запрещённый electric knowledge-reference.
- **Проверки до PR:** целевой контур `51/51` тест — success; общий web/engine typecheck — success; production build — success; `node --check` audit-скрипта и `git diff --check` — success.
- **Сохранность и границы:** источники, парсеры, allowlist, расписание, writer-lock и сохранённые shard-артефакты не менялись. Полный последовательный run `33061297765` не отменялся и не дублировался.
- **Production-состояние на момент записи:** старая generation `gen_1787813433362_1b353465` видима через новый фильтр: всего 19 143, Корея 1 225, Китай 1 725, Япония 149, ОАЭ 3 843, Европа 5 744, Грузия 2 152, Кыргызстан 4 305. В Korea Audi A6 осталась одна прошедшая контракт карточка с доказанными `204 л.с.`; массовые ложные A6 по 100 л.с. из списка удалены без удаления исходных данных.
- **Что осталось:** провести PR/CI/merge и получить зелёный production deploy-аудит; дождаться полного пересчёта и публикации всех семи рынков; затем проверить list/detail/related parity, японские source-отчёты, cleanup и два здоровых scheduled-прохода. Итоговую готовность до этих проверок не объявлять.

---

## 28. 2026-08-27 — фактический merge/deploy fail-closed контура и новый проход Японии

- **Fail-closed read-path:** [PR #737](https://github.com/jeep-jim/AvtoCena/pull/737) слит в `main`, merge SHA `9e98114ba6155a9a503e7b308763e07db7436eca`. После deploy старые небезопасные compact-проекции перестали считаться допустимыми только по исторической серверной отметке.
- **Единый аудит:** [PR #738](https://github.com/jeep-jim/AvtoCena/pull/738) прошёл CI run `33064497425` и слит в `main`, merge SHA `c0aeaec363c3c6108b678ef7892f748eb5088cdc`. Production deploy run `33064667528` завершён `success` на том же SHA.
- **Live-аудит после deploy:** generation `gen_1787827508485_afee7bd3`, видимых карточек 19 143; `invalidReady=0`, `invalidSpecifications=0`, `unsafePendingVisiblePrices=0`, `unpricedPublicCards=0`, `fallback100PublicCount=0`, `unprovenExact100PublicCount=0`, общий `pass=true`.
- **Япония в последовательном run `33061297765`:** пять shard завершены, publish job `98490110810` и health завершены `success`; новый рынок опубликован в generation `gen_1787827508485_afee7bd3`. Публично осталось 149 карточек, все из сохранённого слоя Prestige и все с рассчитанной ценой.
- **Фактическая воронка Японии:** 2 738 кандидатов; JPAuc 284 свежих + 1 062 восстановленных, Prestige 1 392 восстановленных и 0 свежих; CarVector, JP Center и AuctionDataSearch дали 0. После validation осталось 367, publisher отклонил 1 335 JPAuc по `japan_auction_sold_identity_unverified`, 9 по `commercial_or_identity`, 2 по `calculation`.
- **Почему не помогает граница 2010 года:** в опубликованном результате нет строк младше 2010 года не из-за фильтра, а потому что источники их не извлекли/не доказали. Knowledge CORE разрешил точный вариант только для 56 из 2 738 кандидатов; 2 682 не имеют exact core variant, у 2 712 есть обязательные knowledge gaps. У Toyota Crown в сохранённых данных было 24 кандидата Prestige 2015–2026, но отсутствовали подтверждённые drive/fuel/power, поэтому безопасной публичной карточки Crown сейчас нет.
- **Очередь:** после Японии началась Корея. Второй writer не запускался; рынки продолжают идти в порядке Корея → Китай → ОАЭ → Европа → Грузия → Кыргызстан.

---

## 29. 2026-08-27 — найден остаточный путь возврата fallback 100 из snapshot

- **Симптом на production:** live-страница марки `212` показала четыре автомобиля, из них китайский `212 T01` и кыргызстанская строка отображали `100 л.с. · уточнить`. Detail `3dba154958b08292946acde1` показывал `Расчёт по 100 л.с.` и похожие китайские автомобили с той же заглушкой, хотя release-аудит сообщил `fallback100PublicCount=0`.
- **Уточнение по энциклопедии:** `212` — реальная китайская автомобильная марка с моделью T01, поэтому удалять её как «числовой мусор» нельзя. Проблемой является происхождение характеристик конкретных предложений, а не само имя марки.
- **Подтверждённая причина:** часть старых compact V3-строк сохранила fallback только в `calculationSnapshot.powerScenario`, но не в `powerDataSource`. `catalogPowerSanity` проверял лишь `powerDataSource`, а историческая аттестация V3 после этого преждевременно разрешала строку. UI читал snapshot и честно показывал fallback, поэтому аудит/read-gate и рендер расходились.
- **Исправление подготовлено:** power-sanity теперь рассматривает `calculationSnapshot.powerScenario.source` как тот же provenance. Snapshot без явного source считается legacy `fallback_100`; combustion `knowledge_reference` остаётся допустимым, а fallback/customer/source-peak и любой electrified scenario блокируются.
- **Проверки на момент записи:** 42/42 целевых теста прошли, включая новые регрессии для snapshot-only fallback и snapshot-only combustion knowledge-reference. Парсеры, allowlist, cron, writer-lock и текущий семирынковый run не менялись.
- **Что осталось:** пройти расширенные typecheck/build и GitHub CI, слить и задеплоить; затем проверить, что live list/detail/related больше не показывают snapshot-only `100 л.с.`, а новый market writer удаляет такие строки из следующей generation окончательно.

---

## 30. 2026-08-27 — согласование автокаталога и страниц марок с живыми предложениями

- **Что было в production:** главная страница автокаталога показывала 108 марок и 1 289 «моделей», но считала моделями все уникальные marketplace-строки. Например, плитка Toyota сообщала 54 модели, а следующая страница Toyota могла показать только 40 распознанных моделей. Страница марки загружала единственную глобальную первую страницу из 48 предложений; из-за сортировки по свежести Toyota выглядела как марка только из ОАЭ и Грузии, хотя общий счётчик был 1 348. Заголовки рыночных секций показывали число загруженных preview-карточек, а не реальный размер рынка.
- **Подтверждённая причина:** автокаталог смешивал поисковые facet-строки с каноническими сущностями энциклопедии. Страница марки определяла рынки по одному глобальному top-48, поэтому свежий рынок вытеснял остальные ещё до группировки.
- **Что сделано в коде:** автокаталог считает модели через тот же `readBrandModelDirectory`, что и страница марки, и скрывает бренд без хотя бы одной распознанной живой модели. Страница марки отдельно получает точный total и до 12 preview по каждому из семи рынков, объединяет безопасные предложения без дублей и показывает в заголовке секции реальное количество. Удалена ложная подстановка «7 рынков» для пустой марки.
- **Сохранность и границы:** меняется только публичное чтение/представление. Источники, парсеры, allowlist, расчёт, object storage, cron и текущий writer не изменяются; неизвестные модели остаются доступны в обычном каталоге и не превращаются в выдуманные энциклопедические сущности.
- **Проверки до PR:** 40/40 расширенных целевых тестов и затем 29/29 UI/source-scale тестов — success; web и engine typecheck — success; production build — success; `git diff --check` — success.
- **Что осталось:** провести отдельный PR/CI/merge/deploy после завершения snapshot-power deploy; проверить live-счётчики автокаталога, все рыночные badges/sections на Toyota и других крупных брендах, а также отсутствие пустых или ложных переходов.
- **Дополнительная live-регрессия до merge:** deploy `ad57b15e67a958ca9f19323c9ed87c481a79c17c` завершился успешно, но проблемный `212 T01` остался видимым. В его compact snapshot был `powerScenario.horsepower=100`, а отдельный `powerHp` отсутствовал; первая версия guard возвращала `missing` до проверки provenance. В текущую правку добавлен fail-closed порядок: unsafe snapshot блокируется даже без `powerHp` и даже при противоречащем обычном `powerDataSource`. Новые тесты воспроизводят оба случая; production `ad57b15e…` готовым не считается.

---

## 31. 2026-08-27 — фактический merge/deploy snapshot-power и каталогов

- **PR и CI:** [PR #740](https://github.com/jeep-jim/AvtoCena/pull/740) прошёл полный CI run `33066689034` и слит squash-методом в `main`, merge SHA `dc3d807cb243c36cd12e41f506026198adfff82d`.
- **Production deploy:** run `33066923496` завершён `success`; зелёные сборка и загрузка Docker-образа, обновление Yandex Serverless Container, прогрев публичного каталога и аудит каждой видимой цены.
- **Проверка power-контракта на live:** прежняя карточка `3dba154958b08292946acde1`, у которой `fallback_100` находился только в `calculationSnapshot` и отсутствовал отдельный `powerHp`, теперь отвечает 404. На странице марки `212` удалены небезопасные предложения Китая и Кыргызстана; остались две карточки ОАЭ с явными `299` и `248 л.с.`. Snapshot-only `100 л.с.` больше не проходит read-path.
- **Проверка каталога на live:** `/cars` показывает 14 624 видимые безопасные карточки. Автокаталог показывает 64 марки, 538 распознанных живых моделей и 14 554 карточки, относящиеся к этим моделям. Toyota показывает 35 живых моделей, 1 065 карточек и шесть реально представленных рынков; секции рынков используют точные totals, а не длину preview.
- **Сохранность и границы:** источники, парсеры, allowlist, cron, retention, сохранённые shard-артефакты и единственный writer не менялись. Исправление только закрывает unsafe read-path и согласует публичные каталоги.
- **Оставшийся найденный край:** страница марки `212` после удаления unsafe-строк показывает две безопасные машины, но `0 моделей`, потому что source-master хранит имя `212 T01`, а live-проекция — `T01`. Это identity-расхождение, а не отсутствие машин; оно исправляется отдельной узкой правкой make-free alias без изменения парсеров.

---

## 32. 2026-08-27 — выравнивание source-master модели с live-именем

- **Что было:** для реальной марки `212` source-master содержит доказанную Autohome-модель `212 T01`, а предложения хранят модель как `T01`. Точный alias-индекс не связывал эти две записи, поэтому страница марки показывала две машины и одновременно `0 моделей`; автокаталог скрывал марку как не имеющую распознанных моделей.
- **Что сделано в рабочей ветке:** source-backed модель получает дополнительный alias без повторяющегося ведущего имени марки. Для `212 T01` это строго даёт `T01`; исходная модель, provenance, source IDs и технические характеристики не меняются. Правило применяется только при точном ведущем совпадении марки и не создаёт новую модель из сырой строки предложения.
- **Проверка:** добавлен поведенческий тест, который читает реальный source-master и требует alias `T01` у модели `212 T01`.
- **Что осталось:** выполнить целевые тесты, typecheck/build, PR/CI/merge/deploy; после production проверить, что страница `212` показывает одну модель и две безопасные машины, а автокаталог и обычный каталог больше не противоречат друг другу.
- **Проверки после реализации:** 24/24 целевых теста — success; web и engine typecheck — success; production build — success; `git diff --check` — success. Следующий шаг — PR/CI/merge/deploy и live-проверка.

---

## 33. 2026-08-27 — фактический merge/deploy модели 212

- **PR и CI:** [PR #741](https://github.com/jeep-jim/AvtoCena/pull/741) прошёл CI run `33067928167` и слит в `main`, merge SHA `8d1f8dad50804ee2742a0b144e5fab0daa7d683f`.
- **Production deploy:** run `33068282673` завершён `success`; зелёными завершились сборка, обновление Yandex Serverless Container, прогрев каталога и аудит видимых расчётов.
- **Live-проверка страницы марки:** `/cars/brand/212` показывает одну распознанную модель `212 T01`, две безопасные машины ОАЭ и один представленный рынок. У карточек сохранены доказанные `299` и `248 л.с.`; старые snapshot-only заглушки не вернулись.
- **Live-проверка автокаталога:** `/cars/autocatalog` показывает 65 марок, 532 распознанные модели и 14 556 машин; марка `212` больше не скрыта и показывает одну модель / две машины.
- **Сохранность:** парсеры, список источников, расписание, writer-lock и данные активного прохода не менялись.

---

## 34. 2026-08-27 — fail-closed исправление гибридной мощности

- **Что было:** часть предложений с точным названием `Hybrid`, `PHEV`, `HEV` или `e-Power` сохраняла старую метку `powertrainKind=combustion`. Из-за этого пиковая мощность могла ошибочно стать расчётной мощностью обычного ДВС. На сохранённом слое Prestige такой конфликт подтверждён у 222 из 1 392 предложений; среди них Honda Grace Hybrid, Toyota Corolla Hybrid и другие.
- **Что сделано в рабочей ветке:** добавлено определение электрифицированной силовой установки только по identity-bound полям `model/generation/trim/engineType/fuel`, без чтения меню и рекомендаций из сырой страницы. Конфликт `название Hybrid + combustion` исправляется при нормализации и блокируется публичным power-sanity gate; старые `icePowerKw` и `utilizationPowerKw` при таком исправлении очищаются. Для `e-Power` устанавливается `series_hybrid`, для Hybrid/PHEV/HEV — `other_hybrid`.
- **Prestige / Crown:** добавлено точное post-parser enrichment-правило для доказанных пар `CROWN SPORT/AZSH36W`, `CROWN SPORT/AZSH37W`, `CROWN ESTATE/AZSH38W` и `CROWN COMFORT/TSS10`. Оно использует только карточку с подтверждённой detail/photo identity и официальные Toyota/MLIT источники; устанавливает модельную разновидность, тип силовой установки и объём двигателя, но намеренно не придумывает лошадиные силы или 30-минутную мощность электромотора.
- **Границы:** парсеры, адаптеры, allowlist, cron и активный последовательный writer не менялись. Гибрид без документированной расчётной мощности остаётся во внутренней базе и не получает ложную публичную цену.
- **Проверки до PR:** 37/37 целевых тестов — success; web/engine typecheck — success; production build — success; `git diff --check` — success. Проверка сохранённого Prestige-слоя: 222 ошибочно классифицированных предложения исправляются, 24 Crown получают точную chassis/model-разметку без подстановки мощности.
- **Что осталось:** провести PR/CI/merge/deploy, проверить live-отсутствие ошибочных combustion-расчётов у Hybrid/e-Power и дождаться следующей атомарной публикации рынков. Итоговую готовность объявлять только после production-проверки.

---

## 35. 2026-08-27 — фактический merge/deploy гибридного fail-closed контура

- **PR и CI:** [PR #742](https://github.com/jeep-jim/AvtoCena/pull/742) прошёл полный CI run `33069756918`, включая power-нормализацию, typecheck и production build. PR слит squash-методом в `main`, merge SHA `25683d92f92a29861ea449d7cadfda03019238ce`.
- **Production deploy:** run `33069958121` завершён `success` за 4 мин 59 с; видимый расчётный аудит приложен как artifact `visible-calculation-coverage-25683d92f92a29861ea449d7cadfda03019238ce`.
- **Немедленный live-эффект:** fail-closed read-path удалил из старой public-проекции строки, в которых сохранился явный конфликт `Hybrid/e-Power + combustion`. Видимый общий счётчик Японии на detail-секции снизился со 149 до 88 безопасных строк; карточки с доказанным обычным ДВС не удалялись по одному только значению мощности.
- **Проверенный остаток:** в старых compact-строках часть source trim уже сокращена до `LX`/`TRNG` и не содержит слово `Hybrid`, хотя полный сохранённый Prestige shard содержит точное исходное название. Такие строки нельзя честно переклассифицировать только по compact-проекции: Honda Grace выпускалась и с обычным ДВС, и как Hybrid. Поэтому 24 live-карточки Grace со справочным `110 л.с. · уточнить` не были угаданы как гибриды и остаются до повторной полной нормализации исходного shard.
- **Текущий writer:** run `33061297765` не отменён и не дублирован. Япония опубликована на старом SHA; Корея продолжает долгий `collect (0)` Encar, четыре остальных корейских shard сохранены. Конкурирующий republish не запускался.
- **Следующая безопасная последовательность:** дождаться terminal-состояния единственного writer, затем по одному повторно опубликовать сохранённые полные артефакты рынков уже кодом `25683d92…`; проверить ноль `powertrain_identity_conflict`, ноль fallback/unproven 100, list/detail parity и новые market counts. Для электрифицированных машин без сертифицированной 30-минутной мощности публичную цену не придумывать.

---

## 36. 2026-08-28 — доказанный таймаут чтения Object Storage при последовательной публикации

- **Фактический run:** последовательный run `33144720367` завершился `failure`. Публикации Кореи, Китая и ОАЭ завершились успешно. Европа остановлена до публикации строгой проверкой источников (`autoscout_europe_open`: unhealthy, unproductive и без свежих подтверждённых строк; productive sources 1 ниже обязательных 2), Грузия — той же проверкой для `autopapa_georgia_open` (unhealthy и без свежих подтверждённых строк). Старые поколения этих рынков сохранены, источники и проверки не ослаблялись.
- **Кыргызстан:** все пять shard-артефактов сохранены, validation успешно проверил 4 516 предложений. Шаг `Publish market` работал с `2026-08-28T10:41:26Z` до `2026-08-28T12:40:20Z` и был отменён общим двухчасовым timeout job. Publish report и новое поколение не записаны; атомарно осталось предыдущее подтверждённое поколение.
- **Подтверждённая причина зависания:** `ObjectJsonStorage.signedRequest` ограничивал время только до получения HTTP-заголовков. Таймер очищался сразу после `fetch`, а последующее чтение JSON/binary/XML body выполнялось без ограничения. Поэтому Object Storage мог отдать заголовки и навсегда остановить тело одного сохраняемого chunk; publisher не переходил ни к retry, ни к отчёту и завершался только внешним timeout workflow.
- **Исправление в рабочей ветке:** тело каждого не-HEAD ответа полностью читается под отдельным ограниченным таймером до возврата из `signedRequest`. При остановившемся body запрос прерывается, получает точную ошибку `object_storage_response_timeout` и проходит существующий retry с backoff. Буфер повторно используется JSON, binary и XML-потребителями, поэтому второй неограниченный read отсутствует.
- **Регрессия и границы:** тест воспроизводит ответ, который отдаёт заголовки и не завершает body; расширенный целевой контур `39/39` прошёл. Парсеры, source adapters, allowlist, расписания, market shards и правила строгой публикации не менялись.

---

## 37. 2026-08-28 — изоляция page-partition теста перед накопительным проходом Японии

- **Фактический run:** Japan 30k run `33180284686` завершился `failure` на job `japan / validate`, до запуска collect и publish. Все опубликованные поколения и сохранённые market-артефакты остались неизменными.
- **Точный сбой:** из 97 проверок прошли 95; упали только `numeric source pages are split across three independent workers` и `mobile.de workers advance through disjoint search shards`. Японский reusable workflow штатно задаёт `CATALOG_REBUILD_PAGE_PARTITION_COUNT=5`, а эти два теста не передавали ожидаемое значение `3` явно и поэтому проверяли фактические пять partition против ожиданий для трёх.
- **Исправление:** тесты DubiCars и mobile.de теперь явно создают по три partition; отдельные Goo-net проверки по-прежнему явно требуют пять. Production-код, парсеры, source adapters, allowlist, расписания, курсоры и правила публикации не менялись.
- **Следующий шаг:** отдельный PR, полный зелёный CI и merge; после освобождённого writer-lock — один повтор накопительного Japan 30k run с сохранёнными курсорами и обязательной проверкой generation, source counts, Crown/Grace, центральных specification gates и живых list/detail/related.

## 38. 2026-08-29 — восстановлен точный владелецкий allowlist источников и очищена Япония

### 38.1. Доказанная причина появления Goo-net

- Goo-net не был автоматически выбран сайтом: `goonet_japan_exact` ошибочно находился прямо в каноническом `REQUIRED_CATALOG_SOURCES.japan`, active importer, exact/scoped/reliable registries, page partition и Yandex egress bridge.
- Прежний тест «owner-approved allowlist» был круговым: production registry сравнивался с тем же изменяемым объектом и поэтому не мог обнаружить ошибочное расширение списка.
- Исправление выполнено в [PR #750](https://github.com/jeep-jim/AvtoCena/pull/750), merge `7a9febceae39cb6b87348f6c8822ce224ad070a4`. Production deploy [run 33232162059](https://github.com/jeep-jim/AvtoCena/actions/runs/33232162059) завершился `success`.
- Goo-net удалён из всех активных путей collection и partition, из GitHub/Yandex bridge и из default source plans. Старый внутренний endpoint оставлен только как fail-closed tombstone: HTTP `410 catalog_source_not_approved`, без обращения к источнику.
- Новый независимый regression-контракт фиксирует ровно 18 разрешённых пар `market + sourceId + canonical domain` для семи рынков. Неизвестный `sourceId`, несовпадающий домен и Goo-net отвергаются централизованно до retention/publication.
- Расписания рынков не менялись.

### 38.2. Канонический набор после исправления

- ОАЭ: `dubizzle_uae_open` → `uae.dubizzle.com`; `dubicars_uae_exact` → `dubicars.com`.
- Корея: `encar_direct` → `encar.com`; `kcar_korea_open` → `kcar.com`.
- Европа: `mobile_de_open` → `mobile.de`; `autoscout_europe_open` → `autoscout24.com`.
- Грузия: `myauto_georgia_list` → `myauto.ge`; `autopapa_georgia_open` → `autopapa.ge`.
- Китай: `autohome_used_china_open` → `che168.com`; `dongchedi_china_open` → `dongchedi.com`; `guazi_china_open` → `guazi.com`; `autohome_new_china_open` → `autohome.com.cn`.
- Япония: `jpauc_japan_past_open` → `jpauc.com/auction/past`; `carvector_japan_stat_open` → `carvector.com/stat`; `prestige_japan_auctions_open` → `prestigemotorsport.com.au/auctions`; `auctiondatasearch_japan_open` → `auctiondatasearch.jp`; `jpcenter_japan_catalog_open` → `jp.center`.
- Кыргызстан: `mashina_kyrgyzstan_exact` → `mashina.kg`.

### 38.3. Контрольная перепубликация Японии только из пяти разрешённых источников

- Операционный marker commit: `334506a857e02a0132ad62c4171321c48e7fe47a`.
- [Japan run 33232591187](https://github.com/jeep-jim/AvtoCena/actions/runs/33232591187) завершён `success`; пять collector-shards и единый publish завершены успешно.
- Новый generation: `gen_1787976782995_78b7deb5`.
- Валидация попыталась обратиться ко всем пяти обязательным японским адаптерам; missing adapters и unattempted required sources: `0`.
- Собранные candidate pools: JPAuc — `2316`, Prestige — `1392`; CarVector прошёл 25 страниц без новых пригодных кандидатов; Auction Data Search и JP Center завершились source errors без сохранённых строк.
- В validation: `306` допустимых строк (`189` fresh + `117` restored), productive sources — `2`.
- В public опубликовано `87` карточек, все из `prestige_japan_auctions_open`. Старые `131` Goo-net карточка отсутствуют в новом generation.
- JPAuc: `2279` строк отклонены как `japan_auction_sold_identity_unverified`; эти строки сохранены в candidate pool, но не выданы пользователю. Дополнительно отклонены `35` commercial/identity и `2` calculation rows.
- Safety gates Японии: `forbiddenSourceCount=0`, `unpricedPublicCount=0`, `fallback100PublicCount=0`, `unprovenExact100PublicCount=0`, `incompleteSpecificationCount=0`, `suspiciousPowerCount=0`, `japanSoldIdentityFailureCount=0`.
- Crown: `0` публичных карточек; шасси и мощность не выдумывались. Grace: `23` публичных доказанных карточки.
- Detail parity: пять из пяти sampled cards разрешились из public projection; три дополнительно проверенные страницы offer вернули HTTP `200`, related rails присутствуют и не содержат Goo-net.
- Live: `/cars`, `/cars/japan`, `/cars/autocatalog` возвращают HTTP `200`; market/search generation совпадает с `gen_1787976782995_78b7deb5`.

### 38.4. Сохранность остальных рынков

- Корея — `2183`, Китай — `2872`, ОАЭ — `4613`, Европа — `5259`, Грузия — `639`, Кыргызстан — `1007`.
- Publish report подтвердил совпадение ожидаемых и фактических SHA-256 public projections для всех шести нетронутых рынков.
- Общий post-persist audit не содержит failures; forbidden/unpriced/fallback-100/unproven-100 и specification gates равны нулю на всех семи рынках.

---

## 39. 2026-09-01 — вывод Кыргызстана и переход на шесть рынков

- **Новый владелецкий контракт:** активными остаются ровно шесть рынков — Корея, Китай, Япония, ОАЭ, Европа и Грузия. Кыргызстан больше не является рынком АвтоЦены.
- **Удалённый сбор:** удалены тип рынка, настройки, registry/source slot, адаптер Mashina, regional overrides, recovery scripts, market marker и все workflow/job/cron-пути Кыргызстана. Канонический allowlist теперь содержит 17 источников шести рынков.
- **Интерфейс и публичные входы:** рынок удалён из главной, каталога, фильтров, флагов, дилерского интерфейса, CRM, Telegram и юридического footer. Старый параметр/маршрут выведенного рынка перенаправляется в `/cars`; поиск, facets и offer lookup fail-closed не читают его даже из прежнего generation.
- **Защита от возврата:** единый runtime market set используется publisher/read-model; старые строки выведенного рынка не проходят `isPublicOffer`, а current read-model republisher читает только шесть активных market keys. Отдельный regression-контракт проверяет runtime, business settings, source registry, очередь и отсутствие седьмого job.
- **Безопасная очистка production:** добавлена ручная сериализованная операция `Catalog maintenance · republish active markets`. Под общим writer-lock она сохраняет публичные строки всех шести рынков byte-stable, проверяет counts и SHA-256 до и после записи и атомарно переключает manifest без retired market keys. Парсеры при этой операции не запускаются.
- **Проверки до PR:** профильный набор — `57/57`, новый six-market контракт — `7/7`, web/engine typecheck — success, production build — success, 29 изменённых workflow YAML — валидны, `git diff --check` — success. Полный набор имеет те же 25 известных падений, что неизменённый базовый `main`; новых падений — `0`.
- **До production-подтверждения:** PR/CI/merge/deploy и атомарная live-очистка ещё не объявлены завершёнными. Финальный результат фиксируется только после manifest с шестью ключами, hash-сохранности шести рынков и live-проверки интерфейса/API.

---

## 40. 2026-09-02 — аварийная пауза каталога и полный ремонт данных шести рынков

- **Решение владельца:** до завершения ремонта запрещены автоматический парсинг, обновление предложений, переключение public generation и автоматическая очистка Object Storage. Текущий публичный каталог остаётся неизменным; возобновление требует отдельного проверяемого изменения кода.
- **Техническая блокировка:** удалены активные cron-триггеры основной sequential queue и storage cleanup. В центральный `persistCatalogOffers` добавлен fail-closed запрет любых production-записей при Object Storage driver. Это блокирует обычные, recovery, reindex и случайно запущенные ручные publisher-пути на общей границе записи.
- **Будущий retention-контракт:** после ремонта пять неяпонских рынков обновляются раз в неделю, предложение хранится и перепроверяется в течение 14 дней (`1209600000 ms`), затем отсутствующие у источника предложения удаляются. Для Японии сохраняется отдельное окно 30 дней, но её запуск остаётся заблокированным до доказанного источника конечной цены завершённого лота.
- **Япония:** существующие 10K+ строки нельзя считать подтверждёнными только по стартовой цене. Стартовая цена не является ценой продажи. До отдельной сертификации `sold identity + published final/end price` новые японские карточки не публикуются и не используются как точная основа расчёта.
- **Общий ремонт, а не точечный Kia-fix:** проводится аудит всех источников и всех шести рынков по цепочке `source detail → нормализация → exact vehicle variant → fuel/powertrain/engine/power/year → calculation → public card`. Конфликт или неоднозначность обязательного поля переводит предложение в `conflict/ambiguous/missing` и запрещает достоверный публичный расчёт; модельная средняя мощность не должна маскировать отсутствие точной версии.
- **Подтверждённый пример дефекта:** Encar для Kia Niro сообщает `Бензин + Электричество`, тогда как карточка АвтоЦены сохранила `Бензин` и одновременно подставила `105 л.с.` из базы модели. Причина находится на стыке выбора вложенного fuel-поля, source-first enrichment и fallback мощности модели. Исправление должно быть системным и покрываться cross-market regression-набором.
- **План выхода из паузы:** (1) инвентаризация качества и конфликтов по каждому market/source; (2) исправление detail adapters и доказуемой нормализации; (3) exact/ambiguous/conflict/missing matcher; (4) очистка и доведение собственной энциклопедии версий; (5) небольшой AI-слой только для очереди нерешённых записей с сохранением доказательств; (6) dry-run всех шести рынков; (7) последовательная чистая перепубликация и только затем включение недельного расписания.

### 40.1. Пауза подтверждена в production; начат этап ремонта характеристик

- **Фактическое завершение аварийной паузы:** PR `#798` и follow-up `#799` влиты; CI и оба production deploy завершены успешно. После `#799` merge SHA `adaf9f2b3f7fc60a041052746480cb37fff3b09f` запустился только deploy — автоматических collection/publish/cleanup workflow не было.
- **Единственная очистка между двумя PR:** push-trigger старой версии cleanup успел выполниться один раз и удалил только `151` осиротевшее изображение (`10 663 476` байт). Удалено `0` generation objects, `0` internal chunks, `0` source candidates и `0` brand projections; ошибок `0`. После `#799` этот trigger отсутствует.
- **Текущая рабочая граница:** production Object Storage остаётся fail-closed для записи; существующий каталог доступен только для чтения. Никакие парсеры и перепубликации не включать до полного dry-run и владелецкого подтверждения результатов.
- **Первый ремонтный пакет:** устранить потерю составного топлива в source detail (например, `Бензин + Электричество`), запретить model-wide representative power как основание расчёта, сохранить source-field provenance и добавить единый six-market regression-контракт для электрифицированных обозначений.
- **После первого пакета:** построить отчёт по каждому из 17 разрешённых источников: exact / ambiguous / conflict / missing для `year`, `fuel/powertrain`, `engineCc`, `powerHp` и сертифицированной мощности у EV/Hybrid. Дальше исправлять адаптеры по источникам, не ослабляя publication gates.

### 40.2. Массовый дефект модельных средних, review-вариантов и диапазонов Dubizzle

- **Первый системный пакет в production:** PR `#800` слит в `main`, merge SHA `7d7988899aba67f3f033f223537ffbba36cc342d`; CI run `33614380491` и production deploy run `33614638806` завершены `success`. Составное топливо больше не сворачивается до первого вложенного значения, семантический конфликт блокирует публикацию, а отсутствующая мощность не заполняется модельным representative. Парсеры и writers после deploy не запускались, аварийная пауза сохранена.
- **Новые примеры тестировщика:** Honda Accord 2022 1.5 показывал `147 л.с.`, Toyota Camry 2022 2.5 — `148 л.с.`, Toyota Raize 2023 — `1499 см³ / 99 л.с.`. Проверка данных доказала, что `147` и `148` — не характеристики этих машин, а `representativePowerHp` всего семейства Accord/Camry.
- **Измеренный масштаб model-wide риска:** в legacy knowledge содержится `4 899` моделей; у `212` из них задан `representativePowerHp`. Такое поле остаётся допустимым только для аналитики семейства и больше не может создавать спецификацию или расчёт конкретного предложения.
- **Измеренный масштаб review-ряда:** в Encyclopedia V2 найдено `18 941` вариантов со статусом `review`, и у всех есть хотя бы одно критичное поле (`engineCc`, `fuel/powertrain`, `powerHp` или `powerKw`). Несмотря на прямые research notes «не использовать для auto-resolve», прежний runtime доверял verified evidence внутри review-записи. Теперь `review` полностью исключается из автоматического обогащения.
- **Cross-market дефект:** европейская review-запись Accord `1498 см³ / 114 кВт` могла примениться к предложению ОАЭ, потому что совпадение рынка было бонусом, а несовпадение не являлось запретом. Теперь явно указанный чужой рынок отбрасывает вариант до scoring.
- **Dubizzle — не точные значения, а диапазоны:** Car Overview/Algolia возвращают категории вида `0–1499 cc`, `50–99 HP`, `100–199 HP`. Detail parser выбирал правую границу и помечал её как exact, поэтому на live ОАЭ массово появились повторяющиеся `1499/1999 см³` и `99/199/299 л.с.`. Новый parser принимает только одиночное число с явной единицей; закрытые и открытые диапазоны удаляются как неизвестные.
- **Оставшийся display-обход:** detail-render дополнительно вычислял consensus по всем версиям модели и мог снова заполнить отсутствующие engine/fuel/transmission/drive. Этот model-wide consensus удалён из расчётных полей; допускаются только source evidence, exact Knowledge CORE variant или явный текст конкретного объявления.
- **Legacy Drom поколения:** варианты без production-year больше не сопоставляются только по make/model/engine. Если у записи есть generation, предложение обязано доказать то же generation либо пройти явное trim-правило; иначе результат `ambiguous/missing`.
- **Регрессии и текущий результат:** целевой набор `44/44` прошёл. Синтетические проверки по трём карточкам дают: Accord сохраняет известные `1500 см³`, но не получает мощность; Camry сохраняет `2500 см³`, но не получает мощность; Raize без точного source-поля не получает ни объём, ни мощность. Следующий обязательный этап — полный six-market audit и чистый dry-run; текущий production generation пока не переписывается.
- **Безопасность уже замороженного generation:** старые Dubizzle-объекты сохраняют исходный Algolia payload внутри `operational.raw.parsed.rawText`. Read-time sanitizer удаляет сохранённые `engineCc/powerHp` только когда этот payload доказывает диапазон и сохранённое число равно его правой границе. Поэтому текущие ошибочные `1499/99/199` перестают участвовать в расчёте сразу после web deploy без записи в Object Storage; настоящее точное совпадающее число без range evidence не изменяется.

### 40.3. Инвентаризация характеристик по 17 разрешённым источникам

- **Следующий этап ремонта реализован как отдельный read-only аудит:** `scripts/catalog-audit-source-specifications.mjs` перечисляет канонические candidate pools всех 17 источников шести рынков и строит по каждому источнику распределение `exact / ambiguous / conflict / missing` для `year`, `fuel/powertrain`, `engineCc`, `powerHp` и сертифицированной 30-минутной мощности EV/Hybrid.
- **Методика не маскирует происхождение данных:** для каждого поля отдельно считаются source evidence, exact Knowledge CORE и сохранённые значения без классифицированного provenance. `review`-варианты, модельная representative-мощность, диапазоны источника и семантические противоречия не считаются точными.
- **Сертифицированная мощность:** проверяется только для EV/Hybrid; обычные ДВС получают `not_applicable`, а не искусственный `missing`. Отсутствующая либо недоказанная 30-минутная мощность остаётся явным разрывом до официального/регуляторного подтверждения.
- **Граница безопасности:** аудит использует только `listObjects/readChunkedDataJson`, не вызывает парсеры, `persistCatalogOffers`, замену чанков или любой writer. Production Object Storage остаётся неизменным, аварийная пауза не снимается.
- **Локальная проверка реализации:** новый аудит вместе с power/public-safety контуром — `65/65`, расширенный source/pause/six-market набор — `31/31`; web/engine typecheck и production build успешны, workflow YAML валиден, `git diff --check` чистый. Полный набор сохраняет ровно те же `25` известных падений, которые уже зафиксированы для неизменённого `main`; новых падений нет.
- **Production-результат:** одноразовый workflow успешно прочитал фактические candidate pools и выгрузил JSON-артефакт; измерения и следующий source-specific пакет зафиксированы ниже. Полный six-market collection dry-run по-прежнему не запускался.

### 40.4. Фактический source audit и первый provenance-ремонт Encar

- **Запуск production-аудита:** run `33626881824` для merge SHA `0358f14fa5e0c1ea0aa1f0701852f64572568a54` завершён `success`; production deploy run `33626881766` также завершён `success`. Аудит только читал Object Storage и не запускал collection, cleanup или publication.
- **Объём:** прочитано `57 285` строк из перекрывающихся candidate objects, после дедупликации — `52 508` предложений. Все шесть рынков и все `17` обязательных источников перечислены, candidate pool objects найдены для каждого источника, ошибок чтения нет.
- **Поля по 52 508 уникальным строкам:** `year` — `52 508 exact`; `fuel/powertrain` — `48 982 ambiguous`, `3 526 missing`; `engineCc` — `49 895 ambiguous`, `18 conflict`, `1 590 missing`, `1 005 not_applicable`; `powerHp` — `32 755 exact`, `6 447 ambiguous`, `1 067 conflict`, `12 239 missing`.
- **Сертифицированная 30-минутная мощность:** электрифицированных строк `4 114`; только `125` имеют exact evidence, у `3 989` значение отсутствует. Остальные `48 394` строки с ДВС корректно помечены `not_applicable`.
- **Нулевое покрытие при существующем pool:** строк нет у `dongchedi_china_open`, `guazi_china_open`, `carvector_japan_stat_open`, `auctiondatasearch_japan_open`, `jpcenter_japan_catalog_open` и `autoscout_europe_open`. Наличие storage object поэтому остаётся инфраструктурной проверкой, а отдельный `coverageGate` теперь явно показывает источники без строк и не позволяет принять их за готовое покрытие.
- **Почему большинство engine/fuel не exact:** старые candidate rows содержат сами значения, но не сохраняют классифицированный field provenance. Аудит намеренно считает их `ambiguous`, а не объявляет неверными или точными без доказательства.
- **Первый источник ремонта — Encar:** у него `9 157` строк и `1 189` электрифицированных; до ремонта `9 127` fuel/powertrain и `8 590` engineCc оставались unclassified, обнаружены `8` конфликтов engineCc, у `8 611` строк отсутствует powerHp и у `1 113` электрифицированных строк отсутствует certified power.
- **Новый fail-closed контракт Encar:** `displacement` и `power` больше не берутся как первое рекурсивное совпадение. Адаптер собирает все listing/detail значения, принимает только один согласованный exact metric, различает range/ambiguous и conflict, сохраняет semantic evidence и удаляет небезопасное значение после generic normalization. Parser/publication pause этим изменением не снимается; следующий шаг после source-adapter пакетов — полный шестирыночный collection dry-run без записи.

### 40.5. Encar в production и следующий exact-source пакет mobile.de

- **Encar-пакет в production:** PR `#804` слит, merge SHA `e1790d1c5e4180bf7abb54f62120fee67c5c5fe1`; CI run `33629207930`, повторный read-only audit `33629450841` и deploy `33629450828` завершены `success`. Никакие collection, cleanup или publication workflow не запускались.
- **Новый coverage-контракт подтверждён production-артефактом:** `sourcesWithCandidatePools=17`, `sourcesWithCandidateRows=11`, structural gate проходит, coverage gate и release gate остаются закрыты. Нулевые источники перечислены явно, ошибок чтения нет.
- **Диагноз JPAuc:** `941` power-конфликт состоит из `847 unconfirmed_power_scenario` и `94 electrified_power_scenario`. Текущий JPAuc HTML parser мощность не извлекает вообще; это retained legacy enrichment, уже запрещённый общим power-safety контрактом. Исправлять JPAuc parser ради этих чисел нельзя — их исчезновение должен доказать будущий чистый dry-run.
- **Следующий источник — mobile.de:** в сохранённом pool `18 601` строк. До ремонта fuel/powertrain содержит `18 507 ambiguous` и `94 missing`, engineCc — `18 577 ambiguous`, `9 conflict`, `15 missing`; при этом `18 597` peak-power значений уже классифицируются exact, а certified power доказана только для `33` из `748` электрифицированных строк.
- **mobile.de SRP/VIP evidence:** нормализованный SRP теперь сохраняет exact provenance топлива, объёма и мощности. VIP повторно связывается по listing id и цене; согласованные SRP/VIP значения остаются exact (для PS/kW допускается только погрешность пересчёта), а расхождения становятся semantic conflict, очищают расчётные поля и блокируют публикацию. Отсутствие сертифицированной 30-минутной мощности этим пакетом не заполняется.

### 40.6. mobile.de в production

- **Merge:** PR `#805` слит в `main`, merge SHA `8a9a977ec18110ba3f4dceb35d8f36171756eb84`.
- **Проверки:** CI run `33630699251` и production deploy run `33630988361` завершены `success`.
- **Граница результата:** deploy обновил код адаптера, но не запускал collection, cleanup или publication и не переписывал уже сохранённые карточки. Аварийный production write-lock остаётся включённым.

### 40.7. Решение владельца: ремонт и пересборка поверх действующего каталога

- **Отменённый вариант:** каталог, карточки и индексируемые страницы не закрывать, не скрывать и не обнулять на время ремонта.
- **Сохранность:** существующие source candidates, chunks, изображения, public generation и предыдущие generations не удалять. Автоматическую очистку не включать до завершения ремонта, контрольной пересборки и отдельного подтверждения владельца.
- **Порядок обновления:** исправлять source adapters и контракты полей при действующей аварийной паузе writers; затем выполнить полный шестирыночный dry-run без production-записи; после прохождения release gates собрать новую полную generation поверх сохранённых данных и атомарно переключить public manifest. Старые ошибочные карточки исчезают из публичной выдачи только в результате этого проверенного переключения, а не предварительного удаления или скрытия.
- **Индексация:** действующие публичные URL сохраняются. Новая generation должна заменить содержимое тех же страниц без смены SEO-маршрутов; sitemap и индексируемые страницы проверяются после атомарного переключения.
- **Запрет преждевременной публикации:** разрешение работать поверх существующего каталога не снимает production write-lock немедленно и не разрешает частичную публикацию непроверенных рынков. Сначала source-specific ремонт и полный dry-run, затем единая контролируемая пересборка.
- **Следующий шаг:** продолжить source-specific provenance-ремонт с `main` после Encar и mobile.de; для каждого источника сохранять точные доказательства полей и блокировать range, conflict и unclassified values. После завершения всех 17 источников провести шестирыночный dry-run и представить владельцу фактические release-gate числа перед production-переключением.

### 40.8. Source-specific provenance-пакет Che168

- **Доказанный риск в коде:** основной адаптер `autohome_used_china_open` извлекал первое число объёма и мощности из строки `carinfo.engine`, без классификации диапазона или нескольких противоречащих значений. Общая нормализация также могла поднять объём из названия листинга до получения identity-bound detail.
- **Исправление:** Che168 теперь отдельно классифицирует `year`, `fuel/powertrain`, `engineCc` и `powerHp` как `exact / ambiguous / conflict / missing`. Fuel сравнивается между listing и carinfo после канонизации; объём принимается только с явной единицей; мощность принимает явные hp/PS/bhp или kW с допуском только на округление пересчёта. Диапазон, неизвестная единица или конфликт очищают соответствующие расчётные поля и оставляют предложение в `needs_data`.
- **Защита листинга:** числа из `carname/specname` больше не становятся engine/power до точного ответа `/api/v1/carinfo/{id}`. Detail остаётся связан с тем же `infoid`; provenance сохраняется в `operational.semanticEvidence` для общего аудита и publication gate.
- **Изменённые файлы:** `apps/web/lib/catalog/che168-global-exact-source.ts`, `tests/catalog-che168-specification-evidence.test.ts`, `.github/workflows/ci.yml`, `roadmap.md`.
- **Проверки:** новый regression-набор `5/5`, расширенный профильный набор `35/35`, web/engine typecheck и production build — success; полный локальный набор остаётся на зафиксированном baseline `838` тестов: `813 pass`, те же `25 fail`, новых падений нет; `git diff --check` чистый.
- **Production-граница:** collection, publication, cleanup и Object Storage writers не запускались. Действующий каталог и индексируемые URL не изменялись.
- **Доставка:** PR `#806` слит в `main`, merge SHA `d876ffd600e2e8d531b8fc00172dd8fa88e2d5e8`; полный PR CI run `33634668218` завершён `success`. Даже после deploy сохранённый китайский pool не изменится до будущего полного dry-run и согласованной пересборки поверх текущего каталога.

### 40.9. Che168 readiness следует каноническому provenance

- **Post-merge source-only run:** readiness run `33634893459` не писал в Object Storage и не публиковал каталог. Сборщик успешно получил `3` identity-bound карточки Che168 Global с точными detail и галереями; gate упал только на устаревшем сравнении исходной строки `fuelname` с каноническим значением `petrol/hybrid`.
- **Исправление контракта:** readiness и будущий strict source-exhaustion workflow теперь проверяют exact semantic evidence, привязку raw fuel/engine к тому же carinfo detail и отсутствие числовых engine/power значений при non-exact evidence. Канонизация разрешённого source fuel больше не считается переписыванием данных.
- **Production-граница:** workflow остаётся source-only, создаёт только временный Actions artifact и не содержит production credentials или writers. Cleanup, publication и пересборка этим исправлением не запускаются.

### 40.10. Source-specific provenance-пакет MyAuto Georgia

- **Доказанный риск в коде:** generic normalization мог интерпретировать цифры модели (например, BMW 330) как объём двигателя до ответа MyAuto product API. Product snapshot выбирал первое из двух полей `engine_volume/engine_cc` и `power_hp/horsepower`, не обнаруживая расхождение между одновременно присутствующими значениями.
- **Исправление:** год и топливо листинга получают явный semantic evidence; топливо хранится в каноническом виде. Engine и power остаются missing до identity-bound `/en/products/{car_id}`. Product API принимает только одно согласованное bounded integer значение; расхождения становятся conflict, не попадают в расчётные поля и сохраняются в provenance.
- **Fail-closed:** неизвестное топливо не выводится из названия модели; электромобиль не может получить ненулевой engineCc; недоказанная мощность очищает связанные hp/kW и confidence/source поля. Галерея по-прежнему обязана совпасть по `car_id` и, когда он известен, по photo identity.
- **Production-граница:** меняется только код будущего сбора и тесты. Существующий грузинский pool, public generation, SEO URL и изображения не удаляются и не переписываются до общего шестирыночного dry-run и согласованной пересборки поверх текущего каталога.

### 40.11. Source-specific provenance-пакет K Car Korea

- **Масштаб по read-only production-аудиту:** K Car содержит `2 335` строк; fuel/powertrain у всех `2 335` был stored-unclassified, engineCc — у `2 218`. Peak power присутствует и уже считается exact у `2 328` строк, но адаптер не сохранял единый semantic evidence для всех расчётных полей.
- **Исправление:** identity-bound `rvo` detail теперь классифицирует год, каноническое топливо, engineCc и peak power. Структурные числа принимаются только как одиночные bounded integers; конфликт model year и manufacture year, диапазон или неизвестный формат не становятся значением.
- **Единицы мощности:** `hrspow` трактуется как kW только для точного электрического fuel code и как hp только для неэлектрического автомобиля. Несовпадение fuel name и unit code становится conflict. Электромобиль с ненулевым displacement не получает engineCc и сохраняет conflict provenance.
- **Production-граница:** меняется только адаптер будущего сбора; существующий корейский pool и public generation не переписываются. Cleanup и publication остаются выключены до общего шестирыночного dry-run и пересборки поверх текущего каталога.

### 40.12. Полное source-exhaustion доказательство Che168

- **Read-only результат:** strict run `33636014673` обошёл весь доступный источник: `168` страниц, `4 020` строк, `3 984` сохранённых карточки, stop reason `sources_exhausted`. Collection завершился успешно, production storage не использовался; gate показал один остаточный класс `fuel_evidence_not_exact`.
- **Фактическая причина:** `94` карточки имели точное source-значение `Range Extender`, которое общий fuel canonicalizer ещё не распознавал; ещё `4` карточки имели маркер `--` и корректно оставались без fuel. Это не ошибка идентичности, цены, фото, engine или power.
- **Исправление:** `Range Extender/Extended Range/增程` канонизируются как hybrid, а Che168 помечает их `series_hybrid`. Strict gate требует каноническое fuel только при exact evidence; при ambiguous/missing evidence он требует пустые fuel и unknown powertrain, поэтому неизвестное значение сохраняется в candidate provenance, но не продвигается в расчёт или публикацию.
- **Граница:** повторная проверка остаётся source-only. Полный source-exhaustion запускается автоматически только после изменения в `main` либо вручную, а не на каждом промежуточном branch commit. Существующие карточки, изображения, public generation и индексируемые URL не удаляются и не переписываются.
- **Финальное подтверждение:** после исправления strict run `33639430044` завершён `success`: `168` страниц, `4 017` seen/normalized, `3 981` accepted unique, `26` rejected core и `10` rejected images, `problems=[]`, stop reason `sources_exhausted`. Production deploy `33639429896`, post-deploy parity `33639983342` и visible calculation coverage `33639983456` также завершены `success`; ни один из этих запусков не перепубликовывал каталог.

### 40.13. Source-specific provenance-пакет JPAuc

- **Масштаб старого pool:** `12 563` строки. Fuel/powertrain: `0 exact`, `10 317 ambiguous`, `2 246 missing`. EngineCc: `0 exact`, `12 422 ambiguous`, `1 conflict`, `121 missing`, `19 not_applicable`. Peak power: `10 040 exact`, `282 ambiguous`, `941 conflict`, `1 300 missing`; конфликт включает `847 unconfirmed_power_scenario` и `94 electrified_power_scenario`. Certified power среди `241` электрифицированной строки: `1 exact`, `240 missing`.
- **Диагноз:** текущий HTML parser JPAuc вообще не извлекает fuel или power. Сохранённые power-значения старого pool являются retained legacy enrichment, а не доказательством JPAuc; переносить или восстанавливать их в адаптере запрещено. Их исчезновение должен подтвердить будущий чистый dry-run.
- **Новый source-контракт:** год принимается только как одно согласованное явное значение из Year cell. Объём принимается только из одиночного bounded `cc`-токена (`300–10 000`); диапазоны становятся ambiguous, несколько разных значений — conflict, отсутствие явной единицы — missing. Числа model, grade, modelCode и chassis не могут стать engine или power.
- **Fail-closed provenance:** адаптер сохраняет semantic evidence для `year`, `fuel`, `engineCc`, `powerHp` и `powerKw`. Fuel и обе мощности всегда остаются missing, пока источник не предоставит identity-bound структурное доказательство; не-exact engine не попадает в расчётное поле, но candidate row сохраняется для будущего сопоставления.
- **Production-граница:** меняется только код будущего сбора и regression-контракт. Существующий JPAuc pool, public generation, предыдущие generations, изображения и индексируемые URL не удаляются и не переписываются. Production writers, publication и cleanup остаются выключены до полного шестирыночного no-write dry-run и согласованной пересборки поверх текущего каталога.
- **Доставка:** PR `#811` слит в `main`, merge SHA `bc82179925f576ca67a9e24ec484d21b4e93f23e`; CI run `33642803911` и production deploy run `33643120745` завершены `success`. Deploy обновил только код и не запускал collection, publication или cleanup.

### 40.14. Source-specific provenance-пакет Prestige Japan

- **Масштаб старого pool:** `1 392` строки, из них `208` электрифицированных. Year: `1 392 exact`. Fuel/powertrain: `0 exact`, `237 ambiguous`, `1 155 missing`. EngineCc: все `1 392 ambiguous` из-за отсутствия field provenance. Peak power: `25 exact`, `110 ambiguous`, `1 257 missing`; certified power: `0 exact`, `208 missing` у электрифицированных строк.
- **Доказанный parser-риск:** прежний `positiveInteger` удалял из `Capacity` все символы кроме цифр. Значение вроде `1,500–2,000 CC` могло превратиться в склеенное число вместо semantic ambiguity. Тот же подход не сохранял доказательство именованного source field.
- **Новый source-контракт:** `Year` принимается только как одно согласованное значение в разрешённом диапазоне. `Capacity` допускает одно bounded значение `300–10 000` с явной `cc/cm3` единицей либо целиком числовое значение внутри именованной ячейки; диапазон становится ambiguous, разные значения — conflict, неизвестный формат — missing. Значения make/model/chassis/title не участвуют в извлечении engine.
- **Fuel и power:** Prestige detail их не предоставляет, поэтому source adapter хранит `missing` и не заполняет fuel, peak power или certified power. Отдельные будущие enrichment-этапы обязаны предъявить identity-bound CarVector/official evidence и не могут выдавать старое legacy enrichment за данные Prestige.
- **Проверяемость:** semantic evidence сохраняется в каждой normalized строке; source-only readiness и partition strict-check требуют соответствия exact year/engine и пустых unsupported fields. Candidate с не-exact capacity сохраняется, но его engineCc не попадает в расчёт.
- **Production-граница:** текущий public generation и `1 392` сохранённые строки не меняются этим пакетом. Production writers, cleanup и публикация остаются выключены; исправленный адаптер должен пройти source-only readiness, а затем общий шестирыночный no-write dry-run перед единой пересборкой поверх.
- **Новый внешний блок:** source-only readiness runs `33644322884` и `33644675512` дошли до production egress, но live AJAX ответил HTTP `400` с `cf-turnstile=failure / Bot check failed`. Это не parser-regression и не разрешение обходить защиту: адаптер теперь сохраняет bounded response detail и явно помечает health как `blocked`. Prestige остаётся незавершённым источником, пока не появится разрешённый публичный/партнёрский маршрут; release gate не ослабляется.

### 40.15. Source-specific provenance-пакет Dubizzle UAE

- **Масштаб старого pool:** `5 842` строки, из них `684` электрифицированных. Year: `5 842 exact`. Fuel/powertrain: `0 exact`, все `5 842 ambiguous` как stored-unclassified. EngineCc: `0 exact`, `4 227 ambiguous`, `1 424 missing`, `191 not_applicable`; в том числе `1 056` строк содержат явные source ranges. Peak power: `66 exact`, `5 296 ambiguous`, `3 conflict`, `477 missing`; `3 891` значений не имели exact provenance. Certified power: `1 exact` и `683 missing` среди электрифицированных строк.
- **Доказанный parser-риск:** официальный Algolia payload и identity-bound `Car Overview` давали именованные поля, но адаптер сохранял только число и терял его semantic classification. При последующем detail merge использовалось первое непустое значение, поэтому несовпадение listing/detail не становилось конфликтом. Исторические диапазоны категории вроде `0–1,499 cc` и `50–99 hp` должны остаться только evidence, а не расчётными значениями.
- **Новый source-контракт:** год, fuel, engineCc и powerHp классифицируются как `exact / ambiguous / conflict / missing`. Fuel канонизируется только из именованного поля; engine/power принимают одно bounded значение из именованного unit-поля Algolia или label-bound `Car Overview`. Диапазоны, неизвестное топливо и неразбираемый формат не продвигаются. Exact значения Algolia и identity-bound detail объединяются только при согласии; расхождение очищает расчётное поле и сохраняет conflict с обоими raw values.
- **Fail-closed:** электромобиль с ненулевым displacement получает engine conflict; non-exact power очищает hp/kW и source-confidence. Listing/detail identity и минимум пять связанных фото остаются обязательными. Candidate сохраняется с `needs_data`, даже если отдельная характеристика не доказана.
- **Production-граница:** исправляется только код будущего сбора и его regression-контракт. Текущий UAE pool, public generation, предыдущие generations, изображения и индексируемые URL не удаляются и не переписываются. Collection, publication и cleanup writers остаются выключены до завершения остальных источников, полного шестирыночного no-write dry-run и единой пересборки поверх текущего каталога.

### 40.16. Source-specific provenance-пакет DubiCars UAE

- **Масштаб старого pool:** `60` строк, из них `6` электрифицированных. Year: `60 exact`. Fuel/powertrain: `0 exact`, `59 ambiguous`, `1 missing`. EngineCc: все `60 ambiguous`. Peak power: `1 exact` только через knowledge, `51 ambiguous`, `8 missing`. Certified power: `0 exact`, все `6` электрифицированных строк missing.
- **Доказанный parser-риск:** адаптер сначала извлекал engine/power из page-wide текста и даже из title, а затем общий normalizer повторно сканировал `trim` и raw page row. Поэтому очистка подозрительного значения в parser не гарантировала, что число не появится снова. По той же странице ниже основного автомобиля присутствуют рекомендации с чужими характеристиками.
- **Новый source-контракт:** основной блок ограничивается от source label `Model year` до границы description/recommendations. Year согласуется с identity-bound годом URL. Fuel, engine capacity и horsepower принимаются только из именованных полей этого блока и классифицируются как `exact / ambiguous / conflict / missing`; повторяющиеся разные значения, диапазоны, неизвестные fuel и мощность выше bounded лимита не продвигаются.
- **Защита после generic normalization:** итоговые fuel, powertrain, engineCc, hp/kW и power provenance повторно выставляются только из semantic evidence. Числа из title/model/raw не могут вернуться в расчётные поля. EREV/REEV остаются series hybrid только при exact hybrid fuel и identity-bound названии; EV с ненулевым displacement становится conflict. Все новые source rows остаются `needs_data` до отдельного расчётного этапа.
- **Галерея:** разрешён dealer slug внутри реального DubiCars gallery URL; UUID-подобный файл, допустимый image host pattern и coherent gallery grouping остаются обязательными, поэтому это не разрешает логотипы или несвязанные рекомендации.
- **Production-граница:** текущие `60` строк, public generation, предыдущие generations, изображения, sitemap и индексируемые URL не меняются. Исправляется только будущий сбор; production collection, publication и cleanup не запускаются до общего шестирыночного no-write dry-run и согласованной пересборки поверх текущего каталога.

### 40.17. Source-specific provenance-пакет AutoPapa Georgia

- **Масштаб старого pool:** `96` строк, из них `11` электрифицированных. Year: `96 exact`. Fuel/powertrain: все `96 ambiguous`. EngineCc: `94 ambiguous`, `2 not_applicable`. Peak power: `17 exact` (`10` source evidence и `7` knowledge), `8 ambiguous`, `3 conflict`, `68 missing`. Certified power: `0 exact`, все `11` электрифицированных missing.
- **Исправление:** listing-bound карточка теперь сохраняет semantic evidence для year, fuel и engine; повторяющиеся разные значения становятся conflict, диапазон или неизвестный формат — ambiguous. Fuel канонизируется, EV с ненулевым displacement получает conflict. Power остаётся missing до identity-bound detail `Power`, ограниченного блоком фактов до `Car description`.
- **Защита нормализации:** после общего normalizer расчётные fuel/powertrain/engine/power поля повторно ограничиваются source evidence, поэтому цифры из title и raw card не возвращаются. Точный detail power сохраняет собственный provenance и не используется как certified 30-minute power для EV/hybrid.
- **Production-граница:** текущий грузинский pool, public generation, предыдущие generations, изображения и индексируемые URL не меняются. Writers, cleanup и публикация остаются выключены до полного шестирыночного no-write dry-run и пересборки поверх текущего каталога.

### 40.18. Source-specific provenance-пакет AutoScout24 Europe

- **Масштаб старого pool:** сохранённых строк `autoscout_europe_open` нет. Это ремонт контракта будущего сбора, а не основание считать источник или европейский рынок готовым.
- **Доказанный parser-риск:** старый parser удалял все нецифровые символы из displacement и мог превращать диапазон в одно большое число. kW и hp извлекались независимо из первого поля `Power`, поэтому противоречащая пара могла получить `source_exact` без проверки пересчёта.
- **Новый source-контракт:** first registration, named fuel, structured `engineDisplacementInCCM` и именованный `vehicleDetails Power` сохраняют `exact / ambiguous / conflict / missing`. Диапазоны и повторяющиеся разные числа не продвигаются. Одновременно указанные kW и hp принимаются только при согласии с переводом единиц в пределах округления; неизвестный fuel остаётся ambiguous. URL может доказать fuel только при однозначном `electric` без признаков combustion/hybrid.
- **Fail-closed:** в offer попадают только exact fuel/engine/power. EV с ненулевым displacement получает engine conflict; inconsistent kW/hp очищает оба поля и provenance. Candidate остаётся `needs_data`, а identity-bound detail gallery с минимум пятью HQ-фото остаётся отдельным обязательным этапом.
- **Production-граница:** текущий public catalog, поколения, source candidates, изображения, sitemap и индексируемые URL не удаляются и не переписываются. Collection, publication и cleanup writers не запускаются до ремонта остальных source adapters, полного шестирыночного no-write dry-run и единой пересборки поверх текущего каталога с атомарным переключением public manifest.
- **Live readiness после первого parser-пакета:** run `33698837307` получил HTTP `200` и `806 575` байт с `20` listing rows, но вернул `count=0`. Read-only recon доказал причину: реальный `tracking.firstRegistration` имеет формат `MM-YYYY`, который общий range detector ошибочно считал диапазоном. Разбор регистрации отделён от metric ranges и ограничен `MM-YYYY / MM/YYYY / YYYY`; readiness теперь обязательно запускает и specification-evidence regression suite.
- **Source-side age boundary:** live page без `fregfrom` содержит в основном старые машины; post-parse filter оставлял только `1` из первых `20`. Current adapter теперь передаёт источнику `fregfrom=2020` и всё равно повторно применяет локальный `year>=2020`, поэтому readiness проверяет актуальную выдачу, не ослабляя возрастной gate.

### 40.19. Source-specific provenance-пакет Autohome New China

- **Масштаб старого pool:** `40` строк, из них `34` электрифицированных. Year: `40 exact`. Fuel/powertrain: `0 exact`, все `40 ambiguous` как stored-unclassified. EngineCc: `0 exact`, `6 ambiguous`, `34 not_applicable`. Peak power: `6 exact`, `34 missing`; certified power: `0 exact`, `34 missing` у электрифицированных строк.
- **Доказанный parser-риск:** адаптер переносил китайское поле `能源类型` как непроверенную строку и считал `汽油+48V轻混` обычным ДВС. Engine hp/kW принимались независимо без semantic provenance и без проверки согласия перевода единиц. Это позволяло неверно классифицировать mild hybrid и выдать противоречивую пару мощности за exact.
- **Новый source-контракт:** год listing/detail обязан совпадать. Named energy канонизируется в `petrol/diesel/electric/hybrid`, причём `增程` становится `series_hybrid`, а `轻混/混合/插电/油电` — `other_hybrid`. Unknown energy, диапазоны engine/power и несогласованные hp/kW не продвигаются; EV с ненулевым displacement получает conflict.
- **Мощность электрифицированных машин:** engine/motor/system maxima остаются в identity-bound raw config. Они не являются сертифицированной 30-минутной мощностью и не заполняют `powerHp`, `powerKw` или `power30MinKw`. Для обычного ДВС engine-section hp/kW принимаются только как согласованная пара; отсутствующая единица может быть детерминированно вычислена из единственного exact source-поля.
- **Live-доступность:** read-only recon `2026-09-03` получил HTTP `502` OpenResty на legacy price-list endpoint и HTTP `200` на главной. Главная содержит series/spec ссылки, но не доказывает цену конкретной комплектации, поэтому она не используется как ослабленный fallback. Source readiness должен либо получить точный list price, либо завершиться честным external-access failure без публикации.
- **Production-граница:** меняется только будущий source adapter, его тесты и read-only readiness. Текущий public generation, старый candidate pool, предыдущие generations, изображения, sitemap и индексируемые URL не удаляются и не переписываются. Writers, cleanup и publication остаются выключены до ремонта всех источников, полного шестирыночного no-write dry-run и единой пересборки поверх текущего каталога.

### 40.20. Source-specific provenance-пакет Guazi China

- **Масштаб старого pool:** candidate objects существуют, но уникальных строк `0`. Поэтому источник нельзя считать готовым по сохранённым данным; его контракт и live production bridge проверяются отдельно.
- **Доказанный parser-риск:** Guazi normalizer искал первый engine token сразу в объединении title, URL и listing text без semantic classification. Несовпадение `2.0L` в title и `14l` в product URL не становилось conflict, а fuel/power не имели явного missing provenance.
- **Новый source-контракт:** listing year обязан согласоваться с identity-bound title и product URL. EngineCc принимается только из согласованных одиночных token `1.4L/14l` в title/URL; диапазон, `00l` placeholder или разные значения не продвигаются. Fuel, powertrain и power остаются missing/unknown, пока Guazi не предоставит именованное identity-bound поле; числа из свободного текста не могут стать мощностью.
- **Граница галереи:** остаются обязательными точный `/products/<id>.html`, identity check detail-страницы, минимум пять изображений с Guazi host и verified gallery flags. GitHub readiness использует только фиксированный production bridge без произвольного URL.
- **Production-граница:** текущий public generation, пустой старый pool, предыдущие generations, изображения, sitemap и индексируемые URL не удаляются и не переписываются. Live gate является read-only; writers, publication и cleanup остаются выключены.

### 40.21. Source-specific provenance-пакет CarVector Japan

- **Роль источника:** CarVector является `auction_history`, а не самостоятельным владельцем публичной галереи. Его exact sold result/specification evidence соединяется с тем же японским лотом JPAuc; изображения для публичной карточки остаются за JPAuc после полного identity join.
- **Доказанный live-контракт:** публичный SSR `ng-state` содержит вложенный `FindOffersAuctionsResult` с `total/offers`; recon получил HTTP `200`, `50` строк на странице и `596 679` записей в источнике. Общая лента смешивает легковые автомобили со спецтехникой и содержит записи с нулевой итоговой ценой, поэтому generic HTML parser был небезопасен.
- **Новый source-контракт:** адаптер принимает только `OfferAuction / AUCTION_STATS`, UUID-связанную `/stat/.../<id>` ссылку, точные make/model/chassis, venue/date/lot, положительную `finishPrice.JPY` и exact year/engineVolume. Нулевая цена, иной kind или неполная identity не становятся auction result. Пустое fuel не уничтожает полезную sold-price историю, но оставляет карточку в `needs_data`.
- **Power safety:** CarVector `power` становится exact hp/kW только при именованном combustion fuel. Если fuel отсутствует, powertrain остаётся `unknown`, а power хранится только в raw evidence и не попадает в расчёт. Для hybrid/EV source power также не доказывает ICE или сертифицированную 30-минутную мощность; EV с ненулевым displacement получает conflict.
- **Защита публикации:** standalone CarVector adapter намеренно возвращает пустую gallery и `needs_data`. Отдельный no-write readiness проверяет evidence rows, но не публикует их как карточки; существующий JPAuc exact-join остаётся единственным путём к публичной японской карточке.
- **Production-граница:** текущий public generation, candidate objects, предыдущие generations, изображения, sitemap и индексируемые URL не удаляются и не переписываются. Writers, cleanup и publication не запускаются этим пакетом.


### 40.22. Явная внешняя блокировка Guazi China

- **Live-факт:** production bridge получил от `https://en.guazi.com/used-cars/` HTTP `200`, но тело страницы содержало только JavaScript challenge Tencent EdgeOne (`EO-Bot-Js-Token`, `solveChallenge`) и не содержало товарных ссылок. Поэтому прежнее состояние `exact_list:0` было ложным описанием доступности источника.
- **Fail-closed исправление:** Guazi adapter распознаёт доказанные маркеры challenge и возвращает typed blocked error `guazi_source_blocked_bot_challenge`; health, фиксированный production bridge и GitHub/Yandex wrapper сохраняют `blocked=true`. HTTP `200` challenge больше не может выглядеть как успешно прочитанная пустая выдача.
- **Граница доступа:** challenge не обходится. Для live-ready статуса Guazi нужен разрешённый публичный endpoint либо официальный partner feed; до этого источник остаётся внешне заблокированным и не даёт новых карточек.
- **Production-граница:** текущий public generation, предыдущие generations, кандидаты, изображения, sitemap и индексируемые URL остаются на месте. Этот пакет меняет только диагностику будущего no-write сбора; collection, publication и cleanup не запускаются.


### 40.23. Явные access-контракты Dongchedi, Auction Data Search и JP Center

- **Dongchedi:** read-only запрос `https://www.dongchedi.com/usedcar` завершился HTTP `200` на `https://www.dongchedi.com/login-required?redirect=%2Fusedcar`. Страница входа не является пустой товарной выдачей и не может считаться успешным source scan.
- **Auction Data Search:** `/search/` и `/statistics/` отвечают redirect на `/accounts/login/?next=...`; публичный endpoint количества по производителям не содержит identity-bound vehicle, price и gallery. Источник требует разрешённого входа или официального partner feed.
- **JP Center:** публичный loader доказан и для Toyota Alphard вернул `450` строк с lot/auction/date/year/chassis/engine. Однако `b.o` в шаблоне является модельной average statistics, а индивидуальные `b.s/b.t` (start/finish price) для гостя пусты или нулевые. Public detail даёт только ограниченный набор изображений, а `price-*`/`photo-*` прямо требуют login и платный запрос. Среднее запрещено выдавать за цену конкретной машины.
- **Prestige Japan:** официальный `robots.txt` прямо запрещает `/auction-vehicle-display/`, а разрешённые landing/AJAX запросы сейчас дополнительно останавливаются Turnstile. Даже исправленный exact-detail parser не должен обращаться к запрещённому detail route; для него также требуется разрешённый partner feed.
- **Fail-closed исправление:** четыре обязательных sourceId остаются в owner-approved allowlist и collection registry, но generic/restricted-route adapters заменены typed `partner_feed` adapters. Они возвращают `blocked=true` с точной причиной вместо `ok + 0`, не создают кандидаты из login/public shell и не генерируют карточки из средней цены.
- **Production-граница:** существующий public generation, предыдущие generations, кандидаты, изображения, sitemap и индексируемые URL не меняются. Для разблокировки нужны разрешённые публичные vehicle endpoints либо официальные partner feeds; collection, publication и cleanup этим пакетом не запускаются.
- **Общий readiness:** шестирыночная матрица остаётся no-write и после merge проверит все `17` обязательных sourceId. Workflow больше не имеет `contents: write` и не коммитит служебный run pointer в `main`, поэтому диагностический запуск не вызывает побочный deploy и не меняет ни репозиторий, ни production storage.
- **Role-aware gate:** CarVector проверяется как `auction_history` evidence-only: точный sold result/price/identity обязателен, standalone gallery обязана оставаться пустой, а владельцем фото после exact join остаётся JPAuc. Общая матрица больше не требует от evidence-only источника пять публичных изображений и не выдаёт это безопасное ограничение за parser failure.
- **JPAuc readiness:** первый общий no-write run обрывал многошаговый session bootstrap внешним `page_timeout` через `30` секунд ещё до первой страницы. Для JPAuc установлен отдельный bounded page budget `180` секунд при сохранении `30` секунд на каждый сетевой запрос и одном retry только для timeout/429/5xx; readiness ограничен одной последней датой. Его фактический контракт приведён к трём same-lot Aleado изображениям, уже требуемым public safety gate.
- **JPAuc диагностика:** после увеличения bounded budget источник отдал `30` строк на трёх страницах без сетевых ошибок, но все строки вернули `normalizeOffer=null`. Отдельный result-contract probe переведён в строго read-only режим (`contents: read`, без commit/push) для проверки текущих listing/detail/history полей до изменения price parser.
- **JPAuc live-контракт:** read-only probe `33705548382` подтвердил HTTP `200`, `26 564` доступных identity rows и точные date/venue/lot/make/model/year/modelCode, но у проверенных строк `Status: available`, `Start Price: ¥0`, `End Price: N/A`; guest detail также не раскрыл VIN/history. JPAuc поэтому проходит readiness только как `identity_gallery_after_exact_price_join`: три same-lot Aleado изображения и identity обязательны, standalone карточка с нулевой ценой запрещена, sold price должен прийти из exact CarVector join.

### 40.24. Четырёхрыночный exact dry-run перед пересборкой поверх

- **Решение владельца от 2026-09-03:** качество важнее прежнего объёма. Рабочим результатом считаются даже ориентировочно `3–5 тыс.` карточек по всем готовым рынкам, если у каждой машины доказаны identity, кузов, fuel/powertrain, объём двигателя, мощность, галерея и полный расчёт доставки. Неполное или противоречивое значение не угадывается и не показывается.
- **Текущий этап:** полный строго read-only сбор и расчёт `korea`, `uae`, `europe`, `georgia`. Китай отложен до отдельного прохода; Япония полностью исключена из этого этапа и не изменяется.
- **Источники этапа:** Korea — `encar_direct`, `kcar_korea_open`; UAE — `dubizzle_uae_open`, `dubicars_uae_exact`; Europe — `mobile_de_open`, `autoscout_europe_open`; Georgia — `myauto_georgia_list`, `autopapa_georgia_open`.
- **Exact-контракт:** в выходной файл допускаются только карточки с каноническим source-bound кузовом, exact semantic evidence для года, fuel/powertrain, двигателя и peak power, а для EV/hybrid — с доказанной certified/utilization power. Модельные средние, `review`-варианты, representative/reference power и preliminary calculation запрещены. Каждая карточка обязана пройти текущий public eligibility gate и иметь не менее пяти identity-bound фотографий.
- **Граница безопасности:** workflow не получает production storage credentials, не читает и не пишет текущий manifest, не публикует, не запускает cleanup и не удаляет существующие карточки. Текущий индексируемый public generation остаётся без изменений.
- **Следующий checkpoint:** сохранить run ID и четыре market-отчёта, разобрать фактический объём и причины отклонений. Только после этого готовить отдельную атомарную пересборку поверх для прошедших рынков с byte-stable сохранением Китая и Японии; сама публикация требует отдельного подтверждённого решения по результатам dry-run.

### 40.25. Новый курс: квалификация источников для шести рынков вместо ремонта фиксированного списка

- **Решение владельца от 2026-09-03:** сохранить все шесть рынков АвтоЦены — `korea`, `china`, `japan`, `uae`, `europe`, `georgia` — но отказаться от предположения, что текущий список площадок является вечным эталоном. Для каждого рынка разрешён поиск и проверка новых источников. Старые источники сохраняются как кандидаты до вынесения доказанного вердикта.
- **Главный продуктовый принцип:** в автоматически рассчитанный каталог допускается не «весь подходящий сайт», а только каждая отдельная карточка, прошедшая offer-level exact gate. Источник сначала квалифицируется технически, после чего каждая его машина всё равно отдельно доказывает identity, цену, валюту, год, кузов, fuel/powertrain, engine displacement либо EV not-applicable, мощность, необходимые certified/utilization данные, доступность объявления и listing-bound gallery.
- **Режимы источников:** `exact_catalog` — может давать автоматически рассчитанные карточки; `lead_only` — может использоваться только для поиска/заявки без выдуманного полного расчёта; `rejected` — не используется. Неполное поле, диапазон, модельная средняя, свободный текст без привязанной метки, конфликт list/detail или недоказанная фотография никогда не повышаются до exact.
- **Все рынки и индексируемые страницы сохраняются:** сокращения географии до трёх рынков сейчас не происходит. Существующий public generation, URL, sitemap, предыдущие generations и изображения не удаляются и не переписываются. Новые данные готовятся поверх в staging/no-write и заменят соответствующий рынок только атомарно после проверки.
- **Текущий четырёхрыночный run `33717909118` становится диагностическим baseline:** Europe формально собрала `1500` exact-кандидатов, но ручная проверка выявила ошибочное превращение категории Mobile.de `Limousine` в `sedan` (например, Abarth 595). UAE просмотрела `270` DubiCars и `9 883` Dubizzle строк, но получила `0` прошедших карточек: DubiCars не выставлял доказательство photo identity, а Dubizzle в основном отдавал диапазоны engine/power. Эти результаты не разрешают публикацию, но подтверждают необходимость source qualification.
- **Первое исправление после baseline:** Mobile.de больше не считает `Limousine` точным кузовом; неоднозначная категория очищает `bodyType` и блокирует strict-публикацию. Локальный commit `66e236c7`; профильные тесты `13/13`, общий typecheck — success. До повторного live-run Европа не считается готовой.
- **Порядок новой работы:** (1) утвердить единый паспорт пригодности; (2) составить широкий candidate ledger по шести рынкам; (3) выполнить read-only structure/access probe; (4) проверить выборку реальных detail-карточек; (5) присвоить `exact_catalog / lead_only / rejected`; (6) написать адаптеры только для прошедших источников; (7) выполнить no-write сбор и ручной semantic spot-check; (8) подготовить атомарную пересборку поверх без удаления текущего каталога.
- **Критерий результата:** объём не является самостоятельным основанием допуска. Несколько тысяч полностью доказанных карточек являются рабочим результатом; ни одна карточка с угаданным кузовом, объёмом, типом двигателя или мощностью не должна попадать в новый рассчитанный набор.
- **Следующий checkpoint для нового чата:** продолжить с документа `docs/catalog-source-qualification-v1.md`; создать и наполнить candidate ledger реальными площадками всех шести рынков. Не продолжать бесконечное точечное лечение прежних площадок до их повторной квалификации. Китай и Японию не публиковать; любые исследования и пробы для них только read-only.
- **Первый discovery sweep выполнен:** в `data/catalog/source-qualification-v1.json` к прежним `17` источникам добавлены `16` внешних кандидатов: KB ChaChaCha и Bobaedream; Guangdong Good Car и iAutos; BE FORWARD, ExportCar, JapaneseUsedCars, SBT Japan и TCV; CarSwitch, YallaMotor, AutoMarket UAE и CARS24 UAE; La Centrale и AutoUncle; WorldAuto Georgia. Все они имеют только `research_pending` и `publishAllowed=false`: найденная публичная площадка ещё не считается пригодным источником. Следующее действие — read-only access/detail probes и сохранение доказательств по каждому обязательному полю.

### 40.26. Deficit resolution: что реально удалось доказать после PR #826

- **Дата checkpoint:** 2026-09-03. Продолжение раздела 40.25 без перезапуска исследования и без изменения production.
- **Исходная точка:** PR #826 влит в `main`, merge SHA `3826f80b04371ba8c4c0b8a3321292fa93bf5db2`. После него четыре strongest detail-signal кандидата — Bobaedream, DubiCars, CarSwitch и CARS24 UAE — оставались `research_pending`, `publishAllowed=false`.
- **Targeted recon:** run `33744960785` — success; artifact `9889257990`; digest `sha256:d7008f31989b6fac8f941349122c1da63a72e2593866b199ed73140310b010db`. Проверены только те же 8 detail offers; найдены offer-bound gallery evidence, embedded source keys и два реальные same-origin Bobaedream spec routes.
- **Bobaedream discovered-route probe:** run `33745638721` — success; artifact `9889505193`; digest `sha256:5ec558b74397826ba513f1798e0a078615f97fc9ec4f8fe1da3e0056a7ea50d5`. Два реально найденных spec routes стабильны, но canonical body field не найден; generic `승용/SUV/쿠페` навигация не считается offer evidence.
- **Final deficit resolution:** run `33746996319` — success; artifact `9890047197`; digest `sha256:63a090905f0b55585a2683e16c8c08ffeb45ab53f93ddb20904c65c23211be17`. Перед ним run `33746766296` упал только на regression test корейского `마력`; live probe не запускался, причина исправлена тестом.
- **Bobaedream:** exact engine/power подтверждены самим offer (`2359 cc / 190 hp`, `3342 cc / 282 hp`), gallery series offer-bound и >5 изображений; **blocker = canonical body**.
- **CarSwitch:** offer-bound `1.5L / 5.7L`, price/currency/body/fuel/gallery доказаны; **blockers = exact engineCc и offer-bound powerHp**. Маркетинговый литраж не превращается автоматически в cc.
- **CARS24 UAE:** `31 499 AED` и `64 999 AED` доказаны одновременно hero price + embedded offer state; `1.5L / 1.8L` доказаны; **blockers = exact engineCc и source-bound powerHp**.
- **DubiCars:** EV peak `313 HP` доказан, но certified/utilization/30-minute power нет; ICE exact cc/power нет. Gallery window содержит лишние UUID относительно source `image_count=11/14`, поэтому gallery evidence пока contaminated и не повышается до exact.
- **Вердикт этапа:** ни один из 4 источников не получает `exact_catalog`; у всех `publishAllowed=false`. Текущий public generation, manifest, Object Storage, cleanup и production catalog не менялись.
- **Следующий основной блок:** не лечить эти четыре бесконечно. Перейти к partial-signal группе `carvector_japan_stat_open`, `chngoodcar_china_candidate`, `iautos_china_candidate`, `exportcar_japan_candidate` и применить тот же read-only source-bound field contract. К strongest-группе возвращаться только при наличии конкретного разрешённого route/evidence, способного закрыть оставшееся поле без inference.

### 40.27. Guangdong Good Car: exact passenger ICE adapter только в no-write режиме

- **Дата checkpoint:** 2026-09-03. Продолжение partial-signal исследования после 40.26; production не изменялся.
- **Классификационный результат:** `chngoodcar_china_candidate` имеет `class=exact_catalog` только как доказанную способность источника давать exact-поля для ограниченного offer-level ICE scope. `publishAllowed=false`; sourceId намеренно не добавлен в `REQUIRED_CATALOG_SOURCES.china` и production allowlist.
- **Реализация:** на ветке `feat/chngoodcar-exact-adapter-v1-20260903` создан стандартный `CatalogSourceAdapter` `apps/web/lib/catalog/chngoodcar-exact-source.ts`, regression-тест `tests/catalog-source-chngoodcar-exact-adapter-v1.test.ts`, no-write runner `scripts/catalog-source-chngoodcar-exact-dry-run-v1.ts` и read-only workflow `.github/workflows/catalog-source-chngoodcar-exact-dry-run-v1.yml`.
- **Exact gate v1:** USD принимается только по явной CarsList-оси `价格(US $)` плюс совпадение list/detail title и цены; detail обязан дать production date/year, mileage, точные `排量 (ml)` и `功率 (kw)`, combustion fuel, подтверждённый passenger body и не менее пяти listing-bound фото до `猜你喜欢`. kW сохраняется как source-exact provenance; `powerHp = round(kW / 0.73549875)`. EV/hybrid, unknown body, неизвестная make/model identity, неполная галерея или parity failure блокируются.
- **Первый green намеренно не принят:** run `33756537493` был `success` (`8/8` tests, typecheck, parsed `14`, accepted ICE `7`, electrified blocked `1`; artifact `9893690091`, digest `sha256:f89660f119c1806b64d5095b515bf699d56e44b1a4295d332a49f98ccf9c0a21`), но ручной разбор artifact обнаружил `douyin.png` в gallery и ложное `drive=付款方式` у автобусной карточки. Это зафиксировано как дефект, а не скрыто зелёным CI.
- **Исправление после ручного аудита:** social/UI assets исключены из gallery; transmission/drive ограничены whitelist точных source values; v1 scope сужен до passenger body (`轿车/SUV/MPV/两厢车/三厢车/旅行车/跑车`); добавлены fail-closed regression checks для social gallery, body gate и electrified offers.
- **Финальный no-write run:** `33757087189`, head `aae2f3540c58d05d23c56b79aa6dba2db43ceb75` — `success`; `9/9` tests, typecheck, live dry-run и no-write envelope зелёные. Artifact `9893932333`, digest `sha256:ad370395949557f958184458bbf0a370e16445928dbb3c67ee05e3be5e576288`. Homepage discovery увидел `32` offer links, detail parsed `14`, exact passenger ICE accepted `3`, electrified blocked `1`, body-gate blocked `6`, ещё `4` passenger ICE отклонены оставшимися exact gates; `failures=[]`.
- **Accepted sample:** Hyundai Elantra `2057736003732369408` — `2022-01`, `42000 km`, `1500 ml`, `84.5 kW → 115 hp`, `9950 USD`, 10 фото; Mazda CX-50 `2049753443165270016` — `2023-10`, `15000 km`, `2000 ml`, `114 kW → 155 hp`, `23600 USD`, 20 фото; MG5 `1988133087980023808` — `2025-09`, `0 km`, `1495 ml`, `95 kW → 129 hp`, `8800 USD`, 10 фото. Все три combustion/passenger и прошли list/detail parity.
- **Ручной source spot-check:** Mazda detail независимо совпал по title/price/body/date/mileage/engine/power/transmission/fuel/drive; MG5 detail совпал по тем же полям. Текущая homepage повторно подтвердила Hyundai list identity `9950 / 2022-01 / 42000km`. CarsList явно показывает `价格(US $)`. Независимый web-cache Hyundai detail в момент ручной проверки не открылся, поэтому он не записан как отдельная полная web-detail сверка.
- **Safety boundary подтверждён:** `productionWrites=false`, `classificationMutations=false`, `publishAllowedMutations=false`, `objectStorageWrites=false`, `catalogGenerationWrites=false`, `productionAllowlisted=false`. Public generation, manifest, current China data, cleanup и production writers не затронуты.
- **Оставшийся блокер:** v1 обнаруживает bounded набор current offers с public homepage, а CarsList использует как явный currency contract. Полная CarsList discovery/pagination ещё не реализована и не доказана. Поэтому production promotion запрещён до отдельного pagination/readiness, no-write scale run и ручного offer-level spot-check на расширенной выборке.
- **Следующий безопасный шаг:** оставить `publishAllowed=false` и China production registry без изменений; реализовать/доказать полный CarsList discovery/pagination в отдельной ветке, затем снова прогнать exact no-write scale. Только после этого отдельно решать, добавлять ли `chngoodcar_china_candidate` в production source registry.

### 40.28. Guangdong Good Car: доказана полная CarsList pagination, production всё ещё закрыт

- **Дата checkpoint:** 2026-09-04. Продолжение 40.27. Этот этап не включал источник в production и не менял действующий China catalog.
- **Реальный discovery contract доказан, а не угадан:** `GET /Home/CarsList` используется для anti-forgery token/cookie и явного `价格(US $)`, затем source-declared `POST /Car/SearchCarList` с `Hot=false`, `DefaultSort=1`, `PriceSort=0`, `MileageSort=0`, `YearSort=0`, `pageindex`, `pagesize=15`. URL вида `?page=2` и непроверенный внутренний API не используются.
- **Full-list exhaustion:** финальный run `33824559065`, head `e817b82a4bd7f94596ebb0adcdb492ffb52c2590` — `success`; artifact `9919508699`, digest `sha256:db9875b88c09de5b3abbb6217a66a4428c937b00476df4229c8336dc7a9c6a71`.
- **Полный индекс источника на момент run:** source total `1434`; пройдены `96/96` страниц; `1434/1434` raw numeric IDs; total min/max `1434/1434`, drift `0`; duplicate raw IDs `0`; valid identity IDs `1426`, duplicate identity IDs `0`; `8` строк отклонены как `non_usd_currency`.
- **Стратифицированный detail audit:** страницы `1, 20, 39, 58, 77, 96`; accepted exact passenger ICE после всех gates — `24`; rejected passenger ICE — `32`; blocked non-passenger — `1`; accepted makes: `现代`, `马自达`, `大众`, `比亚迪`, `宝马`, `奥迪`, `丰田`, `日产`, `长安`, `起亚`. Все sampled detail pages health-green, `failures=[]`.
- **Цена fail-closed:** первый full run `33777919699` специально не был принят, потому что Mazda CX-5 с exact list/detail parity имела source price `100 USD`. Цена не исправлялась и не заменялась рыночной оценкой. Для modern rows (2020+) source price `<2000 USD` теперь создаёт manual-price-review reject. В финальном run эта Mazda (`2049030561644670976`) заблокирована, исходные `100 USD` сохранены как evidence.
- **Exact-version conflict ledger — reject-only, без подмены source fields:** Toyota Prado 2016 3.5 TX (`diesel` vs Autohome spec 23948 gasoline); BYD Song MAX 2018 1.5T 6-seat (`SUV` vs spec 33704 `MPV`); Toyota Camry 2012 尊瑞 2.5HG (`汽油` vs spec 12931 hybrid); Geely Xingyue 2019 300T 探星者 (`汽油` vs spec 39287 `汽油+48V轻混系统`, matching 1477 cc / 130 kW); VW 朗行 2017 180TSI DSG舒适版 (`轿车` vs spec 29388 `两厢车`, matching 1.2T / 81 kW). External references только блокируют точный конфликт и никогда не заполняют/переписывают Good Car.
- **Финальная выборка реально поймала 4 reference conflicts:** Song MAX page 20, Xingyue page 39, Camry page 58, Langxing page 77. Prado не находился на шести sampled pages, но его narrow regression rule остаётся активным. Reference regression после последних правок — `7/7` pass.
- **Five-page confirmation после финальных conflict tests:** run `33824474523` — `success`; 75 raw rows, 73 valid identity, 50 joined details, 15 accepted, 3 identity-electrified blocked, 1 verified reference conflict blocked, `failures=[]`; artifact `9919467819`, digest `sha256:703fd9c5495150e726a1dd33a31ba68ee8a3db0acf8f8eb5765f397fff9647ab`.
- **Identity normalization:** добавлен отдельный regression для дублирующего `汽车` после exact make (например `吉利汽车 / 汽车 星越`), без общего fuzzy-переименования моделей.
- **Safety boundary подтверждён:** `productionWrites=false`, `classificationMutations=false`, `publishAllowedMutations=false`, `objectStorageWrites=false`, `catalogGenerationWrites=false`, `productionAllowlisted=false`; `publishAllowed=false`; текущие public generation/manifest/Object Storage/cleanup/publication не затронуты.
- **Документ evidence:** `docs/catalog-source-chngoodcar-pagination-v1-evidence.md` содержит route contract, историю intentional failures, final runs, artifact/digest и точные safety boundaries.
- **Вердикт:** Good Car доказан как массовый public CarsList source с реальной server-side pagination и стабильным inventory `1434` rows на момент проверки. Это усиливает source-level `exact_catalog`, но не превращает все `1434` строк в готовые карточки: offer-level exact gate обязателен для каждой машины.
- **Следующий безопасный шаг:** слить этот pagination/exhaustion research-пакет только в родительскую audit-ветку после полного PR CI; оставить Good Car вне production allowlist и `publishAllowed=false`; затем продолжить квалификацию остальных источников/рынков. Любое production promotion Good Car — отдельное решение после общего six-market no-write readiness.

### 40.29. Guangdong Good Car: post-40.28 price-review refinement

- **Дата checkpoint:** 2026-09-04. Раздел 40.28 остаётся неизменным историческим checkpoint для run `33824559065` (`24 accepted / 1 manual-price-review`). Этот пункт фиксирует следующий более строгий gate после ручного аудита accepted sample.
- **Новая обнаруженная аномалия:** Good Car offer `1432600975113187328`, `马自达 昂克赛拉 2017款 三厢 1.5L 自动舒适型 国V`, имеет согласованный list/detail source price `93900 USD`. Независимая exact-version проверка использована только как основание для manual review; исходная цена `93900` не заменяется, не пересчитывается и не подменяется рыночным значением.
- **Fail-closed исправление:** в `apps/web/lib/catalog/chngoodcar-price-review.ts` добавлен narrow reason `verified_exact_version_extreme_price_outlier_manual_review`. В `tests/catalog-source-chngoodcar-price-review-v1.test.ts` закреплено, что exact offer блокируется reviewed-adapter, а `sourcePrice/listPrice=93900` остаются без изменений.
- **Повторный полный proof после regression lock:** run `33825326173`, head `04b292fc5ebb45038fbeb1bbf77ada52c2f9d38a` — `success`; artifact `9919781133`, digest `sha256:0c84dc88d9fb0363dd8b027a38d243f1be44ccb607b463407a83f527be6d1610`.
- **Полный CarsList inventory снова подтверждён:** source total `1434`, `96/96` страниц, `1434` unique raw IDs, `1426` valid identity IDs, total без drift и без duplicate IDs. Stratified detail pages: `1,20,39,58,77,96`.
- **Итог более строгого sample gate:** exact passenger ICE accepted `23`; rejected passenger ICE `32`; blocked non-passenger `1`; blocked independently verified reference conflicts `4`; blocked manual-price-review `2`; `failures=[]`.
- **Две price-review строки:** Mazda CX-5 `2049030561644670976` — source `100 USD`, reason `modern_offer_in_source_under_2000_usd_band`; Mazda3/Axela `1432600975113187328` — source `93900 USD`, reason `verified_exact_version_extreme_price_outlier_manual_review`. Обе исходные цены сохраняются как evidence и не используются для автоматического exact допуска.
- **Проверки:** новый high-price regression, существующий low-price regression, identity normalization, CarsList contract, paginated exact, reference-conflict suites, typecheck, полный 96-page exhaustion и no-write envelope — green.
- **Safety boundary неизменён:** `productionWrites=false`, `classificationMutations=false`, `publishAllowedMutations=false`, `objectStorageWrites=false`, `catalogGenerationWrites=false`, `productionAllowlisted=false`, source `publishAllowed=false`. Public China generation, manifest, Object Storage, cleanup и publication не менялись.
- **Следующий шаг:** завершить PR `#829` только в родительскую audit-ветку `chore/source-partial-field-audit-v1-20260903` после полного PR CI. Good Car не добавлять в production China allowlist; production promotion разрешён только отдельным решением после общего six-market no-write readiness. После merge этого research-пакета продолжить квалификацию остальных источников/рынков.

### 40.30. KB ChaChaCha Korea: access-policy qualification останавливает автоматический crawl

- **Дата checkpoint:** 2026-09-04. Следующий новый candidate после закрытия Good Car pagination-пакета.
- **Цель:** до исследования detail/pagination отдельно проверить, разрешён ли сам публичный маршрут для автоматического каталога. KB ранее был `research_pending`/list-only candidate с полезными public search signals, но без доказанного permitted automated route.
- **Bounded probe:** run `33826631629`, head `5be42c80545baa4ce79e0a7425128322d061fb76` — `success`; artifact `9920244140`, digest `sha256:4f4c66bb38a28870c4b8fce53e5cc9313397f148f4a2a52d81574d7254fa265f`.
- **Жёсткий request envelope:** ровно `2` HTTP-запроса — `robots.txt` и registry-declared `https://www.kbchachacha.com/public/search/main.kbc`; `detailRequests=0`, `paginationRequests=0`, `externalScriptRequests=0`, raw bodies не сохранялись.
- **Live access facts:** `robots.txt=200`, observed robots policy для registry route разрешает доступ; public search page также `200`. Но в **видимом официальном public HTML** обнаружен явный запрет несанкционированного scraping/automation. Robots allowance не трактуется как отдельное разрешение при наличии такого ограничения самого сайта.
- **Решение:** `kbchachacha_korea_candidate -> class=lead_only`, `publishAllowed=false`. Текущий scope — только ручной поиск/reference navigation; automated inventory ingestion, pagination/detail crawl и публикация карточек из этого public route запрещены в нашем движке.
- **Важно:** это не означает, что данные KB плохие. Источник отклонён именно как текущий автоматический ingestion route. Requalification возможна при появлении официально разрешённого API, partner/dealer feed или письменного разрешения на автоматическое использование.
- **После permitted-access requalification:** заново доказать offer identity/direct URL, KRW price, body, fuel/powertrain, exact engineCc, exact power, mileage и listing-bound gallery. Public-page scraping нельзя использовать как обход.
- **Safety:** `productionWrites=false`, `publishAllowedMutations=false`, `objectStorageWrites=false`, `catalogGenerationWrites=false`, production catalog/generation/manifest/cleanup не менялись.
- **Evidence:** `docs/catalog-source-kbchachacha-access-policy-v1-evidence.md` и `data/catalog/source-partial-classification-v1.json`.
- **Следующий шаг:** не тратить запросы на KB без разрешённого data route; продолжить квалификацию следующего `research_pending` source, сохраняя тот же no-write/source-permission-first порядок.

## 40.31 — WorldAuto Georgia: technical field contract proven, commercial reuse blocked; Japan paused

Дата: 2026-09-04.

Ветка исследования: `chore/worldauto-detail-route-probe-v1-20260904`. Production/publication writes не выполнялись.

Что подтверждено по WorldAuto Georgia:

- permission-first/no-write цепочка завершена успешными runs: `33854811320`, `33855034005`, `33855980435`, `33856160338`, `33856330429`, `33856502011`, `33856667630`;
- frontend самого WorldAuto объявляет `GET /search/sell/car/get` и backend base URL `https://worldauto-backend-production.up.railway.app`; скрытые endpoint'ы не угадывались;
- один bounded no-param GET к source-declared search endpoint вернул `200 application/json`; пагинация и detail crawl не запускались;
- из первого сбалансированного offer-object доказаны id, 10 фото, Toyota Land Cruiser Prado, 2021, price 45000, Diesel, 2.8, 204, Automatic, AWD, mileage 0, Batumi;
- официальный UI WorldAuto показывает тот же образец как `45000$` и маркирует цены продажи в долларах;
- технически источник близок к exact contract, но это **не даёт права публикации**.

Решающая причина остановки WorldAuto:

- официальный public footer WorldAuto указывает, что content страницы, включая images, vehicle descriptions/details, является собственностью `worldauto.ge`; коммерческое reuse лицами, отличными от seller, запрещено, при использовании материалов требуется ссылка;
- поэтому для коммерческого каталога AvtoCena WorldAuto переводится в `lead_only`, `publishAllowed=false`; автоматический reuse/republication прекращён;
- все одноразовые WorldAuto qualification workflows после снятия доказательств удалены, чтобы обычные push не запускали новые запросы;
- повторно открывать автоматизацию WorldAuto можно только после явно разрешённого API/partner feed либо письменного разрешения на commercial data reuse/republication.

### Japan — пауза по указанию владельца

Япония сейчас **не входит в активную очередь qualification**. Старые Japan ledger entries остаются только историей исследования. Экспериментальные Japan branches не вливать в активный source path и новые Japan probes не запускать. Возвращаться к Японии только после нахождения кандидата, который реально отдаёт уже завершённые/отыгранные аукционные лоты под требуемый exact contract, и после явного возобновления рынка владельцем. На текущем подтверждённом состоянии такого источника в проекте нет; fixed-price/export-stock SBT/TCV/BE FORWARD не доказывают completed-auction coverage.

### Следующее действие после 40.31

Продолжать только non-Japan `research_pending` источники, строго permission-first/no-write. Приоритет — кандидат, который по уже собранным данным ближе всего к exact contract; не расширять WorldAuto и не возвращаться к Japan до снятия указанных блокеров.

## 40.32 — AutoScout24 Europe: official terms block automated query and commercial database reuse

Дата: 2026-09-04.

Ветка: `chore/autoscout24-access-field-audit-v1-20260904`.

- Следующий non-Japan candidate после WorldAuto проверен **сначала по source permission**, до технического crawl.
- Consumer GTC AutoScout24 (effective 01.04.2024), section 8.2: automated queries via scripts/search software or similar bypass of the provided online search masks are not permitted.
- Section 8.3: queried data may not be used to build a separate database, for commercial data exploitation/provision, or linked/integrated with other databases/meta-databases.
- Dealer/company GTC (effective 01.04.2025) likewise prohibits automated database querying by software and copying database contents to other websites/media unless it is the dealer's own content.
- Поэтому ранее полученные formal/exact-looking baseline rows не дают права строить automated adapter: `autoscout_europe_open -> lead_only`, `publishAllowed=false`.
- Scope сейчас: только manual public reference/link-out. Повторная qualification возможна только через официальный API/feed/agreement или письменное разрешение, которое явно покрывает automated query + commercial database reuse/republication.
- После проверки условий **не запускались** AutoScout24 list/detail/API crawler probes; обходов ограничений не делалось. Production/Object Storage/catalog writes отсутствуют.
- Japan остаётся на паузе по указанию владельца; Japan branches не возобновлять и не вливать.

### Следующее действие после 40.32

Продолжить следующий non-Japan `research_pending` source в том же порядке: сначала официальные access/reuse terms; только если они не блокируют автоматизацию — bounded no-write technical field qualification.

## 40.33 — mobile.de Europe: public scraping blocked; official partner Search-API is the permitted path

Дата: 2026-09-04.

Ветка: `chore/mobilede-access-policy-v1-20260904`.

- Source-permission-first проверка выполнена до нового technical crawl.
- Current Professional Domain GTC mobile.de (valid from 01.04.2026), Article 11: vehicle search должен идти через предоставленные search screens; unauthorized search tools, extraction/reuse, data mining, robots, grabbing, scraping и аналогичные технологии запрещены.
- Public-domain GTC содержит тот же core restriction для public marketplace.
- Поэтому `mobile_de_open -> lead_only`, `publishAllowed=false`; public-page crawler/list/detail qualification не запускать.
- Важный положительный сигнал: mobile.de публикует официальные Search-API GTC. Это не public permission: Search-API предназначен для API PARTNER и работает в рамках API Partner Agreement/Partner Application.
- Значит правильный следующий путь для mobile.de — официальный partner/API agreement, а не scraping. После получения разрешённого API scope можно заново делать exact field qualification именно на API.
- После чтения terms automated mobile.de inventory requests не запускались; production/Object Storage/catalog writes отсутствуют.
- Japan остаётся на паузе.

### Следующее действие после 40.33

Продолжить следующий non-Japan `research_pending` source: сначала official access/reuse terms, затем bounded no-write technical qualification только если terms допускают автоматизацию.

## 40.34 — La Centrale Europe: public database is consultation-only; automated reuse blocked

Дата: 2026-09-04.

Ветка: `chore/lacentrale-access-policy-v1-20260904`.

- Следующий non-Japan candidate проверен source-permission-first до нового technical crawl.
- Official La Centrale CGU, Article 5: site/content разрешены только для strictly personal use; reproduction/representation/diffusion требуют prior written and express authorization.
- Database data предоставляются публике только для pure consultation. Extraction/reuse, выходящие за то, что исключительно и строго необходимо для pure consultation, без prior written approval выходят за normal-use conditions.
- Поэтому `lacentrale_europe_candidate -> lead_only`, `publishAllowed=false`. Public list/detail/API crawler qualification не запускать, AvtoCena catalog не строить/обновлять из публичной La Centrale database.
- Повторно открывать exact technical qualification только после письменного разрешения Groupe La Centrale либо official data/API/partner feed, который явно покрывает automated commercial use, retention и republication.
- После permission check автоматические inventory requests к La Centrale не запускались; production/Object Storage/catalog writes отсутствуют.
- Japan остаётся на паузе по указанию владельца; Japan branches не возобновлять и не вливать.

### Следующее действие после 40.34

Продолжить следующий non-Japan `research_pending` source в том же порядке: сначала official access/reuse conditions; только если они не блокируют автоматизацию — bounded no-write technical field qualification.

## 40.35 — AutoUncle Europe: public scraping requires permission; official B2B API is the candidate path

Дата: 2026-09-04.

Ветка: `chore/autouncle-access-policy-v1-20260904`.

- Source-permission-first проверка выполнена до нового public inventory crawl.
- Official AutoUncle Terms of Service, last updated 27 November 2024, section 4: users agree not to scrape or collect data without permission.
- Поэтому `autouncle_europe_candidate -> lead_only`, `publishAllowed=false`; public-site list/detail crawler не запускать без разрешения.
- Положительный путь найден: AutoUncle официально предлагает B2B Automotive API для enterprise integrations с API key, market valuation, deal rating, sales-time forecast и live comparables. Это кандидат на разрешённую интеграцию, но не public-site permission.
- Exact technical qualification AutoUncle возобновлять только через API/enterprise agreement либо письменное разрешение, которое явно покрывает AvtoCena use case, нужные поля, retention и republication.
- После terms check public inventory requests не запускались; production/Object Storage/catalog writes отсутствуют.
- Japan остаётся на паузе по указанию владельца; Japan branches не возобновлять и не вливать.

### Следующее действие после 40.35

Продолжить следующий non-Japan `research_pending` source: official access/reuse conditions first; если permission path не закрыт — bounded no-write technical qualification.
