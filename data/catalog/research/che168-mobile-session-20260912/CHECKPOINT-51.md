# Domestic Che168 browser collection checkpoint

2026-09-12 15:52 UTC. 51 unique nonempty detail records preserved (+17 versus checkpoint 34). Two empty navigation captures are excluded from the count and retained in the raw journal. This is collection evidence, not published inventory.

`session-full-51.json.gz` is a self-contained JSON archive with UTF-8 files under `files`. It preserves all root capture files, full capture/revision journals, source prices where present, extracted technical rows, photo URLs, listing observations and resumable browser helper. No photo binaries are stored.

Some records have missing cash-price boxes or incomplete technical tables. `needs_data` / `totalRub:null` remain unchanged. No claims of exact calculations, full source coverage or production availability. Published from this collection: 0.

The existing mobile domestic browser session continued returning genuine car detail pages. The browser-control connection timed out intermittently; saved records survived reconnects. This does not establish a production runner or an unattended crawl. No GitHub crawl was launched and no source/login/challenge bypass was attempted.

Old `audit.json` is only the 29-record parser snapshot; do not report its numbers as the audit of these 51 records. This checkpoint does not merge PR924, change main, change any market marker, alter quality gates, or publish the China catalog.
