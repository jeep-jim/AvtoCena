import { NextResponse } from "next/server";
import { getCurrentUser, isAdminRole } from "@/lib/auth";
import { mutateDataJson } from "@/lib/data";
export async function PATCH(request:Request,{params}:{params:{id:string}}){const user=getCurrentUser(); if(!user||!isAdminRole(user.role)) return NextResponse.json({ok:false,error:"forbidden"},{status:403}); const body=await request.json().catch(()=>({})); const now=new Date().toISOString(); try{let updated:any=null; await mutateDataJson<any[]>("auth/users.json",[],(users)=>users.map((manager)=>{if(manager.id!==params.id)return manager; updated={...manager}; if(body.role) updated.role=body.role; if(body.disable) updated.status="disabled"; if(body.enable) updated.status="active"; if(body.revokeSessions) updated.sessionVersion=Number(updated.sessionVersion||0)+1; updated.updatedAt=now; return updated;})); if(!updated)return NextResponse.json({ok:false,error:"manager_not_found"},{status:404}); return NextResponse.json({ok:true,manager:updated});}catch{return NextResponse.json({ok:false,error:"storage_write_failed"},{status:500});}}
