"use client";
import { useRouter } from "next/navigation";
import { useRef, useState } from "react";
export function ManagerActions({ manager }: { manager:any }){const router=useRouter(); const op=useRef(crypto.randomUUID()); const [loading,setLoading]=useState(false); const [error,setError]=useState(""); async function send(body:any){setLoading(true);setError("");try{const res=await fetch(`/api/crm/managers/${manager.id}`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({...body, operationId: op.current})});const data=await res.json().catch(()=>({})); if(!res.ok) throw new Error(data.error||"manager_update_failed");op.current=crypto.randomUUID();router.refresh();}catch(e){setError(e instanceof Error?e.message:"manager_update_failed");}finally{setLoading(false);}} return <div className="mt-4 grid gap-3 text-sm font-bold"><button disabled={loading} onClick={()=>send({role:manager.role==="manager"?"admin":"manager"})} className="rounded-2xl bg-white/10 px-4 py-3 text-white">Изменить роль</button>{manager.status==="disabled"?<button disabled={loading} onClick={()=>send({enable:true})} className="rounded-2xl bg-emerald-500/20 px-4 py-3 text-emerald-100">Разблокировать</button>:<button disabled={loading} onClick={()=>confirm("Заблокировать менеджера?")&&send({disable:true})} className="rounded-2xl bg-red-500/20 px-4 py-3 text-red-100">Заблокировать</button>}<button disabled={loading} onClick={()=>send({revokeSessions:true})} className="rounded-2xl bg-white/10 px-4 py-3 text-white">Отозвать все сессии</button>{error&&<div className="rounded-2xl bg-red-500/15 px-4 py-3 text-red-100">{error}</div>}</div>}
