// Compatibility entry point. The approved UI is an attached dropdown, not a panel below the whole grid.
// The shared regression also covers compact date/fuel columns, extra hybrid/N1 tiles, inputs and reset.
await import('./attached-parameter-dropdowns.mjs');
