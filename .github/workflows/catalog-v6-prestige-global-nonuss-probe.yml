name: Catalog V6 · Prestige global non-USS probe

on:
  workflow_dispatch:
  push:
    paths:
      - ".github/workflows/catalog-v6-prestige-global-nonuss-probe.yml"
      - "scripts/prestige-japan-global-nonuss-probe.mjs"

permissions:
  contents: read

concurrency:
  group: catalog-v6-prestige-global-nonuss-probe
  cancel-in-progress: true

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - run: node scripts/prestige-japan-global-nonuss-probe.mjs
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: prestige-japan-global-nonuss-probe
          path: prestige-japan-global-nonuss-probe.json
          if-no-files-found: error
          retention-days: 3
