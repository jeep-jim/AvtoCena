name: Emergency cancel stale preliminary publisher
on:
  push:
    branches: [main]
    paths: [".github/workflows/emergency-cancel-31361354007.yml"]
permissions:
  actions: write
jobs:
  cancel:
    runs-on: ubuntu-latest
    steps:
      - name: Cancel stale run
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh api -X POST repos/$GITHUB_REPOSITORY/actions/runs/31361354007/cancel
