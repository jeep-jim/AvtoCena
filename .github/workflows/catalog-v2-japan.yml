name: Catalog V2 · Japan
run-name: Catalog V2 · Japan · ${{ github.sha }}

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  japan:
    uses: ./.github/workflows/catalog-v2-market-reusable.yml
    with:
      market: japan
      display_name: Япония
    secrets: inherit
