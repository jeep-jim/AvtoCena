name: Deprecated Korea Japan repair

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled. Use Catalog clean rebuild all five markets."
