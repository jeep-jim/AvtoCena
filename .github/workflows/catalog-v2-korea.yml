name: Catalog V3 · Korea · 10k
run-name: Catalog V3 · Korea · 10k · ${{ github.sha }}

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - ".github/market-runs/korea"

# Automatic scheduling lives only in catalog-v3-sequential-queue.yml.
permissions:
  contents: read

jobs:
  korea:
    uses: ./.github/workflows/catalog-v3-market-10k-reusable.yml
    with:
      market: korea
      display_name: Корея
      reset_cursor: true
      priority_target: "8000"
    secrets: inherit
