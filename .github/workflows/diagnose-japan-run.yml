name: Diagnose Carused production gallery

on:
  pull_request:

permissions:
  contents: read

jobs:
  inspect:
    runs-on: ubuntu-latest
    env:
      JSON_STORAGE_DRIVER: object
      YC_OBJECT_STORAGE_ENDPOINT: ${{ secrets.YC_OBJECT_STORAGE_ENDPOINT }}
      YC_OBJECT_STORAGE_REGION: ${{ secrets.YC_OBJECT_STORAGE_REGION }}
      YC_OBJECT_STORAGE_BUCKET: ${{ secrets.YC_OBJECT_STORAGE_BUCKET }}
      YC_OBJECT_STORAGE_ACCESS_KEY_ID: ${{ secrets.YC_OBJECT_STORAGE_ACCESS_KEY_ID }}
      YC_OBJECT_STORAGE_SECRET_ACCESS_KEY: ${{ secrets.YC_OBJECT_STORAGE_SECRET_ACCESS_KEY }}
      YC_OBJECT_STORAGE_PREFIX: ${{ secrets.YC_OBJECT_STORAGE_PREFIX }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - name: Inspect exact Probox internal row
        shell: bash
        run: |
          node --import tsx --input-type=module <<'NODE'
          const { readAllOffersForMaintenance } = await import('./apps/web/lib/catalog/storage.ts');
          const rows = await readAllOffersForMaintenance();
          const offer = rows.find((row) => String(row.id) === '0f9e3ab6d54902d209298f88');
          if (!offer) throw new Error('internal_offer_not_found');
          const raw = offer?.operational?.raw || {};
          console.log(JSON.stringify({
            id: offer.id,
            sourceId: offer.sourceId,
            sourceOfferId: offer.sourceOfferId,
            sourceUrl: offer?.operational?.sourceUrl,
            imageCount: Array.isArray(offer.images) ? offer.images.length : 0,
            images: (offer.images || []).map((x) => x?.url || x),
            rawKeys: Object.keys(raw),
            rawImages: raw.images || null,
            rawPhotos: raw.photos || null,
            rawGallery: raw.gallery || null,
            rawImageUrls: raw.imageUrls || null,
            rawPhotoUrls: raw.photoUrls || null,
            rawParsedImages: raw?.parsed?.images || null,
          }, null, 2));
          NODE
