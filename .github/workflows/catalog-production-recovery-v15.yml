name: Catalog source-scale daily DISABLED on strict certification branch

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  stopped:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Raw/source-scale publishing is disabled on codex-strict-catalog-source-only. Certify exact sources through the strict source-only ladder before production publishing."
