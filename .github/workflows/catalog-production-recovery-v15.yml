name: Catalog source-scale cancel guard

on:
  workflow_dispatch:
  push:
    branches:
      - codex-strict-catalog-source-only
    paths:
      - ".github/workflows/catalog-production-recovery-v15.yml"

permissions:
  contents: read

concurrency:
  group: catalog-source-scale-daily
  cancel-in-progress: true

jobs:
  stopped:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Cancelling legacy raw/source-scale run on the strict certification branch; no catalog publication is performed."
