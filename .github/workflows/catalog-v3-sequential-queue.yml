name: Catalog V3 · Sequential queue (manual only)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    name: Automatic queue is disabled
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Automatic catalog chaining is disabled."
          echo "Run a market workflow manually from the Actions UI when needed."
