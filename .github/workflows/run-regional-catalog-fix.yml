name: Run regional catalog fix (legacy, manual only)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy automatic catalog patch workflow is disabled."
