name: Catalog strict · DubiCars exact source-only 30k DISABLED

on:
  workflow_dispatch:
  push:
    branches:
      - codex-strict-catalog-source-only
    paths:
      - ".github/workflows/catalog-strict-dubicars-30k.yml"

permissions:
  contents: read

concurrency:
  group: catalog-strict-dubicars-30k
  cancel-in-progress: true

jobs:
  stopped:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Long sequential DubiCars 30k certification is disabled. Use the parallel all-markets collector instead."
