name: Catalog V2 · Europe
run-name: Catalog V2 · Europe · ${{ github.sha }}

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  europe:
    uses: ./.github/workflows/catalog-v2-market-recovery-reusable.yml
    with:
      market: europe
      display_name: Европа
    secrets: inherit
