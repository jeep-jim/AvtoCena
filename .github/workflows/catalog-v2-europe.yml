name: Catalog V3 · Europe · 10k
run-name: Catalog V3 · Europe · 10k · ${{ github.sha }}

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - ".github/market-runs/europe"
  schedule:
    - cron: "17 0 * * *"

# Daily independent market refresh. Verified rows remain inside the configured
# 30-day window; expired and policy-rejected cards are removed.
permissions:
  contents: read

jobs:
  europe:
    uses: ./.github/workflows/catalog-v3-market-10k-reusable.yml
    with:
      market: europe
      display_name: Европа
      reset_cursor: ${{ github.event_name != 'schedule' }}
    secrets: inherit
