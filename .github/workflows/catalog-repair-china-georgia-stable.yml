name: Catalog repair · China + Georgia stable (retired)
run-name: Retired China + Georgia historical repair · ${{ github.sha }}

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Historical repair is retired
        run: |
          echo "This workflow depended on historical recovery artifacts and obsolete catalog quotas."
          echo "Use current canonical collectors and preservation publishers for China and Georgia."
