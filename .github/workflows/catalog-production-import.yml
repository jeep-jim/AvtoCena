name: Catalog daily verified all markets (paused)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Automatic catalog imports are temporarily paused."
          echo "Use the safe staged rebuild workflow after the public catalog has been restored."
