name: Catalog production import

on:
  workflow_dispatch:
    inputs:
      reset:
        description: "Очистить текущий каталог перед импортом"
        required: false
        default: false
        type: boolean
  push:
    branches:
      - main
    paths:
      - "apps/web/lib/catalog/**"
      - "scripts/catalog-import-sample.mjs"
      - ".github/workflows/catalog-production-import.yml"
  schedule:
    - cron: "17 */2 * * *"

concurrency:
  group: catalog-production-import
  cancel-in-progress: false

jobs:
  import:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npm run typecheck
      - run: npm test
      - name: Remove test-generated local catalog data
        run: |
          rm -rf data/catalog/internal data/catalog/generations data/catalog/scans data/catalog/health data/catalog/imports data/catalog/sources
          rm -f data/catalog/manifest.json data/catalog/import-lock.json
      - name: Import Korea, China, Japan, UAE and Europe
        run: npm run catalog:import:sample
        env:
          JSON_STORAGE_DRIVER: object
          YC_OBJECT_STORAGE_ENDPOINT: ${{ secrets.YC_OBJECT_STORAGE_ENDPOINT }}
          YC_OBJECT_STORAGE_REGION: ${{ secrets.YC_OBJECT_STORAGE_REGION }}
          YC_OBJECT_STORAGE_BUCKET: ${{ secrets.YC_OBJECT_STORAGE_BUCKET }}
          YC_OBJECT_STORAGE_ACCESS_KEY_ID: ${{ secrets.YC_OBJECT_STORAGE_ACCESS_KEY_ID }}
          YC_OBJECT_STORAGE_SECRET_ACCESS_KEY: ${{ secrets.YC_OBJECT_STORAGE_SECRET_ACCESS_KEY }}
          YC_OBJECT_STORAGE_PREFIX: ${{ secrets.YC_OBJECT_STORAGE_PREFIX }}
          CATALOG_IMPORT_RESET: ${{ github.event_name == 'workflow_dispatch' && inputs.reset && 'true' || 'false' }}
          CATALOG_TARGET_PUBLIC_OFFERS: "224"
          CATALOG_ENCAR_DIRECT_PAGE_SIZE: "24"
          CATALOG_CHE168_GLOBAL_PAGE_SIZE: "24"
          CATALOG_CHE168_GLOBAL_MAX_BRANDS: "12"
          CATALOG_IMPORT_MAX_OFFERS: "48"
          CATALOG_IMPORT_MAX_DETAILS: "48"
          CATALOG_IMPORT_MAX_PAGES: "4"
          CATALOG_MAX_IMAGES_PER_OFFER: "6"
          CATALOG_OFFER_RETENTION_MS: "172800000"
          CATALOG_STALE_GRACE_MS: "172800000"
          CATALOG_IMPORT_REPORT_FILE: catalog-import-public-markets-report.json
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: catalog-import-public-markets-report
          path: catalog-import-public-markets-report.json
          if-no-files-found: warn
      - name: Summary
        if: always()
        run: |
          node - <<'NODE' >> "$GITHUB_STEP_SUMMARY"
          const fs = require('fs');
          const path = 'catalog-import-public-markets-report.json';
          if (!fs.existsSync(path)) {
            console.log('Отчёт импорта не создан. Смотрите журнал шага импорта.');
            process.exit(0);
          }
          const json = JSON.parse(fs.readFileSync(path, 'utf8'));
          for (const key of ['imported','updated','expired','skipped','imageFailures','underfilledImages','reusedImageSets','publicOffers','targetPublicOffers','targetReached','generationId']) {
            console.log(`- ${key}: ${json[key] ?? ''}`);
          }
          console.log('\n### Автомобили по рынкам');
          for (const [market, count] of Object.entries(json.publicByMarket || {})) {
            console.log(`- ${market}: ${count}`);
          }
          console.log('\n### Источники');
          for (const source of json.sources || []) {
            console.log(`- ${source.sourceId} (${source.market || 'multi'}): ${source.ok ? `OK, seen ${source.seen ?? 0}` : `ERROR ${source.error || ''}`}`);
          }
          NODE
