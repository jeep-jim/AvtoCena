name: Catalog emergency force-cancel DISABLED

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Emergency force-cancel is disabled; legacy raw publisher is already stopped."
