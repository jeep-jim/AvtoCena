name: Catalog strict · K Car exact source-only 1 · PASSED

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  certified:
    runs-on: ubuntu-latest
    steps:
      - run: echo "K Car target-1 strict source-only stage already passed. Manual-only reproducibility stub; no publication."
