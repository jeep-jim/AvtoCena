name: Diagnose mass-market patch (legacy, manual only)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy automatic diagnostic workflow is disabled."
