name: Catalog Europe verified rebuild v23 (paused)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "The destructive Europe rebuild is paused."
          echo "It must not publish an empty catalog before replacement data is ready."
