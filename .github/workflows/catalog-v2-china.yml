name: Catalog V3 · China · 10k
run-name: Catalog V3 · China · 10k · ${{ github.sha }}

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - ".github/market-runs/china"
  schedule:
    - cron: "17 8 * * *"

# Daily independent market refresh. Existing rows remain only inside the
# configured three-day window; expired and policy-rejected cards are removed.
permissions:
  contents: read

jobs:
  china:
    uses: ./.github/workflows/catalog-v3-market-10k-reusable.yml
    with:
      market: china
      display_name: Китай
      reset_cursor: ${{ github.event_name != 'schedule' }}
    secrets: inherit
