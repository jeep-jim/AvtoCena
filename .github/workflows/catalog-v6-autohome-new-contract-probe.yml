name: Catalog V6 · Autohome exact contract probe

on:
  workflow_dispatch:
  push:
    paths:
      - ".github/workflows/catalog-v6-autohome-new-contract-probe.yml"
      - "scripts/autohome-new-contract-probe.mjs"

permissions:
  contents: read

concurrency:
  group: catalog-v6-autohome-new-contract-probe
  cancel-in-progress: true

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - run: node scripts/autohome-new-contract-probe.mjs
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: autohome-new-contract-probe
          path: autohome-new-contract-probe.json
          retention-days: 3
