name: Catalog full quality refresh v17 (paused)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "The old destructive quality refresh is paused."
          echo "Production data must be rebuilt off-line and published only after validation."
