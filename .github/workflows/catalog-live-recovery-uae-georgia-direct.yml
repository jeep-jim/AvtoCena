name: Catalog live recovery · UAE + Georgia direct
run-name: Direct recovery UAE + Georgia · ${{ github.sha }}

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/catalog-live-recovery-uae-georgia-direct.yml"
      - "apps/web/lib/catalog/auto-georgia-strict-source.ts"
      - "tests/auto-georgia-price.test.ts"
      - "scripts/catalog-live-recovery-publish-batch.mjs"

permissions:
  contents: read

concurrency:
  group: catalog-live-recovery-uae-georgia-direct
  cancel-in-progress: true

env:
  JSON_STORAGE_DRIVER: object
  YC_OBJECT_STORAGE_ENDPOINT: ${{ secrets.YC_OBJECT_STORAGE_ENDPOINT }}
  YC_OBJECT_STORAGE_REGION: ${{ secrets.YC_OBJECT_STORAGE_REGION }}
  YC_OBJECT_STORAGE_BUCKET: ${{ secrets.YC_OBJECT_STORAGE_BUCKET }}
  YC_OBJECT_STORAGE_ACCESS_KEY_ID: ${{ secrets.YC_OBJECT_STORAGE_ACCESS_KEY_ID }}
  YC_OBJECT_STORAGE_SECRET_ACCESS_KEY: ${{ secrets.YC_OBJECT_STORAGE_SECRET_ACCESS_KEY }}
  YC_OBJECT_STORAGE_PREFIX: ${{ secrets.YC_OBJECT_STORAGE_PREFIX }}
  CATALOG_IMAGE_CDN_URL: ${{ secrets.CATALOG_IMAGE_CDN_URL }}
  CATALOG_IMAGE_STORAGE_MODE: source_urls_only
  RECOVERY_TARGET: "1000"
  RECOVERY_MAX_PAGES: "120"
  RECOVERY_TIME_LIMIT_MS: "2700000"
  RECOVERY_PREFERRED_MAX_RUB: "8000000"

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: |
          node --check scripts/catalog-live-recovery-direct-exact.mjs
          node --check scripts/catalog-live-recovery-publish.mjs
          node --check scripts/catalog-live-recovery-publish-batch.mjs
          node --import tsx --test tests/auto-georgia-price.test.ts
          npm run typecheck

  collect:
    needs: validate
    runs-on: ubuntu-latest
    timeout-minutes: 55
    strategy:
      fail-fast: false
      matrix:
        market: [uae, georgia]
    env:
      RECOVERY_DIRECT_MARKET: ${{ matrix.market }}
      RECOVERY_OUTPUT: catalog-rebuild-${{ matrix.market }}.json
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - name: Collect directly from the exact adapter without generic refetch wrappers
        run: npx tsx scripts/catalog-live-recovery-direct-exact.mjs
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: catalog-direct-recovery-${{ matrix.market }}
          path: catalog-rebuild-${{ matrix.market }}.json
          if-no-files-found: warn
          retention-days: 3

  publish:
    needs: [validate, collect]
    # Georgia live writes stay quarantined until exact listing-bound gallery identity is explicitly validated.
    if: ${{ false }}
    runs-on: ubuntu-latest
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - uses: actions/download-artifact@v4
        with:
          pattern: catalog-direct-recovery-*
          path: recovery-input
          merge-multiple: true
      - name: Publish UAE and Georgia in one atomic generation
        env:
          RECOVERY_BATCH_MARKETS: uae,georgia
          RECOVERY_BATCH_INPUT_DIR: recovery-input
          RECOVERY_BATCH_REPORT: catalog-direct-recovery-batch-publish-report.json
          RECOVERY_PUBLISH_MAX: "3000"
        run: npx tsx scripts/catalog-live-recovery-publish-batch.mjs
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: catalog-direct-recovery-publish-reports
          path: catalog-direct-recovery-batch-publish-report.json
          if-no-files-found: warn
          retention-days: 3
