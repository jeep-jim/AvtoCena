name: Catalog · refresh current read models
run-name: One-hop catalog read models · ${{ github.sha }}

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - .github/workflows/catalog-current-read-models.yml
      - apps/web/lib/catalog/display-identity.ts
      - apps/web/lib/catalog/brands.ts
      - apps/web/lib/catalog/public-identity-policy.ts
      - .github/market-runs/refresh-current-read-models
      # persistCatalogOffers already publishes current read models atomically.
      # Keep storage changes out of this second writer to avoid cancelling a
      # recovery publisher waiting in the shared catalog concurrency group.
  workflow_run:
    workflows:
      - "Catalog V3 · Sequential production queue"
      - "Catalog UAE · publish preserved source shards"
      - "Catalog live · daily working markets"
      - "Catalog Japan · publish verified Prestige aggregate"
      - "Catalog V6 · Prestige exact sold up-to-30k"
      - "Catalog · apply certified 30-minute power"
      - "Catalog reindex from vehicle knowledge"
    types: [completed]

permissions:
  contents: read

concurrency:
  group: catalog-live-daily-working-markets
  # Serialize every object-storage catalog writer and read-model repair.
  cancel-in-progress: false

env:
  JSON_STORAGE_DRIVER: object
  YC_OBJECT_STORAGE_ENDPOINT: ${{ secrets.YC_OBJECT_STORAGE_ENDPOINT }}
  YC_OBJECT_STORAGE_REGION: ${{ secrets.YC_OBJECT_STORAGE_REGION }}
  YC_OBJECT_STORAGE_BUCKET: ${{ secrets.YC_OBJECT_STORAGE_BUCKET }}
  YC_OBJECT_STORAGE_ACCESS_KEY_ID: ${{ secrets.YC_OBJECT_STORAGE_ACCESS_KEY_ID }}
  YC_OBJECT_STORAGE_SECRET_ACCESS_KEY: ${{ secrets.YC_OBJECT_STORAGE_SECRET_ACCESS_KEY }}
  YC_OBJECT_STORAGE_PREFIX: ${{ secrets.YC_OBJECT_STORAGE_PREFIX }}
  CATALOG_IMAGE_CDN_URL: ${{ secrets.CATALOG_IMAGE_CDN_URL }}
  CATALOG_ALLOW_EMPTY_MARKETS: "georgia"

jobs:
  refresh:
    if: ${{ github.event_name != 'workflow_run' || github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - name: Validate current read-model publisher
        run: |
          node --check scripts/catalog-publish-current-read-models.mjs
          node --import tsx --test tests/offer-navigation-performance.test.ts tests/catalog-filter-search.test.ts tests/catalog-current-read-model-empty-market-policy.test.ts tests/ai-product-feed.test.ts
          npm run typecheck
      - name: Publish one-hop projections and offer shards
        run: |
          set -o pipefail
          npx tsx scripts/catalog-publish-current-read-models.mjs | tee catalog-current-read-models.log
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: catalog-current-read-models
          path: catalog-current-read-models.log
          if-no-files-found: warn
          retention-days: 3
