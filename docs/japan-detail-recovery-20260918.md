# Япония: характеристики, расчёт и аукционные листы

Владелец поручил полностью удалить прежнюю интеграцию из текущего проекта и будущих сборов, использовать все сохранённые характеристики и добавлять аукционный лист последним в галерею.

## Проверенные результаты

- Публикация 35321333754: удалены 1847 карточек прежнего источника, сохранены все 8021 ProAuctions. Поколение `gen_1789717947575_7eea23a0`.
- Контрольные суммы всех остальных рынков совпали. Общая проверка индексов: `mismatches=[]`, `invalidReady=0`.
- Исходный HTML сохранён для всех 11463 собранных карточек; повторное открытие списка и карточек для восстановления не требуется.
- В Corolla Cross `30222301` уже были 140 л.с., 103 кВт и 1800 см³. Причина скрытия мощности: отсутствовавшее поле происхождения мощности получало статус `estimated` при нормализации. Новые импорты сохраняют `source_exact`; прежняя запись восстанавливается только при совпадении таблицы, ID, URL и точного свидетельства мощности.
- На рабочем сайте поля карточки `02defbbae91fe890a6b1dc3c` заполнены. API расчёта вернул 2586104 ₽, `requiresConfirmation=true`, дата выпуска — `year_midpoint`. Объём из аукционных данных остаётся редактируемым предположением и не превращается в подтверждённый точный рабочий объём.
- Лист принимается только из блока «Аукционный лист», с сервера изображений и из директории того же лота. Изображение декодируется и добавляется после фотографий автомобиля. Минимум две фотографии автомобиля проверяется независимо от листа.

## Удаление интеграции

Удалены адаптеры, задания сбора/обогащения, команды пакета, старые фикстуры, одноразовая миграция и записи характеристик прежнего источника. Сохранены независимые данные и их происхождение; исходные значения не переименовываются в сведения другого поставщика. Поисковый индекс и отчёты справочника пересобраны штатным валидатором. Локальные изображения марок используют нейтральные пути и не обращаются к внешнему поставщику.

## Публикация восстановленных листов

Подготовка: запуск `35321658779`, артефакт `proauctions-detail-patches`. Публикация автоматически запускается после завершения только этого разрешённого запуска; явный маркер оставлен для восстановления при пропущенном событии. Неполный архив блокируется проверками покрытия опубликованных лотов. Публикуются только обновления уже существующих японских карточек; проверяются совпадение лота, цены, года, исходного URL, сохранность всех старых фотографий и контрольные суммы остальных рынков. Новые лоты параллельного продолжения сбора сохраняются без изменений.

## Завершение и независимая проверка

- Восстановление `35321658779`: 10166 из 10166 листов, `failed=[]`.
- Публикация `35327515322`: поколение `gen_1789722333785_09d35723`, 8021 лист добавлен последним в галерею; 7852 подтверждения мощности восстановлены. Все проверки перед переключением manifest прошли. После переключения сетевой GET европейского чанка завершился ошибкой, поэтому исходный run красный, хотя публикация состоялась. Исправление отчётности фиксирует `published=true` сразу после commit manifest и отдельно отмечает результат завершающей проверки.
- Read-only проверка [35329661079](https://github.com/jeep-jim/AvtoCena/actions/runs/35329661079): SUCCESS; `published=true`, `verified=true`. Она прочитала неизменяемые поколения, восстановила ожидаемые японские записи из предыдущих данных и листов, сравнила полные записи и порядок фото, а также контрольные суммы остальных рынков. Каталог повторно не записывался.
- Счётчики: Япония 8021, Корея 14379, Китай 19368, ОАЭ 670, Европа 20551, Грузия 8355; всего 71344. Read-model parity: `mismatches=[]`; аудит: `invalidReady=0`; выборочная проверка карточек прошла.
- На публичной Corolla подтверждены две прежние фотографии и лист `1789240244.7722_1.webp` третьим, поля 1800 см³ и 140 л.с., отсутствие ссылки на посредника. Источник мощности сохраняет 103 кВт. Неподтверждённые или конфликтные параметры других автомобилей не подменялись придуманными значениями.
- Полное удаление интеграции слито и развёрнуто в PR #1037/#1038. Исправление завершения отчёта и инструмент независимой проверки — PR #1039.
