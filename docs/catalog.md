# JSON catalog operations

The site reads prepared JSON chunks from `data/catalog/**` and never waits for external marketplaces during HTTP requests.
Scheduled updates are intentionally disabled; enable the commented weekly cron only after source/legal review.
