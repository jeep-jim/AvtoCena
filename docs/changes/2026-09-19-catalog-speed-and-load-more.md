# Скорость каталога и подгрузка — 19.09.2026

Задача владельца: устранить зависания, заменить цифровую пагинацию на удобный просмотр с сохранением уже показанных автомобилей на десктопе и телефоне. Полный аудит шести рынков и формул — следующий этап, пока не объявлен завершённым.

## Подтверждённые причины и изменения

- Ранее одна стандартная страница рынка вызывала `searchOffers` восемь раз для окна разнообразия. Каждый вызов заново фильтровал и сортировал проекцию рынка. Теперь окно получает один внутренний ограниченный запрос; публичный лимит API остаётся 48. Новый интерфейс выдаёт по 24 карточки.
- Для японских расчётов превью требуются полные записи; готовые расчёты ранее сохранялись всего на 60 секунд. Повторное использование увеличено до 15 минут, ключ по-прежнему включает обновление предложения, исходную цену, конфигурацию и день расчёта. Формулы и условия допуска цены не изменены.
- Добавлен ограниченный серверный кеш готовых страниц: 32 записи, 12 МБ сериализованных данных, 60 секунд, объединение одновременных запросов. Публичный поисковый API допускает кеширование на 30 секунд. Ошибки не записываются как успешный ответ.
- Убраны четыре автоматических запроса полных карточек через 120 мс после открытия страницы; предварительная загрузка остаётся по намерению перейти. Первые видимые фотографии загружаются сразу, остальные лениво.
- Японские обложки используют 400px-версию того же файла и каталога лота у источника. Проверенный пример `1881816509/400/1789240274.5197_2.webp`: HTTP 200, 17 790 байт. При ошибке миниатюры клиент переключается на исходный адрес. Галереи и аукционные листы не изменены; новые фотографии в Object Storage не копируются.
- В браузере исходной версии зафиксированы ошибки React #425/#422. Оформитель фильтров запускался из общего layout и мог менять серверную разметку до гидратации фильтра. Он перенесён внутрь компонента фильтров, чтобы запускаться после его монтирования. Исчезновение ошибки проверяется отдельно на опубликованной версии.

## Поведение просмотра

«Показать ещё» добавляет следующую порцию ниже, показывает число загруженных автомобилей и состояние ожидания. Повторный клик во время запроса не запускает дубль. Ошибка оставляет загруженный список и возможность повторить. Последняя порция завершает список. Возврат из карточки восстанавливает загруженные React-элементы и прокрутку в этой вкладке (до шести контекстов, срок 30 минут). Изменение фильтров создаёт отдельный контекст. Полная перезагрузка начинает с адресованной страницы; старые ссылки `page=` и переход без JavaScript поддержаны.

## Начальные замеры

Один проход curl с компрессией из рабочего подключения, до публикации изменений:

| Запрос | Первый байт | Полный ответ |
|---|---:|---:|
| `/api/health` | 7,99 с | 8,03 с |
| `/cars?market=japan` | 37,48 с | 37,54 с |
| `/api/catalog/search?market=japan&pageSize=48` | 30,27 с | 30,35 с |
| Карточка X-Trail `6c52ef91feab0a62d14a697c` | 11,11 с | 11,17 с |

Это время из конкретного подключения, включая сеть; не чистое время работы сервера и не статистика всех посетителей. Даже health занимал около 8 секунд. Нельзя обещать такую же скорость или ускорение каждому посетителю по одной выборке. Источник японских фото также отвечал медленно. Итоговые замеры и проверка публикации фиксируются в PR.

## Проверки

- Сборка production и TypeScript.
- Проверка равенства порядка и состава одного окна восьми отдельным запросам; ограничение публичного API.
- Проверки кеша, снятых предложений и равенства японского превью подробному расчёту.
- Браузерный сценарий на 390 и 1440 px: добавление, ошибка/повтор, последняя порция, возврат и сохранение прокрутки.

## Дополнение владельца: вернуть резерв курса

Восстановлена прежняя формула: отдельная последняя строка «Резерв на изменение курса» = округление 2% цены автомобиля в рублях. Сам официальный курс, пошлины, логистика и аванс не изменяются этой строкой. 2% подтверждены историей до PR #990. Активная политика всех шести рынков снова 2%, в том числе при сохранённом старом нуле. Ранее удалённые экспортные расходы не возвращаются.

Японские сохранённые расчёты: вычитается точная прежняя сумма резерва, добавляется новая от сохранённой цены авто; повторное применение не накапливает сумму. В компактной проекции сохраняется база цены авто; старые проекции получают её из полной записи. При отсутствии доказуемой базы новая сумма не выдумывается. Проверяются совпадение списка и карточки, суммы строк и неизменность исторического курса.

## Историческая точка передачи до разрешения на публикацию

- Реализация сохранена локальным коммитом `ed7bb3b2` в ветке `fix/catalog-speed-load-more`, рабочая папка `/workspace/scratch/9c467e3a164c/catalog-speed`.
- Production build и TypeScript прошли. Первоначальный полный прогон: 1443 теста, 1436 прошли, 7 старых ожиданий требовали обновления под вынесенный модуль/восстановленный резерв/отключённый фон prefetch. После исправлений все 38 целевых проверок прошли; повторный полный прогон завершён: 1444/1444 прошли, ошибок нет (`/tmp/resume-tests-final.log`).
- На этой исторической точке отправка ветки была отклонена автоматическим approval review. Позже пользователь явно разрешил отправку и публикацию; блокер снят, PR #1053–#1055 слиты. Актуальный итог — в конце этого документа.
- План этой точки впоследствии выполнен через PR #1053–#1055. Результаты публикации, браузерных сценариев и замеров приведены ниже.

## Production проверка PR #1053 и дополнительное исправление

- Пользователь явно разрешил отправку и публикацию. PR #1053 прошёл CI 35422804569 и браузерную проверку 35422802242, опубликован релиз c9efdb1a1e2dd498f074d2c877a02f50ebd4dc78.
- На живом сайте обнаружен HTTP 500 на Server Action подгрузки с Origin https://avtocena.com; Запрос без Origin вернул HTTP 200, но при разборе оказался HTML после перенаправления, поэтому не доказывает успешное выполнение action. Исходный код Next проверяет Origin против forwarded host; несовпадение с внутренним host Yandex проверяется исправлением и повторным живым сценарием. Добавлены только два точных публичных allowedOrigins: avtocena.com и www.avtocena.com.
- В браузере остались #425/#422. Убран потенциально проблемный клиентский noscript с вложенной ссылкой. Ошибка также воспроизводится в общем каталоге без этого компонента, поэтому её основная причина ещё исследуется. Кнопка заменена на обычную ссылку с ролью кнопки: без JS открывает следующую страницу, с JS добавляет порцию. Повтор при ошибке и блокировка двойных запросов сохранены.
- Прогон теста подгрузки теперь должен проверять SSR-гидратацию, а не только клиентское монтирование. Повторная публикация и живая проверка пока ожидаются.

## Установленная причина гидратации

Диагностический run 35423818479 сравнил фактический текст server/client на 390 и 1440 px: CSS в cars/layout.tsx содержал на сервере `&quot;`, `&gt;`, `&#x27;`, а в браузере исходные кавычки и символы. Стили передаются как доверенная статическая CSS-строка через dangerouslySetInnerHTML; та же проблема исправлена в EditablePowerTile, InlineOfferParameters и SellerPrice. Пользовательские значения в CSS не интерполируются. SSR-браузерный тест подгрузки теперь включает настоящий CarsLayout и падает при ошибке гидратации.

## Проверка публикации и замеры

- PR #1054 опубликован как bbc708a7f9a1a248517776a5de7ba66fc521758b; живая подгрузка вернула 48 уникальных карточек, возврат из второй порции восстановил 48.
- PR #1055 прошёл CI 35424035709 и SSR-браузерный тест 35424034447, слит как d5fae22a4ee72b149fa28a51dfd793aed046e0ca. Итог публикации и точного живого сценария фиксируется ниже.
- Резерв на живом Nissan X-Trail 6c52ef91feab0a62d14a697c: цена авто 203 134 ₽, резерв 4 063 ₽ отдельной последней строкой, итог 1 510 481 ₽. Обеспечительный платёж не прибавляется повторно.
- Последовательные HTTP-замеры прогретой версии bbc708a7: health 8,142 с; Япония 8,729 с; повтор 7,649 с. Все HTTP 200. Исходная выборка была 37,54 с для Японии при health 8,03 с. Сеть рабочего подключения доминирует; это не p95 и не гарантия холодной загрузки. Ранее во время параллельного прогрева запрос search на 48 карточек превысил 55 с — его нельзя скрывать или заменять успехом HTML.
- Источник фотографий остаётся ограничением: для лота 1881816580 обложка /400/ вернулась HTTP 200, 13 490 байт за 8,34 с; оригинал получил только 8 192 из 53 126 байт за 25 с и завершился тайм-аутом. Пустые места при задержках внешнего источника полностью не устранены. Повторное хранение снимков в облаке не вводилось.

## Ранний клик фильтров

- Релиз d5fae22 опубликован: deploy 35424263054 успешен; связанные проверки редакторов параметров 35424263083 успешны. Ручной браузер подтвердил 24→48 уникальных карточек, прежние первые 24, возврат к 48 и прокрутке 2412 px, отсутствие React-ошибок.
- Живая мобильная проверка 35424263060 остановилась на первом клике фильтров: ошибок React уже нет, но обработчики ещё не подключились. Диагностический run 35424584628 подтвердил HTML без клиентского оформления, первый клик дал 0 шторок, повтор через 4 секунды — 1.
- PR #1056: кнопки открытия мобильных/расширенных фильтров нативно disabled и aria-busy до монтирования, после подключения обработчиков доступны. Автотест ждёт реальную доступность кнопки, без добавления фиксированных задержек. TypeScript прошёл. Живая проверка запускается для точного нового релиза.

- PR #1056 прошёл CI 35424695804 и Chromium/WebKit 35424692877; слит как 4ca107a7932e56e20a0f8cb10386ec3b98cac899. Итоговая живая проверка этого релиза фиксируется ниже.

## Уточнение браузерных проверок

- Релиз 4ca107a подтверждён `/api/health`; deploy 35424896178 успешен. Ранний клик фильтров прошёл, React-ошибок нет.
- Первая проверка возврата 35424896202 сравнивала прокрутку до дополнительной автопрокрутки Playwright. Диагностический run 35425196014 зафиксировал: измерение до клика 2272 px, реальный клик 2634 px, восстановленная прокрутка 2634 px, 48 карточек сохранены. Это ошибка точки измерения в тесте, а не восстановления на сайте. Теперь эталон фиксируется capture-событием фактического клика.
- Живой прогон фильтров 35424896153 остановился на cookie-баннере, перекрывающем контрол: тест использовал устаревшие селектор/текст закрытия. Он исправлен на реальное уведомление «Уведомление о cookie» / «Закрыть», после готовности обработчиков.

## Итог подтверждённой проверки

- Итоговый живой прогон [35425297615](https://github.com/jeep-jim/AvtoCena/actions/runs/35425297615) успешен на релизе `4ca107a7932e56e20a0f8cb10386ec3b98cac899`: 390 и 1440 px, открытие фильтров, 24→48 уникальных карточек, сохранение первых 24, переход/возврат, точная прокрутка, `errors=[]`.
- Время из GitHub браузера: 390 px — первая загрузка 18,531 с, новая порция 14,005 с, возврат 230 мс; 1440 px — 13,070 с, 11,162 с и 243 мс соответственно. Это подтверждение поведения, но не приемлемой итоговой скорости первой/новой порции. Дальнейшее профилирование серверного чтения/подготовки японских расчётов остаётся открытым; не списывать эти задержки только на фотографии и не заявлять, что все тормоза устранены.
- Последующая запись документации и исправленных тестов идёт с `[skip ci]`: production остаётся на указанном проверенном SHA, повторный деплой документации не нужен.
